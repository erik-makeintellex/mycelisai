# Mycelis Project Environment Variables
# Copy this file to .env and adjust values as needed.

# --- Database (PostgreSQL) ---
# Used by Core Service and Infrastructure
DB_HOST=mycelis-core-postgresql
DB_PORT=5432
DB_USER=mycelis
DB_PASSWORD=password
DB_NAME=cortex

# --- Infrastructure ---
# Postgres Root Password (for Init/Mgmt)
POSTGRES_PASSWORD=password
POSTGRES_USER=mycelis
POSTGRES_DB=cortex

# --- Core Service ---
# API Listening Port
PORT=8080

# --- NATS ---
# Internal Cluster URL
NATS_URL=nats://mycelis-core-nats:4222

# --- Phase 0: Security ---
# API authentication key (server refuses to start without this)
MYCELIS_API_KEY=change-me-to-a-random-secret
# Workspace sandbox root for agent file tools
MYCELIS_WORKSPACE=./workspace

-- Mycelis current schema baseline.
-- Source revision: 773e534c09710792607d243bc5f56aab85fa5ecc
-- Source manifest SHA-256: 16b6caab761aa22668bbf12133ed980a301cdd810f469fe497bb8d84807a2cdc
-- Forward migration count: 63; terminal source: core/migrations/064_code_context.up.sql
-- Generated mechanically; source blocks preserve blob bytes except declared normalization.
-- This file owns one transaction; declared transforms remove 063's nested wrapper and historical trailing whitespace.

BEGIN;

-- BEGIN SOURCE: core/migrations/001_init_memory.sql blob=bb3812e64d3cb15e2be7d9daf9395377f824bc1c source_bytes=1269 source_sha256=4de9e0d96f2a14a4dcb3feaf045d278739c21350e6b355ffd79be75188a3f00b rendered_bytes=1269 rendered_sha256=4de9e0d96f2a14a4dcb3feaf045d278739c21350e6b355ffd79be75188a3f00b transform=none
-- Enable Vector Search for future Intelligence phases
CREATE EXTENSION IF NOT EXISTS vector;
CREATE EXTENSION IF NOT EXISTS pg_trgm;

-- 1. The "Log" (Immutable Event History)
-- Optimized for high-volume time-series writes.
CREATE TABLE IF NOT EXISTS log_entries (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    trace_id TEXT NOT NULL,
    timestamp TIMESTAMPTZ NOT NULL,
    level TEXT NOT NULL,       -- INFO, WARN, ERROR
    source TEXT NOT NULL,      -- agent:finance
    intent TEXT NOT NULL,      -- spend_money
    message TEXT,
    context JSONB,             -- The full payload snapshot
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- Optimize for Cortex UI Stream retrieval
CREATE INDEX IF NOT EXISTS idx_logs_time ON log_entries(timestamp DESC);
CREATE INDEX IF NOT EXISTS idx_logs_source ON log_entries(source);

-- 2. The "State" (Current Projected Reality)
-- A unified registry of who is alive and what they are doing.
CREATE TABLE IF NOT EXISTS agent_registry (
    agent_id TEXT PRIMARY KEY,
    team_id TEXT NOT NULL,
    status TEXT NOT NULL,      -- IDLE, BUSY, OFFLINE, ERROR
    last_seen TIMESTAMPTZ NOT NULL,
    meta JSONB,                -- Battery, Location, Current Task
    last_error_summary TEXT    -- For self-healing logic
);
-- END SOURCE: core/migrations/001_init_memory.sql

-- BEGIN SOURCE: core/migrations/002_schema_v6.up.sql blob=967466ee81e01b714816939b0b074745deeefbfa source_bytes=1015 source_sha256=738ed7f2872fe105316c8020b54a1bc64303d16d5e846d863be0a6624c580d92 rendered_bytes=1015 rendered_sha256=738ed7f2872fe105316c8020b54a1bc64303d16d5e846d863be0a6624c580d92 transform=none
-- Migration 002: V6 Core Schema (Identity & Provisioning)

-- 1. Users (Tier 0 Authority)
CREATE TABLE IF NOT EXISTS users (
    id UUID PRIMARY KEY,
    username TEXT UNIQUE NOT NULL,
    role TEXT DEFAULT 'operator', -- admin, operator, observer
    settings JSONB DEFAULT '{}',
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 2. Teams (Tenancy)
CREATE TABLE IF NOT EXISTS teams (
    id UUID PRIMARY KEY,
    owner_id UUID REFERENCES users(id),
    name TEXT NOT NULL,
    role TEXT DEFAULT 'admin', -- Contextual role
    settings JSONB DEFAULT '{}',
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 3. Service Manifests (The Forge Output)
CREATE TABLE IF NOT EXISTS service_manifests (
    id UUID PRIMARY KEY,
    team_id UUID REFERENCES teams(id),
    name TEXT NOT NULL,
    manifest JSONB NOT NULL, -- The immutable JSON config generated by The Architect
    status TEXT DEFAULT 'draft', -- draft, active, paused, error
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);
-- END SOURCE: core/migrations/002_schema_v6.up.sql

-- BEGIN SOURCE: core/migrations/003_mission_hierarchy.up.sql blob=c623a55dd62c9d17dbf86a6dd10532d7ae48469a source_bytes=1009 source_sha256=b947b576f993a156b2b30fefd96bc491ec2b55c60fac17b4dd8969e69ca01d56 rendered_bytes=1008 rendered_sha256=558eea2de63ae1a100520ccdd29156af8d5fa235faa4ccc1c5053f451af6f299 transform=strip_trailing_whitespace
-- 1. Create the Missions Table (The "Constitution")
CREATE TABLE IF NOT EXISTS missions (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    owner_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    name TEXT NOT NULL,
    directive TEXT NOT NULL, -- The "Prime Directive" (e.g., "Symbiotic Swarm")
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- 2. Update Teams for Hierarchy
ALTER TABLE teams
    ADD COLUMN IF NOT EXISTS mission_id UUID REFERENCES missions(id) ON DELETE CASCADE,
    ADD COLUMN IF NOT EXISTS parent_id UUID REFERENCES teams(id) ON DELETE SET NULL,
    ADD COLUMN IF NOT EXISTS path TEXT DEFAULT ''; -- Materialized Path (e.g. "root_id.sub_id")

-- 3. Indexes for Tree Traversal Performance
CREATE INDEX IF NOT EXISTS idx_teams_mission_id ON teams(mission_id);
CREATE INDEX IF NOT EXISTS idx_teams_parent_id ON teams(parent_id);
CREATE INDEX IF NOT EXISTS idx_teams_path ON teams(path); -- Enables fast "Get all descendants" queries
-- END SOURCE: core/migrations/003_mission_hierarchy.up.sql

-- BEGIN SOURCE: core/migrations/004_registry.up.sql blob=d51ab5743dc02fd5fbac24b806dc4af032424386 source_bytes=1126 source_sha256=67315b4d65b01b5b2db7c1deca8ac0c3cc1b88f7fa1898a3f4e362a97a713281 rendered_bytes=1126 rendered_sha256=67315b4d65b01b5b2db7c1deca8ac0c3cc1b88f7fa1898a3f4e362a97a713281 transform=none
-- 1. Connector Templates (The "Class")
CREATE TABLE IF NOT EXISTS connector_templates (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name TEXT NOT NULL,
    type TEXT NOT NULL, -- 'ingress' (Source) or 'egress' (Sink)
    image TEXT NOT NULL, -- Docker image (e.g. 'mycelis/weather-poller')
    config_schema JSONB NOT NULL, -- JSON Schema for user inputs
    topic_template TEXT NOT NULL -- e.g. "swarm.data.weather.{{city}}"
);

-- 2. Active Connectors (The "Instance")
CREATE TABLE IF NOT EXISTS active_connectors (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    team_id UUID REFERENCES teams(id),
    template_id UUID REFERENCES connector_templates(id),
    name TEXT NOT NULL,
    config JSONB NOT NULL, -- The actual user values
    status TEXT DEFAULT 'provisioning' -- provisioning, running, error
);

-- 3. Agent Blueprints (Reusable Roles)
CREATE TABLE IF NOT EXISTS blueprints (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name TEXT NOT NULL,
    role TEXT NOT NULL,
    cognitive_profile TEXT NOT NULL, -- 'sentry', 'architect'
    tools JSONB DEFAULT '[]' -- List of MCP Tool IDs
);
-- END SOURCE: core/migrations/004_registry.up.sql

-- BEGIN SOURCE: core/migrations/005_nodes.up.sql blob=f168d2042b73f02891d919e73826364f824c84fd source_bytes=346 source_sha256=0ed9a0b6725a52294e6adb347932d001c8239b43cc176719f480c889b68ee093 rendered_bytes=346 rendered_sha256=0ed9a0b6725a52294e6adb347932d001c8239b43cc176719f480c889b68ee093 transform=none
-- Migration 005: Hardware Nodes (Bootstrap)

CREATE TABLE IF NOT EXISTS nodes (
    id TEXT PRIMARY KEY, -- "rpi-01", "drone-alpha", etc.
    status TEXT DEFAULT 'pending', -- pending, assigned, error
    capabilities JSONB DEFAULT '[]', -- ["gpio", "camera"]
    last_seen TIMESTAMPTZ DEFAULT NOW(),
    created_at TIMESTAMPTZ DEFAULT NOW()
);
-- END SOURCE: core/migrations/005_nodes.up.sql

-- BEGIN SOURCE: core/migrations/006_cognitive_registry.up.sql blob=a073dd87e8eb6685c60ec602aa480675727e9b95 source_bytes=1272 source_sha256=8b09245aa14e2aeb6ef487eb87e1f927c62455df436168418abdc84aae400485 rendered_bytes=1272 rendered_sha256=8b09245aa14e2aeb6ef487eb87e1f927c62455df436168418abdc84aae400485 transform=none
-- Migration 006: Cognitive Registry (The Brain Stem)
CREATE TABLE IF NOT EXISTS llm_providers (
    id TEXT PRIMARY KEY,
    -- "ollama-local", "openai-cloud"
    name TEXT NOT NULL,
    driver TEXT NOT NULL,
    -- "ollama", "openai", "anthropic"
    base_url TEXT NOT NULL,
    api_key_env_var TEXT,
    -- Name of env var to read key from (not the key itself!)
    config JSONB DEFAULT '{}',
    -- Model mapping, strict params
    is_default BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMPTZ DEFAULT NOW()
);
CREATE TABLE IF NOT EXISTS system_config (
    key TEXT PRIMARY KEY,
    -- "role.architect", "role.overseer"
    value TEXT NOT NULL,
    -- "ollama-local" (References llm_providers.id)
    updated_at TIMESTAMPTZ DEFAULT NOW()
);
-- Seed Data: The Sovereign Default
INSERT INTO llm_providers (id, name, driver, base_url, is_default)
VALUES (
        'local-sovereign',
        'Local Sovereign (Ollama)',
        'ollama',
        'http://ollama-service:11434',
        -- Internal K8s DNS
        TRUE
    ) ON CONFLICT (id) DO NOTHING;
-- Seed Data: System Roles
INSERT INTO system_config (key, value)
VALUES ('role.architect', 'local-sovereign'),
    ('role.coder', 'local-sovereign'),
    ('role.sentry', 'local-sovereign') ON CONFLICT (key) DO NOTHING;
-- END SOURCE: core/migrations/006_cognitive_registry.up.sql

-- BEGIN SOURCE: core/migrations/007_team_fabric.up.sql blob=16b3622619704cd82315c5f911452069095c8660 source_bytes=1604 source_sha256=34b7f27f1a3fd4c50020a9259356bebb999bdbce5e087b1643d9b87878b84c18 rendered_bytes=1603 rendered_sha256=92bf488ef94f0020a2cda314b85223926291c2ee82b03947f594b759584de0a7 transform=strip_trailing_whitespace
-- Migration 007: The Fractal Fabric (Data Seeds & Node Assignment)
-- 1. Link Nodes to Teams (Ownership)
ALTER TABLE nodes
ADD COLUMN IF NOT EXISTS team_id UUID REFERENCES teams(id);
-- 2. Seed Root Supervisor (The Architect)
INSERT INTO users (id, username, role, settings)
VALUES (
        '00000000-0000-0000-0000-000000000000',
        -- Fixed ID for Root
        'architect',
        'admin',
        '{"theme": "cyber-minimal"}'
    ) ON CONFLICT (id) DO NOTHING;
-- 3. Seed Root Mission (The Prime Directive)
INSERT INTO missions (id, owner_id, name, directive)
VALUES (
        '11111111-1111-1111-1111-111111111111',
        '00000000-0000-0000-0000-000000000000',
        'Symbiotic Swarm',
        'Establish a self-governing, fractal intelligence network that serves humanity.'
    ) ON CONFLICT (id) DO NOTHING;
-- 4. Seed Root Team (Mycelis Core)
INSERT INTO teams (
        id,
        owner_id,
        name,
        role,
        mission_id,
        parent_id,
        path
    )
VALUES (
        '22222222-2222-2222-2222-222222222222',
        '00000000-0000-0000-0000-000000000000',
        'Mycelis Core',
        'admin',
        '11111111-1111-1111-1111-111111111111',
        NULL,
        '22222222-2222-2222-2222-222222222222'
    ) ON CONFLICT (id) DO NOTHING;
-- 5. Create "Directives" Table (if separate entity needed)
-- For now we stick to Missions having a Directive.
-- But if we want sub-directives per team that are distinct from Mission...
-- Let's add a `directives` JSONB column to teams?
-- Or rely on Mission inheritance.
-- Start simple: Teams inherit Mission.
-- END SOURCE: core/migrations/007_team_fabric.up.sql

-- BEGIN SOURCE: core/migrations/008_context_engine.up.sql blob=e3d71391556ec289db9db0c3ca74a5a234eede6a source_bytes=1816 source_sha256=77713ab36fc18f280af992b78662a0f183a671c6a005c813de1cb1bc02945ab8 rendered_bytes=1816 rendered_sha256=77713ab36fc18f280af992b78662a0f183a671c6a005c813de1cb1bc02945ab8 transform=none
-- Migration 008: The Context Engine (Semantic Memory & SitReps)
-- 1. Context Vectors (Semantic Search)
-- Optimized for RAG (Retrieval Augmented Generation) by the Cognitive Engine.
-- We use 768 dimensions (STANDARD for modern embeddings like nomic-embed-text/v1.5 or all-minilm).
CREATE TABLE IF NOT EXISTS context_vectors (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    content TEXT NOT NULL,
    -- The text chunk
    metadata JSONB DEFAULT '{}',
    -- Source: {file: "task.md", type: "doc"}
    embedding vector(768),
    -- The semantic vector
    created_at TIMESTAMPTZ DEFAULT NOW()
);
-- Index for Fast ANN Search (IVFFlat)
-- Note: Requires some data to be effective but good to define template.
-- CREATE INDEX ON context_vectors USING ivfflat (embedding vector_cosine_ops) WITH (lists = 100);
-- 2. Situation Reports (SitReps)
-- Generated by the Archivist (LLM) summarizing time-series logs.
CREATE TABLE IF NOT EXISTS sitreps (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    team_id UUID REFERENCES teams(id),
    timestamp TIMESTAMPTZ DEFAULT NOW(),
    -- When the report was generated
    time_window_start TIMESTAMPTZ NOT NULL,
    time_window_end TIMESTAMPTZ NOT NULL,
    summary TEXT NOT NULL,
    -- High-level narrative
    key_events JSONB DEFAULT '[]',
    -- Structured extraction of critical events
    strategies TEXT,
    -- "Continue monitoring", "Deploy fix"
    status TEXT DEFAULT 'active' -- active, archived
);
-- 3. Short Term Memory (Working Context)
-- Fast Key-Value store for agent collaboration context (e.g. "What are we working on?")
CREATE TABLE IF NOT EXISTS working_memory (
    key TEXT PRIMARY KEY,
    -- "team:core:current_objective"
    value TEXT NOT NULL,
    updated_at TIMESTAMPTZ DEFAULT NOW(),
    expires_at TIMESTAMPTZ -- Optional TTL
);
-- END SOURCE: core/migrations/008_context_engine.up.sql

-- BEGIN SOURCE: core/migrations/009_seed_cognitive_local_dev.up.sql blob=eacdb255c8a64f787664c89070fa0ac0b0706927 source_bytes=1456 source_sha256=00ef225c32bc8244e5bd1211ffccb5c611c4cc74ca71916880fee79f2e6deaff rendered_bytes=1456 rendered_sha256=00ef225c32bc8244e5bd1211ffccb5c611c4cc74ca71916880fee79f2e6deaff transform=none
-- Migration 009: Seed Cognitive Registry for Local Development
-- Dependency: Migration 006 (creates llm_providers + system_config tables)
--
-- WHY: The Meta-Architect, Archivist, and all cognitive profiles require
-- entries in llm_providers and system_config to resolve. Without this seed,
-- the system returns 502 on any cognitive endpoint when the YAML config
-- fails to load or DB is the only config source.

-- 1. Local Dev Ollama Provider (LAN-accessible Ollama instance)
INSERT INTO llm_providers (id, name, driver, base_url, config, is_default)
VALUES (
    'local-ollama-dev',
    'Local Ollama (Dev LAN)',
    'ollama',
    'http://192.168.50.156:11434',
    '{"model_id": "qwen2.5-coder:7b"}'::jsonb,
    FALSE
) ON CONFLICT (id) DO UPDATE SET
    base_url = EXCLUDED.base_url,
    config = EXCLUDED.config;

-- 2. Ensure K8s sovereign entry has model config (patch 006 gap)
UPDATE llm_providers
SET config = '{"model_id": "qwen2.5-coder:7b"}'::jsonb
WHERE id = 'local-sovereign' AND (config IS NULL OR config = '{}'::jsonb);

-- 3. Complete Role→Provider Mappings (006 only had architect, coder, sentry)
INSERT INTO system_config (key, value) VALUES
    ('role.architect', 'local-sovereign'),
    ('role.coder',     'local-sovereign'),
    ('role.sentry',    'local-sovereign'),
    ('role.chat',      'local-sovereign'),
    ('role.creative',  'local-sovereign'),
    ('role.overseer',  'local-sovereign')
ON CONFLICT (key) DO NOTHING;
-- END SOURCE: core/migrations/009_seed_cognitive_local_dev.up.sql

-- BEGIN SOURCE: core/migrations/010_mission_activation.up.sql blob=1da4932d5177c5e63c0f6ea3037889e750876bed source_bytes=367 source_sha256=03601481d47c7bf93adf2e27be592d9869a37bc90d467b8615adb80fb10337be rendered_bytes=367 rendered_sha256=03601481d47c7bf93adf2e27be592d9869a37bc90d467b8615adb80fb10337be transform=none
-- Migration 010: Mission Activation State
-- Adds status tracking and activation timestamp to missions table
-- for the Unified Host Internalization (Phase 6.0).

ALTER TABLE missions
    ADD COLUMN IF NOT EXISTS status TEXT DEFAULT 'draft',
    ADD COLUMN IF NOT EXISTS activated_at TIMESTAMPTZ;

CREATE INDEX IF NOT EXISTS idx_missions_status ON missions(status);
-- END SOURCE: core/migrations/010_mission_activation.up.sql

-- BEGIN SOURCE: core/migrations/011_fix_nodes_type_schema.up.sql blob=6576c5bdb6589a6f754bde271010211a6f5499c5 source_bytes=243 source_sha256=23caa66a8b002be21a5305be2901ca1d04148a87d6802597640e1abc7f89a02d rendered_bytes=243 rendered_sha256=23caa66a8b002be21a5305be2901ca1d04148a87d6802597640e1abc7f89a02d transform=none
-- Migration 011: Fix Nodes Schema Drift
-- Adds missing 'type' column to nodes table required by heartbeat logic.
ALTER TABLE nodes
ADD COLUMN IF NOT EXISTS type TEXT DEFAULT 'worker';
CREATE INDEX IF NOT EXISTS idx_nodes_type ON nodes(type);
-- END SOURCE: core/migrations/011_fix_nodes_type_schema.up.sql

-- BEGIN SOURCE: core/migrations/012_fix_nodes_specs_schema.up.sql blob=1479c85058104e9c71d6e4434a93063468169df1 source_bytes=186 source_sha256=2bbf13060806253f9679364775801a0853a310bd43b60516aaf31c3d1cd248ba rendered_bytes=186 rendered_sha256=2bbf13060806253f9679364775801a0853a310bd43b60516aaf31c3d1cd248ba transform=none
-- Migration 012: Fix Nodes Specs Schema
-- Adds missing 'specs' column to nodes table required by bootstrap service.
ALTER TABLE nodes
ADD COLUMN IF NOT EXISTS specs JSONB DEFAULT '{}';
-- END SOURCE: core/migrations/012_fix_nodes_specs_schema.up.sql

-- BEGIN SOURCE: core/migrations/013_fix_provider_ip.up.sql blob=9c6fd1005ac79d230b0337e2834b23228c40faff source_bytes=666 source_sha256=81b087f1f07bba103aa89799edda1be2a6f473f3e1b97f7dd41c34d91ecff06d rendered_bytes=666 rendered_sha256=81b087f1f07bba103aa89799edda1be2a6f473f3e1b97f7dd41c34d91ecff06d transform=none
-- Migration 013: Fix LLM Provider URL and Roles
-- Align provider 'local-ollama-dev' to localhost and map roles to it.
-- 1. Correct the URL for the dev provider (was 192.168...)
UPDATE llm_providers
SET base_url = 'http://localhost:11434'
WHERE id = 'local-ollama-dev';
-- 2. Repoint roles to the working dev provider
INSERT INTO system_config (key, value)
VALUES ('role.architect', 'local-ollama-dev'),
    ('role.coder', 'local-ollama-dev'),
    ('role.sentry', 'local-ollama-dev'),
    ('role.chat', 'local-ollama-dev'),
    ('role.creative', 'local-ollama-dev'),
    ('role.overseer', 'local-ollama-dev') ON CONFLICT (key) DO
UPDATE
SET value = EXCLUDED.value;
-- END SOURCE: core/migrations/013_fix_provider_ip.up.sql

-- BEGIN SOURCE: core/migrations/014_fix_provider_url_suffix.up.sql blob=7f16235e35937f22a649a673207e2badd06b9749 source_bytes=204 source_sha256=72d3c556b18a7bfdeae3f9e1efba47948fc1a34ce4200df1f0349e3b9aa3e32b rendered_bytes=204 rendered_sha256=72d3c556b18a7bfdeae3f9e1efba47948fc1a34ce4200df1f0349e3b9aa3e32b transform=none
-- Migration 014: Fix LLM Provider URL Suffix
-- Append /v1 to local-ollama-dev URL for OpenAI compatibility.
UPDATE llm_providers
SET base_url = 'http://localhost:11434/v1'
WHERE id = 'local-ollama-dev';
-- END SOURCE: core/migrations/014_fix_provider_url_suffix.up.sql

-- BEGIN SOURCE: core/migrations/015_fix_provider_ipv4.up.sql blob=d3c1b2f063e314be931084dfea724efdad0685f5 source_bytes=192 source_sha256=c41368bf59832ab4b383cdda8b2add21745a6eb290cdd9e50da0a9b6b6715311 rendered_bytes=192 rendered_sha256=c41368bf59832ab4b383cdda8b2add21745a6eb290cdd9e50da0a9b6b6715311 transform=none
-- Migration 015: Fix Provider IPv4
-- Force 127.0.0.1 to avoid localhost IPv6 resolution issues.
UPDATE llm_providers
SET base_url = 'http://127.0.0.1:11434/v1'
WHERE id = 'local-ollama-dev';
-- END SOURCE: core/migrations/015_fix_provider_ipv4.up.sql

-- BEGIN SOURCE: core/migrations/016_mcp_registry.up.sql blob=761e04f0cafa7ae71873bcac1fc3b1ddaa216dd2 source_bytes=881 source_sha256=90649b6fc52a9e546f9257ebccad9c115b5134bf4a4da3a0e94d4ddcd661aa1c rendered_bytes=881 rendered_sha256=90649b6fc52a9e546f9257ebccad9c115b5134bf4a4da3a0e94d4ddcd661aa1c transform=none
CREATE TABLE IF NOT EXISTS mcp_servers (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name TEXT NOT NULL UNIQUE,
    transport TEXT NOT NULL,
    command TEXT,
    args JSONB DEFAULT '[]',
    env JSONB DEFAULT '{}',
    url TEXT,
    headers JSONB DEFAULT '{}',
    status TEXT DEFAULT 'installed',
    error_message TEXT,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE TABLE IF NOT EXISTS mcp_tools (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    server_id UUID NOT NULL REFERENCES mcp_servers(id) ON DELETE CASCADE,
    name TEXT NOT NULL,
    description TEXT,
    input_schema JSONB DEFAULT '{}',
    created_at TIMESTAMPTZ DEFAULT NOW(),
    UNIQUE(server_id, name)
);

CREATE INDEX IF NOT EXISTS idx_mcp_tools_server ON mcp_tools(server_id);
CREATE INDEX IF NOT EXISTS idx_mcp_tools_name ON mcp_tools(name);
-- END SOURCE: core/migrations/016_mcp_registry.up.sql

-- BEGIN SOURCE: core/migrations/017_agent_catalogue.up.sql blob=2bd6e84da1f3fcef72b98190e7f85cc19a69c193 source_bytes=1106 source_sha256=3ac34c12b4b75c99c2c2568079c8b12e06508920828086894dbbbbeb03e38172 rendered_bytes=1106 rendered_sha256=3ac34c12b4b75c99c2c2568079c8b12e06508920828086894dbbbbeb03e38172 transform=none
-- 017: Agent Catalogue — persistent agent template definitions
-- These are reusable agent definitions that can be assigned to teams.
-- Distinct from service_manifests (which are team-scoped runtime instances).

CREATE TABLE IF NOT EXISTS agent_catalogue (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name TEXT NOT NULL UNIQUE,
    role TEXT NOT NULL,                          -- cognitive, sensory, actuation, ledger
    system_prompt TEXT,
    model TEXT,                                  -- LLM profile name (e.g., "chat", "coder")
    tools JSONB DEFAULT '[]',                    -- MCP tool name bindings
    inputs JSONB DEFAULT '[]',                   -- NATS topic patterns
    outputs JSONB DEFAULT '[]',                  -- NATS topic patterns
    verification_strategy TEXT,                  -- none, semantic, empirical
    verification_rubric JSONB DEFAULT '[]',      -- rubric lines
    validation_command TEXT,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_agent_catalogue_role ON agent_catalogue(role);
-- END SOURCE: core/migrations/017_agent_catalogue.up.sql

-- BEGIN SOURCE: core/migrations/018_artifacts.up.sql blob=777ca05154214fc06084cb1032fbd9348184a180 source_bytes=2589 source_sha256=d827bfbbd8824704e8ef5f351c3989223a1f07141afad9dde498916dde249eb3 rendered_bytes=2589 rendered_sha256=d827bfbbd8824704e8ef5f351c3989223a1f07141afad9dde498916dde249eb3 transform=none
-- 018: Artifacts — structured agent output persistence
-- Every agent output (code, documents, media, data) is stored here.
-- Links to mission → team → agent provenance chain.
-- Content can be inline (text/code/json) or file-referenced (/data/artifacts/).

CREATE TABLE IF NOT EXISTS artifacts (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    mission_id UUID REFERENCES missions(id) ON DELETE SET NULL,
    team_id UUID REFERENCES teams(id) ON DELETE SET NULL,
    agent_id TEXT NOT NULL,                          -- agent manifest ID (not UUID — matches AgentManifest.ID)
    trace_id TEXT,                                   -- CTS correlation ID
    artifact_type TEXT NOT NULL,                     -- code, document, image, audio, data, file, chart
    title TEXT NOT NULL,
    content_type TEXT NOT NULL DEFAULT 'text/plain', -- MIME type
    content TEXT,                                    -- inline content (code, markdown, json, small text)
    file_path TEXT,                                  -- relative path under /data/artifacts/ (for binary/large files)
    file_size_bytes BIGINT,
    metadata JSONB DEFAULT '{}',                     -- type-specific: {language: "go", filename: "main.go"} etc.
    trust_score DOUBLE PRECISION,                    -- inherited from CTSEnvelope
    status TEXT NOT NULL DEFAULT 'pending',          -- pending, approved, rejected, archived
    created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_artifacts_mission ON artifacts(mission_id);
CREATE INDEX IF NOT EXISTS idx_artifacts_team ON artifacts(team_id);
CREATE INDEX IF NOT EXISTS idx_artifacts_agent ON artifacts(agent_id);
CREATE INDEX IF NOT EXISTS idx_artifacts_type ON artifacts(artifact_type);
CREATE INDEX IF NOT EXISTS idx_artifacts_status ON artifacts(status);

-- Working memory: key-value store for fast agent state (hot cache spillover).
-- Agents can read/write ephemeral state here with optional TTL.
-- This supplements the existing working_memory table from 008 by adding
-- agent and mission scoping.
CREATE TABLE IF NOT EXISTS agent_state (
    agent_id TEXT NOT NULL,
    key TEXT NOT NULL,
    value TEXT NOT NULL,
    mission_id UUID REFERENCES missions(id) ON DELETE CASCADE,
    updated_at TIMESTAMPTZ DEFAULT NOW(),
    expires_at TIMESTAMPTZ,                          -- optional TTL for auto-cleanup
    PRIMARY KEY (agent_id, key)
);

CREATE INDEX IF NOT EXISTS idx_agent_state_mission ON agent_state(mission_id);
CREATE INDEX IF NOT EXISTS idx_agent_state_expires ON agent_state(expires_at) WHERE expires_at IS NOT NULL;
-- END SOURCE: core/migrations/018_artifacts.up.sql

-- BEGIN SOURCE: core/migrations/019_agent_memories.up.sql blob=2cf70216e2e24551d0846e3285edde18033dd65d source_bytes=560 source_sha256=ad9365f7672efe37b085066e09059bf4217bfb99639f3bc91a3316a6a2d568eb rendered_bytes=560 rendered_sha256=ad9365f7672efe37b085066e09059bf4217bfb99639f3bc91a3316a6a2d568eb transform=none
-- Phase 7.7: Agent Memories (persistent knowledge for admin/council)
CREATE TABLE IF NOT EXISTS agent_memories (
    id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    category    VARCHAR(50) NOT NULL DEFAULT 'fact',
    content     TEXT NOT NULL,
    context     TEXT,
    created_at  TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- Indexes for recall queries
CREATE INDEX IF NOT EXISTS idx_agent_memories_category ON agent_memories(category);
CREATE INDEX IF NOT EXISTS idx_agent_memories_content_trgm ON agent_memories USING gin (content gin_trgm_ops);
-- END SOURCE: core/migrations/019_agent_memories.up.sql

-- BEGIN SOURCE: core/migrations/020_cascade_service_manifests.up.sql blob=8d867f500637d9360261a153fed77fec09abefc4 source_bytes=421 source_sha256=d9b71f1c585fb9859fd39f165d3412f73707278205c22bcebf5e5fcf6166ffa1 rendered_bytes=421 rendered_sha256=d9b71f1c585fb9859fd39f165d3412f73707278205c22bcebf5e5fcf6166ffa1 transform=none
-- Fix service_manifests → teams FK to cascade on delete.
-- Without this, DELETE FROM missions cascades to teams but fails
-- on the service_manifests FK constraint (orphan rows).
ALTER TABLE service_manifests
    DROP CONSTRAINT IF EXISTS service_manifests_team_id_fkey;
ALTER TABLE service_manifests
    ADD CONSTRAINT service_manifests_team_id_fkey
    FOREIGN KEY (team_id) REFERENCES teams(id) ON DELETE CASCADE;
-- END SOURCE: core/migrations/020_cascade_service_manifests.up.sql

-- BEGIN SOURCE: core/migrations/021_conversation_summaries.up.sql blob=0bb9c679b8cc66e806a16c1c62bdba1c528bc90f source_bytes=856 source_sha256=634431c884de77b5802ee3d8e9b2bcc6853efc5902ef2329fe2d650318d37901 rendered_bytes=856 rendered_sha256=634431c884de77b5802ee3d8e9b2bcc6853efc5902ef2329fe2d650318d37901 transform=none
-- Migration 021: Conversation Summaries
-- Stores LLM-generated summaries of chat windows for long-term memory.
-- Vectors go into existing context_vectors table (reuse pgvector infra).

CREATE TABLE IF NOT EXISTS conversation_summaries (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    agent_id TEXT NOT NULL DEFAULT 'admin',
    summary TEXT NOT NULL,
    key_topics TEXT[] DEFAULT '{}',
    user_preferences JSONB DEFAULT '{}',
    personality_notes TEXT,
    data_references JSONB DEFAULT '[]',
    message_count INT DEFAULT 0,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_convsumm_agent ON conversation_summaries(agent_id);
CREATE INDEX IF NOT EXISTS idx_convsumm_created ON conversation_summaries(created_at DESC);
CREATE INDEX IF NOT EXISTS idx_convsumm_topics ON conversation_summaries USING gin(key_topics);
-- END SOURCE: core/migrations/021_conversation_summaries.up.sql

-- BEGIN SOURCE: core/migrations/022_intent_proofs.up.sql blob=0b9264d15c500869a64e2903066c8fa711805ca6 source_bytes=2360 source_sha256=201497a2fa4c8a92a734caa3919252882c43b3b8246ecec795710ddf6d26a1f0 rendered_bytes=2360 rendered_sha256=201497a2fa4c8a92a734caa3919252882c43b3b8246ecec795710ddf6d26a1f0 transform=none
-- Migration 022: Intent Proofs & Confirm Tokens (CE-1: Orchestration Templates)
--
-- Stores intent proof bundles for Chat-to-Proposal executions.
-- Confirm tokens gate state mutation: no token = no commit.
-- Audit events use the existing log_entries table (level='audit').

CREATE TABLE IF NOT EXISTS intent_proofs (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    template_id TEXT NOT NULL,                           -- 'chat-to-answer' or 'chat-to-proposal'
    resolved_intent TEXT NOT NULL,                        -- classified intent text
    user_confirmation_token TEXT,                         -- UUID set when user confirms
    permission_check TEXT NOT NULL DEFAULT 'pass',        -- 'pass' or 'fail'
    policy_decision TEXT NOT NULL DEFAULT 'allow',        -- 'allow', 'deny', 'require_approval'
    scope_validation JSONB,                              -- {tools, affected_resources, risk_level}
    audit_event_id UUID,                                 -- references log_entries(id)
    mission_id UUID REFERENCES missions(id) ON DELETE SET NULL,
    status TEXT NOT NULL DEFAULT 'pending',               -- pending, confirmed, denied
    created_at TIMESTAMPTZ DEFAULT NOW(),
    confirmed_at TIMESTAMPTZ,
    expires_at TIMESTAMPTZ                               -- proof expiry (matches token TTL)
);

CREATE INDEX IF NOT EXISTS idx_intent_proofs_status ON intent_proofs(status);
CREATE INDEX IF NOT EXISTS idx_intent_proofs_mission ON intent_proofs(mission_id);
CREATE INDEX IF NOT EXISTS idx_intent_proofs_token ON intent_proofs(user_confirmation_token)
    WHERE user_confirmation_token IS NOT NULL;

-- Confirm tokens: single-use, TTL-bound tokens that gate state mutation.
-- Generated during negotiate, consumed during commit.
CREATE TABLE IF NOT EXISTS confirm_tokens (
    token UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    intent_proof_id UUID NOT NULL REFERENCES intent_proofs(id) ON DELETE CASCADE,
    template_id TEXT NOT NULL,
    consumed BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    expires_at TIMESTAMPTZ NOT NULL,                     -- 15 min from creation
    consumed_at TIMESTAMPTZ
);

CREATE INDEX IF NOT EXISTS idx_confirm_tokens_proof ON confirm_tokens(intent_proof_id);
CREATE INDEX IF NOT EXISTS idx_confirm_tokens_expires ON confirm_tokens(expires_at)
    WHERE consumed = FALSE;
-- END SOURCE: core/migrations/022_intent_proofs.up.sql

-- BEGIN SOURCE: core/migrations/023_mission_runs.up.sql blob=78d9dcaf1f775967d9f334462e5f3c7c3bdd7803 source_bytes=1476 source_sha256=4157eaa6623195f0e799ef225ed0a0603aabbea8a52b6f1dcf8d8c51c65f2d90 rendered_bytes=1476 rendered_sha256=4157eaa6623195f0e799ef225ed0a0603aabbea8a52b6f1dcf8d8c51c65f2d90 transform=none
-- Migration 023: Mission Runs (V7 Event Spine — Team A)
--
-- Every mission execution is a "run". A mission definition may have many runs.
-- Runs form a chain: parent → trigger → child (for V7 Trigger Engine, Team B).
-- tenant_id = 'default' for V7 (schema-ready, operationally single-tenant).

CREATE TABLE IF NOT EXISTS mission_runs (
    id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    mission_id    VARCHAR(255) NOT NULL,                            -- references missions.id (label or UUID)
    tenant_id     VARCHAR(255) NOT NULL DEFAULT 'default',
    status        VARCHAR(50)  NOT NULL DEFAULT 'pending',          -- pending|running|completed|failed
    run_depth     INT          NOT NULL DEFAULT 0,                  -- recursion depth for trigger chains
    parent_run_id UUID         REFERENCES mission_runs(id),        -- set by Trigger Engine (Team B)
    trigger_rule_id UUID,                                           -- set by Trigger Engine (Team B)
    started_at    TIMESTAMPTZ  NOT NULL DEFAULT NOW(),
    completed_at  TIMESTAMPTZ,
    metadata      JSONB
);

CREATE INDEX IF NOT EXISTS idx_mission_runs_mission   ON mission_runs(mission_id);
CREATE INDEX IF NOT EXISTS idx_mission_runs_status    ON mission_runs(status);
CREATE INDEX IF NOT EXISTS idx_mission_runs_started   ON mission_runs(started_at DESC);
CREATE INDEX IF NOT EXISTS idx_mission_runs_parent    ON mission_runs(parent_run_id)
    WHERE parent_run_id IS NOT NULL;
-- END SOURCE: core/migrations/023_mission_runs.up.sql

-- BEGIN SOURCE: core/migrations/024_mission_events.up.sql blob=ae9ac95fa22fad9d85aef93022004bb615ec9e40 source_bytes=1351 source_sha256=d15f317abfd6f0a9c328caafc2826e0de6c5748e976c0060bd6825836ad0a494 rendered_bytes=1351 rendered_sha256=d15f317abfd6f0a9c328caafc2826e0de6c5748e976c0060bd6825836ad0a494 transform=none
-- Migration 024: Mission Events (V7 Event Spine — Team A)
--
-- The authoritative audit trail for every action in a mission run.
-- Dual-layer design: this table = persistent record; CTS = real-time transport.
-- DB-first rule: Emit() persists here BEFORE publishing the CTS signal.
-- If NATS is offline, events still persist here (degraded mode, no data loss).

CREATE TABLE IF NOT EXISTS mission_events (
    id            UUID         PRIMARY KEY DEFAULT gen_random_uuid(),
    run_id        UUID         NOT NULL REFERENCES mission_runs(id) ON DELETE CASCADE,
    tenant_id     VARCHAR(255) NOT NULL DEFAULT 'default',
    event_type    VARCHAR(100) NOT NULL,   -- tool.invoked, mission.started, artifact.created, etc.
    severity      VARCHAR(20)  NOT NULL DEFAULT 'info',  -- debug|info|warn|error
    source_agent  VARCHAR(255),
    source_team   VARCHAR(255),
    payload       JSONB,
    audit_event_id UUID,                  -- links to log_entries(id) for CE-1 audit events
    emitted_at    TIMESTAMPTZ  NOT NULL DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_mission_events_run      ON mission_events(run_id, emitted_at);
CREATE INDEX IF NOT EXISTS idx_mission_events_type     ON mission_events(event_type);
CREATE INDEX IF NOT EXISTS idx_mission_events_agent    ON mission_events(source_agent)
    WHERE source_agent IS NOT NULL;
-- END SOURCE: core/migrations/024_mission_events.up.sql

-- BEGIN SOURCE: core/migrations/025_trigger_rules.up.sql blob=f4543ffcb09eeccd53e3aa9f4b9fbc6411a8e4b6 source_bytes=1505 source_sha256=fac569157f226745ba4752bcacb74e78c5571ce089dfc26ce8e39cf7b3007a85 rendered_bytes=1505 rendered_sha256=fac569157f226745ba4752bcacb74e78c5571ce089dfc26ce8e39cf7b3007a85 transform=none
-- V7 Team B: Trigger Rules — declarative IF/THEN rules evaluated on event ingest.
-- Depends on: mission_runs (023), mission_events (024).

CREATE TABLE IF NOT EXISTS trigger_rules (
    id                  UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    tenant_id           TEXT NOT NULL DEFAULT 'default',
    name                TEXT NOT NULL,
    description         TEXT,
    event_pattern       TEXT NOT NULL,                       -- e.g. "mission.completed", "tool.completed"
    condition           JSONB NOT NULL DEFAULT '{}',         -- optional filter on event payload
    target_mission_id   TEXT NOT NULL,                       -- mission to launch when rule fires
    mode                TEXT NOT NULL DEFAULT 'propose',     -- "propose" | "auto_execute"
    cooldown_seconds    INT NOT NULL DEFAULT 60,             -- min seconds between consecutive firings
    max_depth           INT NOT NULL DEFAULT 5,              -- recursion guard (max trigger chain depth)
    max_active_runs     INT NOT NULL DEFAULT 3,              -- concurrency guard per target mission
    is_active           BOOLEAN NOT NULL DEFAULT true,
    last_fired_at       TIMESTAMPTZ,
    created_at          TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at          TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS trigger_rules_tenant_active ON trigger_rules(tenant_id, is_active);
CREATE INDEX IF NOT EXISTS trigger_rules_event_pattern ON trigger_rules(event_pattern) WHERE is_active = true;
-- END SOURCE: core/migrations/025_trigger_rules.up.sql

-- BEGIN SOURCE: core/migrations/026_trigger_executions.up.sql blob=5e5b2277a43d89c5d29ea1fe8369197560142b23 source_bytes=1013 source_sha256=04aa0f3e218526ceb96786d73a1fac200396ec9de71b6e9fe2c7f6f613547417 rendered_bytes=1013 rendered_sha256=04aa0f3e218526ceb96786d73a1fac200396ec9de71b6e9fe2c7f6f613547417 transform=none
-- V7 Team B: Trigger Executions — audit log of every rule evaluation.
-- Depends on: trigger_rules (025), mission_runs (023), mission_events (024).

CREATE TABLE IF NOT EXISTS trigger_executions (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    rule_id         UUID NOT NULL REFERENCES trigger_rules(id) ON DELETE CASCADE,
    event_id        TEXT NOT NULL,                   -- mission_events.id that triggered evaluation
    run_id          TEXT,                             -- child run created (NULL if skipped)
    status          TEXT NOT NULL,                    -- "fired" | "skipped" | "proposed"
    skip_reason     TEXT,                             -- e.g. "cooldown", "recursion_limit", "concurrency_limit", "condition_failed"
    executed_at     TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS trigger_executions_rule_id ON trigger_executions(rule_id, executed_at DESC);
CREATE INDEX IF NOT EXISTS trigger_executions_event_id ON trigger_executions(event_id);
-- END SOURCE: core/migrations/026_trigger_executions.up.sql

-- BEGIN SOURCE: core/migrations/028_context_snapshots.up.sql blob=f6728f881f32b1030757ce449cd37a7fc905eddd source_bytes=979 source_sha256=64e07c74e5f3b0d8c2bb5b3ed73400c940e4166a19840ddcbf53874604c8b30c rendered_bytes=979 rendered_sha256=64e07c74e5f3b0d8c2bb5b3ed73400c940e4166a19840ddcbf53874604c8b30c transform=none
-- Migration 028: Context Snapshots
-- Stores point-in-time snapshots of conversation context + provider assignments.
-- Used by mission profiles to cache and restore context when switching profiles.

CREATE TABLE IF NOT EXISTS context_snapshots (
    id             UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name           TEXT NOT NULL,
    description    TEXT,
    messages       JSONB NOT NULL DEFAULT '[]',   -- recent chat history (ChatMessage[])
    run_state      JSONB NOT NULL DEFAULT '{}',   -- active run_id, status, etc.
    role_providers JSONB NOT NULL DEFAULT '{}',   -- provider assignments at snapshot time {"architect":"ollama"}
    source_profile TEXT,                           -- profile ID that was active when snapshot taken
    tenant_id      TEXT NOT NULL DEFAULT 'default',
    created_at     TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS context_snapshots_tenant_created
    ON context_snapshots(tenant_id, created_at DESC);
-- END SOURCE: core/migrations/028_context_snapshots.up.sql

-- BEGIN SOURCE: core/migrations/029_mission_profiles.up.sql blob=7e06b3622bfb7c659dd9840237b2ae4d37bbceee source_bytes=1646 source_sha256=b2a7cc8008fef9b010a1ba714a3c793a2601705529da79dfba0a0e361f05d177 rendered_bytes=1646 rendered_sha256=b2a7cc8008fef9b010a1ba714a3c793a2601705529da79dfba0a0e361f05d177 transform=none
-- Migration 029: Mission Profiles
-- Named configuration sets that map agent roles to specific providers
-- and define reactive NATS subscriptions. Multiple profiles can be active
-- (auto_start=true profiles always run; at most one non-auto profile is active).

CREATE TABLE IF NOT EXISTS mission_profiles (
    id               UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name             TEXT NOT NULL,
    description      TEXT,
    role_providers   JSONB NOT NULL DEFAULT '{}',   -- {"architect":"vllm","coder":"ollama"}
    subscriptions    JSONB NOT NULL DEFAULT '[]',   -- [{"topic":"swarm.team.dev-team.*","condition":null}]
    context_strategy TEXT NOT NULL DEFAULT 'fresh', -- "fresh" | "warm" | "snapshot:<uuid>"
    auto_start       BOOLEAN NOT NULL DEFAULT false,
    is_active        BOOLEAN NOT NULL DEFAULT false,
    tenant_id        TEXT NOT NULL DEFAULT 'default',
    created_at       TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at       TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE UNIQUE INDEX IF NOT EXISTS mission_profiles_name_tenant
    ON mission_profiles(name, tenant_id);

CREATE INDEX IF NOT EXISTS mission_profiles_tenant_active
    ON mission_profiles(tenant_id, is_active);

-- Trigger to keep updated_at current
CREATE OR REPLACE FUNCTION update_mission_profiles_updated_at()
RETURNS TRIGGER AS $$
BEGIN
    NEW.updated_at = NOW();
    RETURN NEW;
END;
$$ LANGUAGE plpgsql;

DROP TRIGGER IF EXISTS mission_profiles_updated_at ON mission_profiles;
CREATE TRIGGER mission_profiles_updated_at
    BEFORE UPDATE ON mission_profiles
    FOR EACH ROW EXECUTE FUNCTION update_mission_profiles_updated_at();
-- END SOURCE: core/migrations/029_mission_profiles.up.sql

-- BEGIN SOURCE: core/migrations/030_conversation_turns.up.sql blob=df8b701dc5cc1a292150c28734d78fd9f8f1cbb2 source_bytes=1577 source_sha256=efc7009ac792c61b5f9b7efe7de5984a91274636d7f30aa882d217ed2b0a1fd1 rendered_bytes=1577 rendered_sha256=efc7009ac792c61b5f9b7efe7de5984a91274636d7f30aa882d217ed2b0a1fd1 transform=none
-- V7: Conversation turns — full-fidelity agent conversation log.
-- Separate from mission_events (lightweight audit); turns are full-text blobs (10KB+).
-- session_id groups turns for a single chat cycle; run_id is nullable (standing-team chats have no run).

CREATE TABLE IF NOT EXISTS conversation_turns (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    run_id          UUID,                        -- nullable: standing-team chats have no run
    session_id      UUID NOT NULL,               -- groups turns for one chat cycle
    tenant_id       VARCHAR(255) NOT NULL DEFAULT 'default',
    agent_id        VARCHAR(255) NOT NULL,       -- "admin", "council-architect", etc.
    team_id         VARCHAR(255),
    turn_index      INT NOT NULL,                -- order within session
    role            VARCHAR(50) NOT NULL,         -- system|user|assistant|tool_call|tool_result|interjection
    content         TEXT NOT NULL,
    provider_id     VARCHAR(100),
    model_used      VARCHAR(255),
    tool_name       VARCHAR(255),
    tool_args       JSONB,
    parent_turn_id  UUID,                        -- links tool_result → tool_call
    consultation_of VARCHAR(255),                -- who was consulted
    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_convturns_run     ON conversation_turns(run_id, created_at);
CREATE INDEX IF NOT EXISTS idx_convturns_session ON conversation_turns(session_id, turn_index);
CREATE INDEX IF NOT EXISTS idx_convturns_agent   ON conversation_turns(agent_id, created_at);
-- END SOURCE: core/migrations/030_conversation_turns.up.sql

-- BEGIN SOURCE: core/migrations/031_inception_recipes.up.sql blob=16a657c673e0533f94dfe53af94cbd63dee381a3 source_bytes=2181 source_sha256=898b9894d9ce72dfe3b826741bffadfb1f25d2f0b4aa2a173dd69229729d3153 rendered_bytes=2181 rendered_sha256=898b9894d9ce72dfe3b826741bffadfb1f25d2f0b4aa2a173dd69229729d3153 transform=none
-- V7: Inception Recipes — structured prompt patterns for Soma to store and recall.
-- When Soma successfully completes a complex task (blueprint creation, analysis, team setup),
-- it can distill a structured recipe capturing: how to ask for it, key parameters, and outcome shape.
-- Recipes are dual-persisted: RDBMS (structured query) + pgvector (semantic recall).

CREATE TABLE IF NOT EXISTS inception_recipes (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    tenant_id       VARCHAR(255) NOT NULL DEFAULT 'default',
    category        VARCHAR(100) NOT NULL,       -- 'blueprint', 'analysis', 'team_setup', 'deployment', 'research', etc.
    title           VARCHAR(500) NOT NULL,       -- "How to create a microservices deployment blueprint"
    intent_pattern  TEXT NOT NULL,               -- The structured way to phrase this request
    parameters      JSONB DEFAULT '{}',          -- Key parameters: {"service_count": "number of services", ...}
    example_prompt  TEXT,                        -- A concrete example prompt that worked well
    outcome_shape   TEXT,                        -- What the successful outcome looks like
    source_run_id   UUID,                        -- The run that generated this recipe (nullable)
    source_session_id UUID,                      -- The session that generated this (nullable)
    agent_id        VARCHAR(255) NOT NULL DEFAULT 'admin',
    tags            TEXT[] DEFAULT '{}',          -- Searchable tags for filtering
    quality_score   FLOAT DEFAULT 0.0,           -- Feedback quality (0.0 = unrated, 1.0 = excellent)
    usage_count     INT DEFAULT 0,               -- Times this recipe was retrieved and used
    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_inception_category ON inception_recipes(category);
CREATE INDEX IF NOT EXISTS idx_inception_agent    ON inception_recipes(agent_id, created_at DESC);
CREATE INDEX IF NOT EXISTS idx_inception_tags     ON inception_recipes USING gin(tags);
CREATE INDEX IF NOT EXISTS idx_inception_title_trgm ON inception_recipes USING gin(title gin_trgm_ops);
-- END SOURCE: core/migrations/031_inception_recipes.up.sql

-- BEGIN SOURCE: core/migrations/032_mcp_tool_sets.up.sql blob=c697ee5bb2da1ce0c658f4ab227388d7e6bf4a8e source_bytes=595 source_sha256=4aff8a659c5d9a77c4bcd1a7a902b29afda26689712e8b3173b12eceb32cefe0 rendered_bytes=595 rendered_sha256=4aff8a659c5d9a77c4bcd1a7a902b29afda26689712e8b3173b12eceb32cefe0 transform=none
-- 032: MCP Tool Sets — named bundles of mcp: tool references for agent-scoped binding.
-- Agents reference tool sets via "toolset:<name>" in their Tools[] manifest field.

CREATE TABLE IF NOT EXISTS mcp_tool_sets (
    id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name        TEXT NOT NULL UNIQUE,
    description TEXT,
    tool_refs   JSONB NOT NULL DEFAULT '[]',
    tenant_id   TEXT NOT NULL DEFAULT 'default',
    created_at  TIMESTAMPTZ DEFAULT NOW(),
    updated_at  TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_mcp_tool_sets_name ON mcp_tool_sets(name);
-- END SOURCE: core/migrations/032_mcp_tool_sets.up.sql

-- BEGIN SOURCE: core/migrations/033_temp_memory_channels.up.sql blob=3935e2ca3cd2f5d6c6dad317730b37c0c21d4ebb source_bytes=1544 source_sha256=33c1bd73726b5fadcd26e2663067345907d2b1078d1dd7a28c9ce8f230ee3f0e rendered_bytes=1544 rendered_sha256=33c1bd73726b5fadcd26e2663067345907d2b1078d1dd7a28c9ce8f230ee3f0e transform=none
-- V7 Sprint 0: Temporary Working Memory Channels
-- Restart-safe, short-horizon memory for lead agents (admin/council)
-- to preserve in-flight work and interaction continuity across service/provider restarts.

CREATE TABLE IF NOT EXISTS temp_memory_channels (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    tenant_id       TEXT NOT NULL DEFAULT 'default',
    channel_key     TEXT NOT NULL,                 -- e.g. lead.shared, lead.council-architect, interaction.contract
    owner_agent_id  TEXT NOT NULL,                 -- writer identity
    content         TEXT NOT NULL,                 -- checkpoint payload (markdown/text/json string)
    metadata        JSONB NOT NULL DEFAULT '{}',
    expires_at      TIMESTAMPTZ,                   -- optional TTL
    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_temp_memory_channel_tenant_updated
    ON temp_memory_channels(tenant_id, channel_key, updated_at DESC);

CREATE INDEX IF NOT EXISTS idx_temp_memory_expires_at
    ON temp_memory_channels(expires_at);

CREATE OR REPLACE FUNCTION update_temp_memory_channels_updated_at()
RETURNS TRIGGER AS $$
BEGIN
    NEW.updated_at = NOW();
    RETURN NEW;
END;
$$ LANGUAGE plpgsql;

DROP TRIGGER IF EXISTS temp_memory_channels_updated_at ON temp_memory_channels;
CREATE TRIGGER temp_memory_channels_updated_at
    BEFORE UPDATE ON temp_memory_channels
    FOR EACH ROW EXECUTE FUNCTION update_temp_memory_channels_updated_at();

-- END SOURCE: core/migrations/033_temp_memory_channels.up.sql

-- BEGIN SOURCE: core/migrations/034_collaboration_groups.up.sql blob=0bc06ed31a18e9b8279ba27c8e3a2b66b3a0ae5f source_bytes=1374 source_sha256=e1d5f4ddf4d7b1a4fc9089eaf8dd8ba6bd0ad08f14352bfe7e825fbcbe5d91d5 rendered_bytes=1374 rendered_sha256=e1d5f4ddf4d7b1a4fc9089eaf8dd8ba6bd0ad08f14352bfe7e825fbcbe5d91d5 transform=none
-- Migration 034: Root-admin collaboration groups (DB-backed persistence)
-- Supports Team A/B contracts:
-- - tenant-scoped group storage
-- - policy reference linkage
-- - audit linkage for create/update mutations

CREATE TABLE IF NOT EXISTS collaboration_groups (
    id UUID PRIMARY KEY,
    tenant_id TEXT NOT NULL DEFAULT 'default',
    name TEXT NOT NULL,
    goal_statement TEXT NOT NULL,
    work_mode TEXT NOT NULL,
    allowed_capabilities JSONB NOT NULL DEFAULT '[]'::jsonb,
    member_user_ids JSONB NOT NULL DEFAULT '[]'::jsonb,
    team_ids JSONB NOT NULL DEFAULT '[]'::jsonb,
    coordinator_profile TEXT NOT NULL DEFAULT '',
    approval_policy_ref TEXT NOT NULL DEFAULT '',
    status TEXT NOT NULL DEFAULT 'active',
    expiry TIMESTAMPTZ NULL,
    created_by TEXT NOT NULL,
    created_audit_event_id UUID NULL,
    updated_audit_event_id UUID NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_collaboration_groups_tenant ON collaboration_groups(tenant_id);
CREATE INDEX IF NOT EXISTS idx_collaboration_groups_status ON collaboration_groups(status);
CREATE INDEX IF NOT EXISTS idx_collaboration_groups_name ON collaboration_groups(name);
CREATE UNIQUE INDEX IF NOT EXISTS idx_collaboration_groups_tenant_name_unique
    ON collaboration_groups(tenant_id, name);
-- END SOURCE: core/migrations/034_collaboration_groups.up.sql

-- BEGIN SOURCE: core/migrations/035_managed_exchange.up.sql blob=a4ea4871b59b15ed10596183380a228fc35b8f1a source_bytes=3867 source_sha256=9fe7145a42f001fb8a4796016a45788c6c6332eb5082d64243414b265e7d4d3b rendered_bytes=3867 rendered_sha256=9fe7145a42f001fb8a4796016a45788c6c6332eb5082d64243414b265e7d4d3b transform=none
CREATE TABLE IF NOT EXISTS exchange_field_registry (
    name             TEXT PRIMARY KEY,
    field_type       TEXT NOT NULL,
    semantic_meaning TEXT NOT NULL,
    indexed          BOOLEAN NOT NULL DEFAULT FALSE,
    visibility       TEXT NOT NULL DEFAULT 'default',
    usage_contexts   JSONB NOT NULL DEFAULT '[]'::jsonb,
    created_at       TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE TABLE IF NOT EXISTS exchange_schema_registry (
    id                    TEXT PRIMARY KEY,
    label                 TEXT NOT NULL,
    description           TEXT NOT NULL,
    required_fields       JSONB NOT NULL DEFAULT '[]'::jsonb,
    optional_fields       JSONB NOT NULL DEFAULT '[]'::jsonb,
    required_capabilities JSONB NOT NULL DEFAULT '[]'::jsonb,
    created_at            TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE TABLE IF NOT EXISTS exchange_channels (
    id               UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name             TEXT NOT NULL UNIQUE,
    channel_type     TEXT NOT NULL,
    owner            TEXT NOT NULL,
    participants     JSONB NOT NULL DEFAULT '[]'::jsonb,
    schema_id        TEXT NOT NULL REFERENCES exchange_schema_registry(id),
    retention_policy TEXT NOT NULL,
    visibility       TEXT NOT NULL DEFAULT 'advanced',
    description      TEXT NOT NULL DEFAULT '',
    metadata         JSONB NOT NULL DEFAULT '{}'::jsonb,
    created_at       TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE TABLE IF NOT EXISTS exchange_threads (
    id             UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    channel_id     UUID NOT NULL REFERENCES exchange_channels(id) ON DELETE CASCADE,
    thread_type    TEXT NOT NULL,
    title          TEXT NOT NULL,
    status         TEXT NOT NULL DEFAULT 'active',
    participants   JSONB NOT NULL DEFAULT '[]'::jsonb,
    continuity_key TEXT,
    created_by     TEXT NOT NULL,
    metadata       JSONB NOT NULL DEFAULT '{}'::jsonb,
    created_at     TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at     TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE TABLE IF NOT EXISTS exchange_items (
    id           UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    channel_id   UUID NOT NULL REFERENCES exchange_channels(id) ON DELETE CASCADE,
    schema_id    TEXT NOT NULL REFERENCES exchange_schema_registry(id),
    payload      JSONB NOT NULL,
    created_by   TEXT NOT NULL,
    addressed_to TEXT,
    thread_id    UUID REFERENCES exchange_threads(id) ON DELETE SET NULL,
    visibility   TEXT NOT NULL DEFAULT 'default',
    metadata     JSONB NOT NULL DEFAULT '{}'::jsonb,
    summary      TEXT NOT NULL DEFAULT '',
    created_at   TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_exchange_channels_type
    ON exchange_channels(channel_type, visibility, created_at DESC);

CREATE INDEX IF NOT EXISTS idx_exchange_threads_channel_status
    ON exchange_threads(channel_id, status, updated_at DESC);

CREATE INDEX IF NOT EXISTS idx_exchange_threads_continuity
    ON exchange_threads(continuity_key)
    WHERE continuity_key IS NOT NULL;

CREATE INDEX IF NOT EXISTS idx_exchange_items_channel_created
    ON exchange_items(channel_id, created_at DESC);

CREATE INDEX IF NOT EXISTS idx_exchange_items_thread_created
    ON exchange_items(thread_id, created_at DESC);

CREATE INDEX IF NOT EXISTS idx_exchange_items_schema_created
    ON exchange_items(schema_id, created_at DESC);

CREATE INDEX IF NOT EXISTS idx_exchange_items_payload
    ON exchange_items USING GIN(payload);

CREATE OR REPLACE FUNCTION update_exchange_threads_updated_at()
RETURNS TRIGGER AS $$
BEGIN
    NEW.updated_at = NOW();
    RETURN NEW;
END;
$$ LANGUAGE plpgsql;

DROP TRIGGER IF EXISTS exchange_threads_updated_at ON exchange_threads;
CREATE TRIGGER exchange_threads_updated_at
    BEFORE UPDATE ON exchange_threads
    FOR EACH ROW EXECUTE FUNCTION update_exchange_threads_updated_at();
-- END SOURCE: core/migrations/035_managed_exchange.up.sql

-- BEGIN SOURCE: core/migrations/036_managed_exchange_security.up.sql blob=5187d197ca70eb03d612182d0f3b644437ebf01f source_bytes=1955 source_sha256=381143eee1366bd7c85ed8187173e85b2cc51d8900512ddfb9178bc1b7e82d9c rendered_bytes=1955 rendered_sha256=381143eee1366bd7c85ed8187173e85b2cc51d8900512ddfb9178bc1b7e82d9c transform=none
CREATE TABLE IF NOT EXISTS exchange_capability_registry (
    id                    TEXT PRIMARY KEY,
    label                 TEXT NOT NULL,
    source                TEXT NOT NULL,
    risk_class            TEXT NOT NULL,
    default_allowed_roles JSONB NOT NULL DEFAULT '[]'::jsonb,
    audit_required        BOOLEAN NOT NULL DEFAULT FALSE,
    approval_required     BOOLEAN NOT NULL DEFAULT FALSE,
    description           TEXT NOT NULL DEFAULT '',
    created_at            TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

ALTER TABLE exchange_channels
    ADD COLUMN IF NOT EXISTS reviewers JSONB NOT NULL DEFAULT '[]'::jsonb,
    ADD COLUMN IF NOT EXISTS sensitivity_class TEXT NOT NULL DEFAULT 'role_scoped';

ALTER TABLE exchange_threads
    ADD COLUMN IF NOT EXISTS allowed_reviewers JSONB NOT NULL DEFAULT '[]'::jsonb,
    ADD COLUMN IF NOT EXISTS escalation_rights JSONB NOT NULL DEFAULT '[]'::jsonb;

ALTER TABLE exchange_items
    ADD COLUMN IF NOT EXISTS sensitivity_class TEXT NOT NULL DEFAULT 'role_scoped',
    ADD COLUMN IF NOT EXISTS source_role TEXT NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS source_team TEXT,
    ADD COLUMN IF NOT EXISTS target_role TEXT,
    ADD COLUMN IF NOT EXISTS target_team TEXT,
    ADD COLUMN IF NOT EXISTS allowed_consumers JSONB NOT NULL DEFAULT '[]'::jsonb,
    ADD COLUMN IF NOT EXISTS capability_id TEXT REFERENCES exchange_capability_registry(id),
    ADD COLUMN IF NOT EXISTS trust_class TEXT NOT NULL DEFAULT 'trusted_internal',
    ADD COLUMN IF NOT EXISTS review_required BOOLEAN NOT NULL DEFAULT FALSE;

CREATE INDEX IF NOT EXISTS idx_exchange_channels_sensitivity
    ON exchange_channels(sensitivity_class, visibility, created_at DESC);

CREATE INDEX IF NOT EXISTS idx_exchange_items_security
    ON exchange_items(sensitivity_class, trust_class, review_required, created_at DESC);

CREATE INDEX IF NOT EXISTS idx_exchange_items_capability
    ON exchange_items(capability_id, created_at DESC);
-- END SOURCE: core/migrations/036_managed_exchange_security.up.sql

-- BEGIN SOURCE: core/migrations/037_scoped_memory_visibility.up.sql blob=5cb0c46be5837a109538bb3ca99e04fadb7330a6 source_bytes=610 source_sha256=2ec95f99c9535091b5941f476d05397a2b2d52158c70d79946f625014df7257e rendered_bytes=610 rendered_sha256=2ec95f99c9535091b5941f476d05397a2b2d52158c70d79946f625014df7257e transform=none
-- V8.2: scoped durable memory metadata for team-aware recall

ALTER TABLE agent_memories
    ADD COLUMN IF NOT EXISTS tenant_id TEXT NOT NULL DEFAULT 'default',
    ADD COLUMN IF NOT EXISTS team_id TEXT,
    ADD COLUMN IF NOT EXISTS agent_id TEXT NOT NULL DEFAULT 'admin',
    ADD COLUMN IF NOT EXISTS run_id TEXT,
    ADD COLUMN IF NOT EXISTS visibility TEXT NOT NULL DEFAULT 'team';

CREATE INDEX IF NOT EXISTS idx_agent_memories_scope_created
    ON agent_memories(tenant_id, team_id, agent_id, created_at DESC);

CREATE INDEX IF NOT EXISTS idx_agent_memories_visibility
    ON agent_memories(visibility);
-- END SOURCE: core/migrations/037_scoped_memory_visibility.up.sql

-- BEGIN SOURCE: core/migrations/038_conversation_templates.up.sql blob=e697ba574f74524027d5b388520bfe0299804ae7 source_bytes=1757 source_sha256=054675f2bea26b36553d134bbad2b05b6e854eaf3f8ad4d049009937c8caa4df rendered_bytes=1757 rendered_sha256=054675f2bea26b36553d134bbad2b05b6e854eaf3f8ad4d049009937c8caa4df transform=none
-- 038: Conversation templates for reusable Soma/Council/team asks.
-- These are distinct from CE-1 orchestration templates, connector templates,
-- organization starter bundles, and inception/RAG recipes.

CREATE TABLE IF NOT EXISTS conversation_templates (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    tenant_id TEXT NOT NULL DEFAULT 'default',
    name TEXT NOT NULL,
    description TEXT NOT NULL DEFAULT '',
    scope TEXT NOT NULL,
    created_by TEXT NOT NULL,
    creator_kind TEXT NOT NULL,
    status TEXT NOT NULL DEFAULT 'active',
    template_body TEXT NOT NULL,
    variables JSONB NOT NULL DEFAULT '{}'::jsonb,
    output_contract JSONB NOT NULL DEFAULT '{}'::jsonb,
    recommended_team_shape JSONB NOT NULL DEFAULT '{}'::jsonb,
    model_routing_hint JSONB NOT NULL DEFAULT '{}'::jsonb,
    governance_tags JSONB NOT NULL DEFAULT '[]'::jsonb,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    last_used_at TIMESTAMPTZ NULL,
    CONSTRAINT conversation_templates_scope_check
        CHECK (scope IN ('soma', 'council', 'team', 'temporary_group')),
    CONSTRAINT conversation_templates_creator_kind_check
        CHECK (creator_kind IN ('user', 'soma', 'council', 'system')),
    CONSTRAINT conversation_templates_status_check
        CHECK (status IN ('active', 'draft', 'archived'))
);

CREATE INDEX IF NOT EXISTS idx_conversation_templates_tenant_status
    ON conversation_templates(tenant_id, status, updated_at DESC);
CREATE INDEX IF NOT EXISTS idx_conversation_templates_scope
    ON conversation_templates(scope, updated_at DESC);
CREATE INDEX IF NOT EXISTS idx_conversation_templates_creator
    ON conversation_templates(creator_kind, created_by, updated_at DESC);
-- END SOURCE: core/migrations/038_conversation_templates.up.sql

-- BEGIN SOURCE: core/migrations/039_identity_auth_foundation.up.sql blob=a8905641bf82aac87c7a4b86522f2cd7a8353729 source_bytes=7840 source_sha256=81157b20fc07a621f51f0bc84d6aa3492afd7ee5ed7e02de0626f682ed46fb4d rendered_bytes=7840 rendered_sha256=81157b20fc07a621f51f0bc84d6aa3492afd7ee5ed7e02de0626f682ed46fb4d transform=none
-- Migration 039: V8.2 identity/auth foundation
-- Adds account-scoped identity primitives without wiring server routes.

CREATE TABLE IF NOT EXISTS accounts (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    tenant_id TEXT NOT NULL DEFAULT 'default',
    slug TEXT NOT NULL,
    name TEXT NOT NULL,
    status TEXT NOT NULL DEFAULT 'active',
    settings JSONB NOT NULL DEFAULT '{}'::jsonb,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    CONSTRAINT accounts_status_check
        CHECK (status IN ('active', 'paused', 'deleted'))
);

CREATE UNIQUE INDEX IF NOT EXISTS idx_accounts_slug_unique ON accounts(slug);
CREATE INDEX IF NOT EXISTS idx_accounts_tenant_status ON accounts(tenant_id, status);

INSERT INTO accounts (tenant_id, slug, name, status)
VALUES ('default', 'default', 'Default Account', 'active')
ON CONFLICT (slug) DO NOTHING;

ALTER TABLE users
    ADD COLUMN IF NOT EXISTS account_id UUID,
    ADD COLUMN IF NOT EXISTS email TEXT,
    ADD COLUMN IF NOT EXISTS display_name TEXT NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS status TEXT NOT NULL DEFAULT 'active',
    ADD COLUMN IF NOT EXISTS external_subject TEXT,
    ADD COLUMN IF NOT EXISTS updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    ADD COLUMN IF NOT EXISTS last_login_at TIMESTAMPTZ;

UPDATE users
SET account_id = accounts.id
FROM accounts
WHERE users.account_id IS NULL AND accounts.slug = 'default';

DO $$
BEGIN
    IF NOT EXISTS (
        SELECT 1 FROM pg_constraint WHERE conname = 'users_account_id_fkey'
    ) THEN
        ALTER TABLE users
            ADD CONSTRAINT users_account_id_fkey
            FOREIGN KEY (account_id) REFERENCES accounts(id) ON DELETE SET NULL;
    END IF;
END $$;

CREATE UNIQUE INDEX IF NOT EXISTS idx_users_account_username_unique
    ON users(account_id, LOWER(username))
    WHERE account_id IS NOT NULL;
CREATE UNIQUE INDEX IF NOT EXISTS idx_users_account_email_unique
    ON users(account_id, LOWER(email))
    WHERE account_id IS NOT NULL AND email IS NOT NULL;
CREATE INDEX IF NOT EXISTS idx_users_account_status ON users(account_id, status);

CREATE TABLE IF NOT EXISTS groups (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    account_id UUID NOT NULL REFERENCES accounts(id) ON DELETE CASCADE,
    key TEXT NOT NULL,
    name TEXT NOT NULL,
    description TEXT NOT NULL DEFAULT '',
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE UNIQUE INDEX IF NOT EXISTS idx_groups_account_key_unique
    ON groups(account_id, key);

CREATE TABLE IF NOT EXISTS roles (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    account_id UUID REFERENCES accounts(id) ON DELETE CASCADE,
    key TEXT NOT NULL,
    name TEXT NOT NULL,
    description TEXT NOT NULL DEFAULT '',
    scope TEXT NOT NULL DEFAULT 'account',
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    CONSTRAINT roles_scope_check
        CHECK (scope IN ('system', 'account', 'group'))
);

CREATE UNIQUE INDEX IF NOT EXISTS idx_roles_account_key_unique
    ON roles(COALESCE(account_id, '00000000-0000-0000-0000-000000000000'::uuid), key);

CREATE TABLE IF NOT EXISTS role_permissions (
    role_id UUID NOT NULL REFERENCES roles(id) ON DELETE CASCADE,
    permission_key TEXT NOT NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    PRIMARY KEY (role_id, permission_key)
);

CREATE INDEX IF NOT EXISTS idx_role_permissions_permission ON role_permissions(permission_key);

CREATE TABLE IF NOT EXISTS org_memberships (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    account_id UUID NOT NULL REFERENCES accounts(id) ON DELETE CASCADE,
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    group_id UUID REFERENCES groups(id) ON DELETE CASCADE,
    role_id UUID NOT NULL REFERENCES roles(id) ON DELETE CASCADE,
    status TEXT NOT NULL DEFAULT 'active',
    expires_at TIMESTAMPTZ,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    CONSTRAINT org_memberships_status_check
        CHECK (status IN ('active', 'invited', 'disabled', 'deleted'))
);

CREATE UNIQUE INDEX IF NOT EXISTS idx_org_memberships_unique_assignment
    ON org_memberships(account_id, user_id, role_id,
        COALESCE(group_id, '00000000-0000-0000-0000-000000000000'::uuid));
CREATE INDEX IF NOT EXISTS idx_org_memberships_user ON org_memberships(user_id, status);
CREATE INDEX IF NOT EXISTS idx_org_memberships_group ON org_memberships(group_id, status);

CREATE TABLE IF NOT EXISTS auth_providers (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    account_id UUID NOT NULL REFERENCES accounts(id) ON DELETE CASCADE,
    type TEXT NOT NULL,
    issuer TEXT NOT NULL DEFAULT '',
    client_id TEXT NOT NULL DEFAULT '',
    config JSONB NOT NULL DEFAULT '{}'::jsonb,
    enabled BOOLEAN NOT NULL DEFAULT TRUE,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    CONSTRAINT auth_providers_type_check
        CHECK (type IN ('local', 'oidc', 'oauth2', 'saml', 'api_key'))
);

CREATE UNIQUE INDEX IF NOT EXISTS idx_auth_providers_account_type_issuer
    ON auth_providers(account_id, type, issuer);

CREATE TABLE IF NOT EXISTS sessions (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    account_id UUID NOT NULL REFERENCES accounts(id) ON DELETE CASCADE,
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    provider_id UUID REFERENCES auth_providers(id) ON DELETE SET NULL,
    token_hash TEXT NOT NULL,
    status TEXT NOT NULL DEFAULT 'active',
    metadata JSONB NOT NULL DEFAULT '{}'::jsonb,
    expires_at TIMESTAMPTZ NOT NULL,
    last_seen_at TIMESTAMPTZ,
    revoked_at TIMESTAMPTZ,
    revoked_by UUID REFERENCES users(id) ON DELETE SET NULL,
    revoke_reason TEXT NOT NULL DEFAULT '',
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    CONSTRAINT sessions_status_check
        CHECK (status IN ('active', 'revoked', 'expired'))
);

CREATE UNIQUE INDEX IF NOT EXISTS idx_sessions_token_hash_unique ON sessions(token_hash);
CREATE INDEX IF NOT EXISTS idx_sessions_user_status ON sessions(user_id, status, expires_at);
CREATE INDEX IF NOT EXISTS idx_sessions_account_status ON sessions(account_id, status, expires_at);

CREATE TABLE IF NOT EXISTS user_profiles (
    account_id UUID NOT NULL REFERENCES accounts(id) ON DELETE CASCADE,
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    profile JSONB NOT NULL DEFAULT '{}'::jsonb,
    context JSONB NOT NULL DEFAULT '{}'::jsonb,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    PRIMARY KEY (account_id, user_id)
);

CREATE TABLE IF NOT EXISTS identity_audit_events (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    account_id UUID NOT NULL REFERENCES accounts(id) ON DELETE CASCADE,
    actor_user_id UUID REFERENCES users(id) ON DELETE SET NULL,
    actor_session_id UUID REFERENCES sessions(id) ON DELETE SET NULL,
    event_type TEXT NOT NULL,
    target_kind TEXT NOT NULL DEFAULT '',
    target_id TEXT NOT NULL DEFAULT '',
    source_kind TEXT NOT NULL DEFAULT 'system',
    source_channel TEXT NOT NULL DEFAULT 'identity.store',
    payload JSONB NOT NULL DEFAULT '{}'::jsonb,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_identity_audit_events_account_time
    ON identity_audit_events(account_id, created_at DESC);
CREATE INDEX IF NOT EXISTS idx_identity_audit_events_actor
    ON identity_audit_events(actor_user_id, created_at DESC)
    WHERE actor_user_id IS NOT NULL;
CREATE INDEX IF NOT EXISTS idx_identity_audit_events_type
    ON identity_audit_events(event_type, created_at DESC);
-- END SOURCE: core/migrations/039_identity_auth_foundation.up.sql

-- BEGIN SOURCE: core/migrations/040_capability_manifests.up.sql blob=777136dc370347b8502ba2324e47fce2f86e9d02 source_bytes=1060 source_sha256=c55da1d20f44b0a8c5fb8e3093415a324fc1c854688f76b0ea9b7ecef5419082 rendered_bytes=1060 rendered_sha256=c55da1d20f44b0a8c5fb8e3093415a324fc1c854688f76b0ea9b7ecef5419082 transform=none
CREATE TABLE IF NOT EXISTS capability_manifests (
    id                    TEXT PRIMARY KEY,
    version               TEXT NOT NULL DEFAULT 'capability_manifest.v1',
    display_name          TEXT NOT NULL,
    kind                  TEXT NOT NULL,
    source                TEXT NOT NULL,
    status                TEXT NOT NULL,
    risk_class            TEXT NOT NULL,
    description           TEXT NOT NULL DEFAULT '',
    tool_refs             JSONB NOT NULL DEFAULT '[]'::jsonb,
    default_allowed_roles JSONB NOT NULL DEFAULT '[]'::jsonb,
    audit_required        BOOLEAN NOT NULL DEFAULT FALSE,
    approval_required     BOOLEAN NOT NULL DEFAULT FALSE,
    metadata              JSONB NOT NULL DEFAULT '{}'::jsonb,
    derived_at            TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at            TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_capability_manifests_kind
    ON capability_manifests(kind, status);

CREATE INDEX IF NOT EXISTS idx_capability_manifests_source
    ON capability_manifests(source, status);

-- END SOURCE: core/migrations/040_capability_manifests.up.sql

-- BEGIN SOURCE: core/migrations/041_execution_trust_spine.up.sql blob=31cbb778303dd20bdb690cda963900f03bc05436 source_bytes=3345 source_sha256=00d8d8009644730ec49b8a04efa518446965a06eb7faa77ddf49e9a59213d7e0 rendered_bytes=3345 rendered_sha256=00d8d8009644730ec49b8a04efa518446965a06eb7faa77ddf49e9a59213d7e0 transform=none
-- Migration 041: Durable execution trust spine
--
-- execution_contracts records the governed promise created before mutation.
-- proof_artifacts records the bounded evidence produced by a confirm-action
-- attempt. Both tables are intentionally minimal but include provenance fields
-- needed for future confidence review and recovery lineage.

CREATE TABLE IF NOT EXISTS execution_contracts (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    tenant_id VARCHAR(255) NOT NULL DEFAULT 'default',
    intent_proof_id UUID REFERENCES intent_proofs(id) ON DELETE SET NULL,
    run_id UUID REFERENCES mission_runs(id) ON DELETE SET NULL,
    template_id TEXT NOT NULL,
    resolved_intent TEXT NOT NULL DEFAULT '',
    execution_shape TEXT NOT NULL DEFAULT 'guided_proposal',
    status TEXT NOT NULL DEFAULT 'proposed',
    execution_status TEXT NOT NULL DEFAULT 'proposed',
    validation_source TEXT NOT NULL DEFAULT 'intent_proof',
    evidence_strength TEXT NOT NULL DEFAULT 'intent_only',
    proof_quality TEXT NOT NULL DEFAULT 'proposed',
    latest_proof_artifact_id UUID,
    audit_event_id UUID,
    output_refs JSONB NOT NULL DEFAULT '[]'::jsonb,
    audit_refs JSONB NOT NULL DEFAULT '[]'::jsonb,
    review_lineage JSONB NOT NULL DEFAULT '[]'::jsonb,
    degradation JSONB NOT NULL DEFAULT '{}'::jsonb,
    recovery JSONB NOT NULL DEFAULT '{}'::jsonb,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    confirmed_at TIMESTAMPTZ,
    completed_at TIMESTAMPTZ,
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE UNIQUE INDEX IF NOT EXISTS idx_execution_contracts_intent_proof
    ON execution_contracts(intent_proof_id)
    WHERE intent_proof_id IS NOT NULL;
CREATE INDEX IF NOT EXISTS idx_execution_contracts_run ON execution_contracts(run_id)
    WHERE run_id IS NOT NULL;
CREATE INDEX IF NOT EXISTS idx_execution_contracts_status ON execution_contracts(status, updated_at DESC);

CREATE TABLE IF NOT EXISTS proof_artifacts (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    tenant_id VARCHAR(255) NOT NULL DEFAULT 'default',
    contract_id UUID REFERENCES execution_contracts(id) ON DELETE SET NULL,
    intent_proof_id UUID REFERENCES intent_proofs(id) ON DELETE SET NULL,
    run_id UUID REFERENCES mission_runs(id) ON DELETE SET NULL,
    artifact_kind TEXT NOT NULL DEFAULT 'confirm_action',
    status TEXT NOT NULL,
    proof_class TEXT NOT NULL DEFAULT 'run_and_audit',
    validation_source TEXT NOT NULL DEFAULT 'confirm_action',
    evidence_strength TEXT NOT NULL DEFAULT 'run_audit',
    proof_quality TEXT NOT NULL,
    output_refs JSONB NOT NULL DEFAULT '[]'::jsonb,
    audit_refs JSONB NOT NULL DEFAULT '[]'::jsonb,
    review_lineage JSONB NOT NULL DEFAULT '[]'::jsonb,
    degradation JSONB NOT NULL DEFAULT '{}'::jsonb,
    recovery JSONB NOT NULL DEFAULT '{}'::jsonb,
    payload JSONB NOT NULL DEFAULT '{}'::jsonb,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_proof_artifacts_contract ON proof_artifacts(contract_id, created_at DESC)
    WHERE contract_id IS NOT NULL;
CREATE INDEX IF NOT EXISTS idx_proof_artifacts_intent_proof ON proof_artifacts(intent_proof_id, created_at DESC)
    WHERE intent_proof_id IS NOT NULL;
CREATE INDEX IF NOT EXISTS idx_proof_artifacts_run ON proof_artifacts(run_id, created_at DESC)
    WHERE run_id IS NOT NULL;
-- END SOURCE: core/migrations/041_execution_trust_spine.up.sql

-- BEGIN SOURCE: core/migrations/042_team_work_spine.up.sql blob=8e8312978d85d108417f0294118c9da193fe3567 source_bytes=4233 source_sha256=8a1a7377d7f7d9338f6e1c1e8ad67ec17347223be5a3596807f31a89a04d33c7 rendered_bytes=4233 rendered_sha256=8a1a7377d7f7d9338f6e1c1e8ad67ec17347223be5a3596807f31a89a04d33c7 transform=none
-- Migration 042: Durable team-work spine
-- Persists Soma/Council team work without implying created teams are already executing.

CREATE TABLE IF NOT EXISTS team_work_items (
    id UUID PRIMARY KEY,
    tenant_id TEXT NOT NULL DEFAULT 'default',
    team_id TEXT NOT NULL,
    run_id UUID NULL,
    intent_proof_id UUID NULL,
    contract_id TEXT NULL,
    proof_id TEXT NULL,
    objective TEXT NOT NULL,
    scope JSONB NOT NULL DEFAULT '[]'::jsonb,
    owner TEXT NOT NULL DEFAULT '',
    execution_shape TEXT NOT NULL,
    expected_outputs JSONB NOT NULL DEFAULT '[]'::jsonb,
    expected_proof JSONB NOT NULL DEFAULT '[]'::jsonb,
    capability_requirements JSONB NOT NULL DEFAULT '[]'::jsonb,
    governance_posture TEXT NOT NULL DEFAULT '',
    state TEXT NOT NULL,
    last_event JSONB NULL,
    needs_operator BOOLEAN NOT NULL DEFAULT FALSE,
    degradation_state TEXT NOT NULL DEFAULT '',
    recovery_options JSONB NOT NULL DEFAULT '[]'::jsonb,
    output_refs JSONB NOT NULL DEFAULT '[]'::jsonb,
    proof_refs JSONB NOT NULL DEFAULT '[]'::jsonb,
    audit_refs JSONB NOT NULL DEFAULT '[]'::jsonb,
    version TEXT NOT NULL DEFAULT 'v1',
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    CONSTRAINT chk_team_work_create_team_state
        CHECK (execution_shape <> 'create_team' OR state IN ('new', 'briefed')),
    CONSTRAINT chk_team_work_execution_state
        CHECK (execution_shape = 'create_team' OR state IN ('queued', 'running', 'needs_operator', 'reviewing', 'output_ready', 'degraded', 'paused', 'archived'))
);

CREATE INDEX IF NOT EXISTS idx_team_work_items_team ON team_work_items(team_id, updated_at DESC);
CREATE INDEX IF NOT EXISTS idx_team_work_items_state ON team_work_items(state);
CREATE INDEX IF NOT EXISTS idx_team_work_items_run ON team_work_items(run_id) WHERE run_id IS NOT NULL;
CREATE INDEX IF NOT EXISTS idx_team_work_items_intent_proof ON team_work_items(intent_proof_id) WHERE intent_proof_id IS NOT NULL;

CREATE TABLE IF NOT EXISTS team_interactions (
    id UUID PRIMARY KEY,
    tenant_id TEXT NOT NULL DEFAULT 'default',
    team_id TEXT NOT NULL,
    work_item_id UUID NOT NULL REFERENCES team_work_items(id) ON DELETE CASCADE,
    run_id UUID NULL,
    intent_proof_id UUID NULL,
    contract_id TEXT NULL,
    proof_id TEXT NULL,
    source_kind TEXT NOT NULL,
    source_channel TEXT NOT NULL,
    actor_ref TEXT NOT NULL DEFAULT '',
    verb TEXT NOT NULL,
    summary TEXT NOT NULL,
    payload_kind TEXT NOT NULL,
    payload_ref TEXT NOT NULL DEFAULT '',
    payload JSONB NULL,
    approval_ref TEXT NOT NULL DEFAULT '',
    audit_refs JSONB NOT NULL DEFAULT '[]'::jsonb,
    timestamp TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    version TEXT NOT NULL DEFAULT 'v1'
);

CREATE INDEX IF NOT EXISTS idx_team_interactions_team ON team_interactions(team_id, timestamp DESC);
CREATE INDEX IF NOT EXISTS idx_team_interactions_work ON team_interactions(work_item_id, timestamp DESC);
CREATE INDEX IF NOT EXISTS idx_team_interactions_run ON team_interactions(run_id) WHERE run_id IS NOT NULL;

CREATE TABLE IF NOT EXISTS team_status_events (
    id UUID PRIMARY KEY,
    tenant_id TEXT NOT NULL DEFAULT 'default',
    team_id TEXT NOT NULL,
    work_item_id UUID NOT NULL REFERENCES team_work_items(id) ON DELETE CASCADE,
    run_id UUID NULL,
    intent_proof_id UUID NULL,
    contract_id TEXT NULL,
    proof_id TEXT NULL,
    state TEXT NOT NULL,
    headline TEXT NOT NULL,
    details TEXT NOT NULL DEFAULT '',
    confidence_posture TEXT NOT NULL DEFAULT '',
    blocked_by JSONB NOT NULL DEFAULT '[]'::jsonb,
    next_action TEXT NOT NULL DEFAULT '',
    source_kind TEXT NOT NULL DEFAULT '',
    source_channel TEXT NOT NULL DEFAULT '',
    payload_kind TEXT NOT NULL DEFAULT '',
    audit_refs JSONB NOT NULL DEFAULT '[]'::jsonb,
    timestamp TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    version TEXT NOT NULL DEFAULT 'v1'
);

CREATE INDEX IF NOT EXISTS idx_team_status_events_team ON team_status_events(team_id, timestamp DESC);
CREATE INDEX IF NOT EXISTS idx_team_status_events_work ON team_status_events(work_item_id, timestamp DESC);
CREATE INDEX IF NOT EXISTS idx_team_status_events_state ON team_status_events(state);
-- END SOURCE: core/migrations/042_team_work_spine.up.sql

-- BEGIN SOURCE: core/migrations/043_capability_manifest_state.up.sql blob=eed901f0109a9a7c1fa1d483e53d46a9388ee514 source_bytes=1647 source_sha256=a0002e313b579eae1cb5b8e8d8f3bc2a7e0f33eed5a7c1ca45c9cb371fc244ce rendered_bytes=1647 rendered_sha256=a0002e313b579eae1cb5b8e8d8f3bc2a7e0f33eed5a7c1ca45c9cb371fc244ce transform=none
-- Migration 043: Durable CapabilityManifestState runtime fields
--
-- Extends the first manifest registry table into the V8.2 trust object shape
-- used by Runtime/Capability and operator capability views.

ALTER TABLE capability_manifests
    ADD COLUMN IF NOT EXISTS capability_id TEXT,
    ADD COLUMN IF NOT EXISTS manifest_version TEXT NOT NULL DEFAULT 'capability_manifest.v1',
    ADD COLUMN IF NOT EXISTS purpose TEXT NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS approval_posture TEXT NOT NULL DEFAULT 'not_required',
    ADD COLUMN IF NOT EXISTS allowed_roles JSONB NOT NULL DEFAULT '[]'::jsonb,
    ADD COLUMN IF NOT EXISTS input_schema_ref TEXT NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS output_schema_ref TEXT NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS health TEXT NOT NULL DEFAULT 'unknown',
    ADD COLUMN IF NOT EXISTS last_probe_status TEXT NOT NULL DEFAULT 'unknown',
    ADD COLUMN IF NOT EXISTS last_probe_at TIMESTAMPTZ,
    ADD COLUMN IF NOT EXISTS failure_posture TEXT NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS recovery_posture TEXT NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS audit_policy TEXT NOT NULL DEFAULT 'not_required',
    ADD COLUMN IF NOT EXISTS secret_ref_policy TEXT NOT NULL DEFAULT 'no_raw_secrets',
    ADD COLUMN IF NOT EXISTS owner TEXT NOT NULL DEFAULT 'runtime_capability';

UPDATE capability_manifests
SET capability_id = id
WHERE capability_id IS NULL OR capability_id = '';

ALTER TABLE capability_manifests
    ALTER COLUMN capability_id SET NOT NULL;

CREATE INDEX IF NOT EXISTS idx_capability_manifests_health
    ON capability_manifests(health, last_probe_status);
-- END SOURCE: core/migrations/043_capability_manifest_state.up.sql

-- BEGIN SOURCE: core/migrations/044_collaboration_group_workspace_folders.up.sql blob=3db18f8b840f46392fe4a092c61ef36008a580a2 source_bytes=758 source_sha256=16f76ebdedd4732a06e822d4f47d6c77f600309497dc89e424256268de277b05 rendered_bytes=758 rendered_sha256=16f76ebdedd4732a06e822d4f47d6c77f600309497dc89e424256268de277b05 transform=none
-- Migration 044: Dedicated workspace folders for collaboration groups
-- Permanent user/Soma groups get a stable workspace-relative output root so
-- generated packages, media, and retained artifacts do not collapse into the
-- general output folders.

ALTER TABLE collaboration_groups
    ADD COLUMN IF NOT EXISTS workspace_folder TEXT NOT NULL DEFAULT '';

UPDATE collaboration_groups
SET workspace_folder = 'groups/' || LOWER(
    TRIM(BOTH '-' FROM REGEXP_REPLACE(
        COALESCE(NULLIF(team_ids->>0, ''), NULLIF(name, ''), id::text),
        '[^[:alnum:]]+',
        '-',
        'g'
    ))
)
WHERE workspace_folder = '';

CREATE INDEX IF NOT EXISTS idx_collaboration_groups_workspace_folder
    ON collaboration_groups(tenant_id, workspace_folder);
-- END SOURCE: core/migrations/044_collaboration_group_workspace_folders.up.sql

-- BEGIN SOURCE: core/migrations/045_schedule_trigger_rules.up.sql blob=62d30da39f22d6fb97c44e7e96ee2904460ae5f6 source_bytes=468 source_sha256=8540d5683717b6b78c8e0df0716b522964a661c13dcec31092aa2eaefce2cba6 rendered_bytes=468 rendered_sha256=8540d5683717b6b78c8e0df0716b522964a661c13dcec31092aa2eaefce2cba6 transform=none
ALTER TABLE trigger_rules
    ADD COLUMN IF NOT EXISTS trigger_kind TEXT NOT NULL DEFAULT 'event',
    ADD COLUMN IF NOT EXISTS schedule_interval_seconds INT,
    ADD COLUMN IF NOT EXISTS next_run_at TIMESTAMPTZ,
    ADD COLUMN IF NOT EXISTS proof_expectations TEXT,
    ADD COLUMN IF NOT EXISTS recovery_behavior TEXT;

CREATE INDEX IF NOT EXISTS trigger_rules_schedule_due
    ON trigger_rules(tenant_id, is_active, next_run_at)
    WHERE trigger_kind = 'schedule';
-- END SOURCE: core/migrations/045_schedule_trigger_rules.up.sql

-- BEGIN SOURCE: core/migrations/046_schedule_trigger_handoffs.up.sql blob=55017cc9c44f7eba400a6aceaec5d0cf917a4c81 source_bytes=866 source_sha256=d86d5e89c1fdd0d24e8a8c40dd314eebc2c301608188cbe124b02ecd63e93a06 rendered_bytes=866 rendered_sha256=d86d5e89c1fdd0d24e8a8c40dd314eebc2c301608188cbe124b02ecd63e93a06 transform=none
ALTER TABLE trigger_executions
    ADD COLUMN IF NOT EXISTS handoff_key TEXT,
    ADD COLUMN IF NOT EXISTS intent_proof_id UUID REFERENCES intent_proofs(id) ON DELETE SET NULL,
    ADD COLUMN IF NOT EXISTS contract_id UUID REFERENCES execution_contracts(id) ON DELETE SET NULL,
    ADD COLUMN IF NOT EXISTS proposal_status TEXT NOT NULL DEFAULT 'recorded',
    ADD COLUMN IF NOT EXISTS handoff_payload JSONB NOT NULL DEFAULT '{}'::jsonb;

CREATE UNIQUE INDEX IF NOT EXISTS trigger_executions_handoff_key_unique
    ON trigger_executions(rule_id, handoff_key)
    WHERE handoff_key IS NOT NULL;

CREATE INDEX IF NOT EXISTS trigger_executions_intent_proof
    ON trigger_executions(intent_proof_id)
    WHERE intent_proof_id IS NOT NULL;

CREATE INDEX IF NOT EXISTS trigger_executions_contract
    ON trigger_executions(contract_id)
    WHERE contract_id IS NOT NULL;
-- END SOURCE: core/migrations/046_schedule_trigger_handoffs.up.sql

-- BEGIN SOURCE: core/migrations/047_mcp_tool_set_scopes.up.sql blob=c58863160e06167b997756752576c02ed21546ce source_bytes=1478 source_sha256=9a578a3bae681f3c9cf3f3012dfc0c9c9cba4ded9b92263ed05c5ea96971abba rendered_bytes=1478 rendered_sha256=9a578a3bae681f3c9cf3f3012dfc0c9c9cba4ded9b92263ed05c5ea96971abba transform=none
-- 047: MCP tool-set scope layering.
-- Scope modes:
-- - all: available as a shared tool set across the organization.
-- - group: targeted to a collaboration/team/group lane identified by scope_ref.
-- - host: targeted to a deployment/runtime host identified by scope_ref.

ALTER TABLE mcp_tool_sets
    ADD COLUMN IF NOT EXISTS scope_kind TEXT NOT NULL DEFAULT 'all',
    ADD COLUMN IF NOT EXISTS scope_ref TEXT;

UPDATE mcp_tool_sets
SET scope_kind = 'all'
WHERE scope_kind IS NULL OR scope_kind = '';

ALTER TABLE mcp_tool_sets
    DROP CONSTRAINT IF EXISTS chk_mcp_tool_sets_scope_kind,
    DROP CONSTRAINT IF EXISTS chk_mcp_tool_sets_scope_ref;

ALTER TABLE mcp_tool_sets
    ADD CONSTRAINT chk_mcp_tool_sets_scope_kind
    CHECK (scope_kind IN ('all', 'group', 'host'));

ALTER TABLE mcp_tool_sets
    ADD CONSTRAINT chk_mcp_tool_sets_scope_ref
    CHECK (
        (scope_kind = 'all' AND (scope_ref IS NULL OR scope_ref = ''))
        OR (scope_kind IN ('group', 'host') AND scope_ref IS NOT NULL AND scope_ref <> '')
    );

CREATE INDEX IF NOT EXISTS idx_mcp_tool_sets_scope
    ON mcp_tool_sets(scope_kind, scope_ref);

ALTER TABLE mcp_tool_sets
    DROP CONSTRAINT IF EXISTS mcp_tool_sets_name_key;

DROP INDEX IF EXISTS idx_mcp_tool_sets_name;

CREATE UNIQUE INDEX IF NOT EXISTS idx_mcp_tool_sets_unique_scope
    ON mcp_tool_sets(tenant_id, name, scope_kind, COALESCE(scope_ref, ''));

CREATE INDEX IF NOT EXISTS idx_mcp_tool_sets_name
    ON mcp_tool_sets(name);
-- END SOURCE: core/migrations/047_mcp_tool_set_scopes.up.sql

-- BEGIN SOURCE: core/migrations/048_outcome_projects.up.sql blob=8424edb78f45ac4a07a219f9ec3d165fa6c571b2 source_bytes=2441 source_sha256=0859267204a18052aae168e2dbfeed2002a862381dc44017ddd926d2ff3bd6cd rendered_bytes=2441 rendered_sha256=0859267204a18052aae168e2dbfeed2002a862381dc44017ddd926d2ff3bd6cd transform=none
-- Migration 048: Durable outcome ownership spine
-- Gives Soma-facing deliverables a project owner record without replacing groups.

CREATE TABLE IF NOT EXISTS outcome_projects (
    id UUID PRIMARY KEY,
    tenant_id TEXT NOT NULL DEFAULT 'default',
    outcome_id TEXT NOT NULL,
    title TEXT NOT NULL,
    purpose TEXT NOT NULL DEFAULT '',
    execution_mode TEXT NOT NULL DEFAULT 'project',
    workspace_folder TEXT NOT NULL DEFAULT '',
    status TEXT NOT NULL DEFAULT 'active',
    run_id TEXT NULL,
    intent_proof_id TEXT NULL,
    contract_id TEXT NULL,
    proof_id TEXT NULL,
    work_item_refs JSONB NOT NULL DEFAULT '[]'::jsonb,
    output_refs JSONB NOT NULL DEFAULT '[]'::jsonb,
    proof_refs JSONB NOT NULL DEFAULT '[]'::jsonb,
    recovery_refs JSONB NOT NULL DEFAULT '[]'::jsonb,
    retention_policy TEXT NOT NULL DEFAULT 'retained',
    version TEXT NOT NULL DEFAULT 'v1',
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_outcome_projects_updated ON outcome_projects(tenant_id, updated_at DESC);
CREATE INDEX IF NOT EXISTS idx_outcome_projects_run ON outcome_projects(run_id) WHERE run_id IS NOT NULL;
CREATE INDEX IF NOT EXISTS idx_outcome_projects_intent_proof ON outcome_projects(intent_proof_id) WHERE intent_proof_id IS NOT NULL;
CREATE UNIQUE INDEX IF NOT EXISTS idx_outcome_projects_tenant_outcome
    ON outcome_projects(tenant_id, outcome_id);

CREATE TABLE IF NOT EXISTS team_registry_entries (
    id UUID PRIMARY KEY,
    tenant_id TEXT NOT NULL DEFAULT 'default',
    project_id UUID NOT NULL REFERENCES outcome_projects(id) ON DELETE CASCADE,
    group_id TEXT NOT NULL DEFAULT '',
    role TEXT NOT NULL,
    team_id TEXT NOT NULL DEFAULT '',
    agent_id TEXT NOT NULL DEFAULT '',
    assignment_reason TEXT NOT NULL DEFAULT '',
    temporary BOOLEAN NOT NULL DEFAULT TRUE,
    expires_at TIMESTAMPTZ NULL,
    status TEXT NOT NULL DEFAULT 'active',
    version TEXT NOT NULL DEFAULT 'v1',
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_team_registry_entries_project ON team_registry_entries(project_id, created_at);
CREATE INDEX IF NOT EXISTS idx_team_registry_entries_team ON team_registry_entries(team_id) WHERE team_id <> '';
CREATE INDEX IF NOT EXISTS idx_team_registry_entries_status ON team_registry_entries(status);
-- END SOURCE: core/migrations/048_outcome_projects.up.sql

-- BEGIN SOURCE: core/migrations/049_outcome_project_text_links.up.sql blob=3ab72bcc3a6f24b05cd2a2421d4b7a6dc405efb6 source_bytes=798 source_sha256=81b16e2d479034815f72a50ff52e128c7223642d2bb32db47a9cb5923c434dc7 rendered_bytes=798 rendered_sha256=81b16e2d479034815f72a50ff52e128c7223642d2bb32db47a9cb5923c434dc7 transform=none
-- Migration 049: Preserve user-facing runtime/proof IDs on OutcomeProject.
-- OutcomeProject is a durable ownership surface and must retain text IDs from
-- runtime flows that are not always UUID-backed rows.

ALTER TABLE outcome_projects
    ALTER COLUMN run_id TYPE TEXT USING COALESCE(run_id::text, ''),
    ALTER COLUMN intent_proof_id TYPE TEXT USING COALESCE(intent_proof_id::text, '');

DROP INDEX IF EXISTS idx_outcome_projects_run;
DROP INDEX IF EXISTS idx_outcome_projects_intent_proof;

CREATE INDEX IF NOT EXISTS idx_outcome_projects_run
    ON outcome_projects(run_id)
    WHERE run_id IS NOT NULL AND run_id <> '';

CREATE INDEX IF NOT EXISTS idx_outcome_projects_intent_proof
    ON outcome_projects(intent_proof_id)
    WHERE intent_proof_id IS NOT NULL AND intent_proof_id <> '';
-- END SOURCE: core/migrations/049_outcome_project_text_links.up.sql

-- BEGIN SOURCE: core/migrations/050_search_sources.up.sql blob=cd5c51aa93be879f806f048f3b36a7b236bce735 source_bytes=1679 source_sha256=467cea3732b90bfe064f27d39b4c4854e14ab88133bffd3a1c553258aede7a87 rendered_bytes=1679 rendered_sha256=467cea3732b90bfe064f27d39b4c4854e14ab88133bffd3a1c553258aede7a87 transform=none
-- 050: Governed Search Sources
-- Stores operator-configured search boundaries and secret references for Soma search.

CREATE TABLE IF NOT EXISTS search_sources (
    id                  TEXT PRIMARY KEY,
    name                TEXT NOT NULL,
    provider            TEXT NOT NULL,
    source_type         TEXT NOT NULL,
    endpoint            TEXT,
    scope_kind          TEXT NOT NULL DEFAULT 'all',
    scope_ref           TEXT,
    boundary            TEXT NOT NULL,
    auth_scheme         TEXT NOT NULL DEFAULT 'none',
    secret_ref          TEXT,
    mode                TEXT NOT NULL DEFAULT 'preview',
    sensitivity_class   TEXT NOT NULL DEFAULT 'public',
    trust_class         TEXT NOT NULL DEFAULT 'bounded_external',
    status              TEXT NOT NULL DEFAULT 'available',
    recovery            TEXT,
    tenant_id           TEXT NOT NULL DEFAULT 'default',
    created_at          TIMESTAMPTZ DEFAULT NOW(),
    updated_at          TIMESTAMPTZ DEFAULT NOW()
);

ALTER TABLE search_sources
    DROP CONSTRAINT IF EXISTS chk_search_sources_scope_kind,
    DROP CONSTRAINT IF EXISTS chk_search_sources_scope_ref;

ALTER TABLE search_sources
    ADD CONSTRAINT chk_search_sources_scope_kind
    CHECK (scope_kind IN ('all', 'group', 'host'));

ALTER TABLE search_sources
    ADD CONSTRAINT chk_search_sources_scope_ref
    CHECK ((scope_kind = 'all' AND COALESCE(scope_ref, '') = '') OR (scope_kind IN ('group', 'host') AND COALESCE(scope_ref, '') <> ''));

CREATE INDEX IF NOT EXISTS idx_search_sources_scope
    ON search_sources(tenant_id, scope_kind, scope_ref);

CREATE INDEX IF NOT EXISTS idx_search_sources_provider
    ON search_sources(provider);
-- END SOURCE: core/migrations/050_search_sources.up.sql

-- BEGIN SOURCE: core/migrations/051_registered_input_sources.up.sql blob=63c0ae0ce1efe8393742354062a0f8cbfd70b9b5 source_bytes=4767 source_sha256=5a8f90443c80fc6adabe8b2072dd5e885ce642f74de3381abe3b2fc85d6e66e4 rendered_bytes=4767 rendered_sha256=5a8f90443c80fc6adabe8b2072dd5e885ce642f74de3381abe3b2fc85d6e66e4 transform=none
-- 051: Registered Input Sources
-- Stores governed external/service/device ingress sources and their durable buffers.

CREATE TABLE IF NOT EXISTS input_sources (
    id                      TEXT PRIMARY KEY,
    name                    TEXT NOT NULL,
    source_type             TEXT NOT NULL,
    adapter_kind            TEXT NOT NULL,
    scope_kind              TEXT NOT NULL DEFAULT 'all',
    scope_ref               TEXT,
    target_outcome_id       TEXT,
    target_group_id         TEXT,
    target_host_id          TEXT,
    auth_scheme             TEXT NOT NULL DEFAULT 'none',
    secret_ref              TEXT,
    allowed_ingress_subject TEXT NOT NULL,
    payload_schema_ref      TEXT,
    buffer_mode             TEXT NOT NULL DEFAULT 'append_log',
    buffer_policy           JSONB NOT NULL DEFAULT '{}'::jsonb,
    sensitivity_class       TEXT NOT NULL DEFAULT 'governed',
    trust_class             TEXT NOT NULL DEFAULT 'bounded_external',
    status                  TEXT NOT NULL DEFAULT 'available',
    recovery                TEXT,
    tenant_id               TEXT NOT NULL DEFAULT 'default',
    created_at              TIMESTAMPTZ DEFAULT NOW(),
    updated_at              TIMESTAMPTZ DEFAULT NOW()
);

ALTER TABLE input_sources
    DROP CONSTRAINT IF EXISTS chk_input_sources_scope_kind,
    DROP CONSTRAINT IF EXISTS chk_input_sources_scope_ref,
    DROP CONSTRAINT IF EXISTS chk_input_sources_auth_secret,
    DROP CONSTRAINT IF EXISTS chk_input_sources_buffer_mode,
    DROP CONSTRAINT IF EXISTS chk_input_sources_ingress_subject;

ALTER TABLE input_sources
    ADD CONSTRAINT chk_input_sources_scope_kind
    CHECK (scope_kind IN ('all', 'group', 'host'));

ALTER TABLE input_sources
    ADD CONSTRAINT chk_input_sources_scope_ref
    CHECK ((scope_kind = 'all' AND COALESCE(scope_ref, '') = '') OR (scope_kind IN ('group', 'host') AND COALESCE(scope_ref, '') <> ''));

ALTER TABLE input_sources
    ADD CONSTRAINT chk_input_sources_auth_secret
    CHECK ((auth_scheme = 'none' AND COALESCE(secret_ref, '') = '') OR (auth_scheme <> 'none' AND COALESCE(secret_ref, '') <> ''));

ALTER TABLE input_sources
    ADD CONSTRAINT chk_input_sources_buffer_mode
    CHECK (buffer_mode IN ('append_log', 'latest_state', 'append_with_latest', 'windowed_rollup'));

ALTER TABLE input_sources
    ADD CONSTRAINT chk_input_sources_ingress_subject
    CHECK (allowed_ingress_subject LIKE 'swarm.global.input.%' AND allowed_ingress_subject NOT LIKE '%>%');

CREATE TABLE IF NOT EXISTS input_source_events (
    event_id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    source_id         TEXT NOT NULL REFERENCES input_sources(id) ON DELETE CASCADE,
    channel_key       TEXT NOT NULL DEFAULT 'default',
    payload           JSONB NOT NULL,
    payload_hash      TEXT,
    source_timestamp  TIMESTAMPTZ,
    received_at       TIMESTAMPTZ DEFAULT NOW(),
    run_id            TEXT,
    team_id           TEXT,
    agent_id          TEXT,
    source_kind       TEXT NOT NULL DEFAULT 'web_api',
    source_channel    TEXT NOT NULL,
    payload_kind      TEXT NOT NULL DEFAULT 'event',
    tenant_id         TEXT NOT NULL DEFAULT 'default'
);

CREATE TABLE IF NOT EXISTS input_source_latest (
    source_id         TEXT NOT NULL REFERENCES input_sources(id) ON DELETE CASCADE,
    channel_key       TEXT NOT NULL DEFAULT 'default',
    event_id          UUID REFERENCES input_source_events(event_id) ON DELETE SET NULL,
    payload           JSONB NOT NULL,
    received_at       TIMESTAMPTZ DEFAULT NOW(),
    source_timestamp  TIMESTAMPTZ,
    tenant_id         TEXT NOT NULL DEFAULT 'default',
    PRIMARY KEY (source_id, channel_key)
);

CREATE TABLE IF NOT EXISTS input_source_windows (
    source_id     TEXT NOT NULL REFERENCES input_sources(id) ON DELETE CASCADE,
    channel_key   TEXT NOT NULL DEFAULT 'default',
    window_key    TEXT NOT NULL,
    summary       TEXT NOT NULL,
    payload       JSONB NOT NULL DEFAULT '{}'::jsonb,
    count         INTEGER NOT NULL DEFAULT 0,
    started_at    TIMESTAMPTZ NOT NULL,
    ended_at      TIMESTAMPTZ NOT NULL,
    tenant_id     TEXT NOT NULL DEFAULT 'default',
    PRIMARY KEY (source_id, channel_key, window_key)
);

CREATE INDEX IF NOT EXISTS idx_input_sources_scope
    ON input_sources(tenant_id, scope_kind, scope_ref);

CREATE INDEX IF NOT EXISTS idx_input_sources_status
    ON input_sources(status);

CREATE INDEX IF NOT EXISTS idx_input_source_events_source_received
    ON input_source_events(source_id, channel_key, received_at DESC);

CREATE INDEX IF NOT EXISTS idx_input_source_events_team
    ON input_source_events(team_id, received_at DESC);

CREATE INDEX IF NOT EXISTS idx_input_source_windows_source
    ON input_source_windows(source_id, channel_key, ended_at DESC);
-- END SOURCE: core/migrations/051_registered_input_sources.up.sql

-- BEGIN SOURCE: core/migrations/052_team_work_intent_lifecycle.up.sql blob=6df3fe58434c96c5bdfd0db297cccd44ce3211cb source_bytes=386 source_sha256=559cb89ac9b63b7cf863a12a593183a481cb23040bb730179ce509f97e604586 rendered_bytes=386 rendered_sha256=559cb89ac9b63b7cf863a12a593183a481cb23040bb730179ce509f97e604586 transform=none
-- Migration 052: Preserve approved WorkIntent lifecycle on durable team work.

ALTER TABLE team_work_items
    ADD COLUMN IF NOT EXISTS execution_mode TEXT NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS work_intent JSONB NULL;

ALTER TABLE team_status_events
    ADD COLUMN IF NOT EXISTS execution_mode TEXT NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS work_intent JSONB NULL;
-- END SOURCE: core/migrations/052_team_work_intent_lifecycle.up.sql

-- BEGIN SOURCE: core/migrations/053_execution_dispatch_outbox.up.sql blob=43964123c54e631978c60af83f69f8e9e366cf10 source_bytes=1239 source_sha256=0870d58114e665384b5a90fc99ade2aea40ad173d9905f47de9d87fc32c44687 rendered_bytes=1239 rendered_sha256=0870d58114e665384b5a90fc99ade2aea40ad173d9905f47de9d87fc32c44687 transform=none
-- Migration 053: Durable, replayable dispatch for approved execution plans.

CREATE TABLE IF NOT EXISTS execution_dispatch_outbox (
    id UUID PRIMARY KEY,
    idempotency_key TEXT NOT NULL UNIQUE,
    dispatch_kind TEXT NOT NULL,
    status TEXT NOT NULL DEFAULT 'staged',
    run_id TEXT NOT NULL,
    intent_proof_id UUID NOT NULL REFERENCES intent_proofs(id) ON DELETE CASCADE,
    contract_id UUID NULL,
    team_id TEXT NULL,
    work_item_id TEXT NULL,
    source_kind TEXT NOT NULL,
    source_channel TEXT NOT NULL,
    payload_kind TEXT NOT NULL,
    payload JSONB NOT NULL DEFAULT '{}'::jsonb,
    attempt_count INTEGER NOT NULL DEFAULT 0,
    available_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    lease_until TIMESTAMPTZ NULL,
    last_error TEXT NULL,
    recovery JSONB NOT NULL DEFAULT '{}'::jsonb,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    published_at TIMESTAMPTZ NULL,
    completed_at TIMESTAMPTZ NULL
);

CREATE INDEX IF NOT EXISTS idx_execution_dispatch_outbox_ready
    ON execution_dispatch_outbox (status, available_at, created_at);

CREATE INDEX IF NOT EXISTS idx_execution_dispatch_outbox_run
    ON execution_dispatch_outbox (run_id, created_at);
-- END SOURCE: core/migrations/053_execution_dispatch_outbox.up.sql

-- BEGIN SOURCE: core/migrations/054_operator_sse_events.up.sql blob=bae2cf976cae6c063f46c4c646971e6ac4a1a9f5 source_bytes=269 source_sha256=6a4ff2a2afec9cebb1952c3bad234dffa528855b541eb02f504342303eaabd26 rendered_bytes=269 rendered_sha256=6a4ff2a2afec9cebb1952c3bad234dffa528855b541eb02f504342303eaabd26 transform=none
CREATE TABLE IF NOT EXISTS operator_sse_events (
    sequence BIGSERIAL PRIMARY KEY,
    payload TEXT NOT NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_operator_sse_events_created_at
    ON operator_sse_events (created_at);
-- END SOURCE: core/migrations/054_operator_sse_events.up.sql

-- BEGIN SOURCE: core/migrations/055_team_signal_receipts.up.sql blob=5b9ae5c4beee125c84266d5b3635f6cb27b7998d source_bytes=812 source_sha256=ef783a5bd68cb3709c6a71340f46b952a9aa2c6c6610f315fb7fcfc381d60c21 rendered_bytes=812 rendered_sha256=ef783a5bd68cb3709c6a71340f46b952a9aa2c6c6610f315fb7fcfc381d60c21 transform=none
-- Migration 055: Durable acceptance and replay protection for team bus signals.

CREATE TABLE IF NOT EXISTS team_signal_receipts (
    id UUID PRIMARY KEY,
    tenant_id TEXT NOT NULL DEFAULT 'default',
    team_id TEXT NOT NULL,
    work_item_id UUID NOT NULL REFERENCES team_work_items(id) ON DELETE CASCADE,
    direction TEXT NOT NULL,
    signal_key TEXT NOT NULL,
    source_channel TEXT NOT NULL DEFAULT '',
    accepted_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    CONSTRAINT chk_team_signal_receipt_direction
        CHECK (direction IN ('command', 'result')),
    CONSTRAINT uq_team_signal_receipt
        UNIQUE (tenant_id, team_id, work_item_id, direction, signal_key)
);

CREATE INDEX IF NOT EXISTS idx_team_signal_receipts_work
    ON team_signal_receipts (team_id, work_item_id, accepted_at DESC);
-- END SOURCE: core/migrations/055_team_signal_receipts.up.sql

-- BEGIN SOURCE: core/migrations/056_agent_profile_library.up.sql blob=f271a4963651f12bb0d3cbec656ac985d1d8c679 source_bytes=5078 source_sha256=3f8627edd73bbd17238fa23679d7e2057e93c56b65a216ae6a02d4b1c8e8c6e4 rendered_bytes=5078 rendered_sha256=3f8627edd73bbd17238fa23679d7e2057e93c56b65a216ae6a02d4b1c8e8c6e4 transform=none
-- 056: reusable worker profiles with governed capability and context bindings.

ALTER TABLE agent_catalogue
    ADD COLUMN IF NOT EXISTS profile_key TEXT,
    ADD COLUMN IF NOT EXISTS description TEXT,
    ADD COLUMN IF NOT EXISTS source TEXT NOT NULL DEFAULT 'user',
    ADD COLUMN IF NOT EXISTS locked BOOLEAN NOT NULL DEFAULT FALSE,
    ADD COLUMN IF NOT EXISTS capability_refs JSONB NOT NULL DEFAULT '[]'::jsonb,
    ADD COLUMN IF NOT EXISTS context_bindings JSONB NOT NULL DEFAULT '[]'::jsonb,
    ADD COLUMN IF NOT EXISTS usage_policy JSONB NOT NULL DEFAULT '{}'::jsonb;

CREATE UNIQUE INDEX IF NOT EXISTS idx_agent_catalogue_profile_key
    ON agent_catalogue(profile_key) WHERE profile_key IS NOT NULL;

UPDATE agent_catalogue
SET capability_refs = tools
WHERE capability_refs = '[]'::jsonb AND tools <> '[]'::jsonb;

INSERT INTO agent_catalogue (
    id, profile_key, name, description, role, system_prompt, model, tools,
    capability_refs, context_bindings, usage_policy, inputs, outputs,
    verification_strategy, verification_rubric, source, locked
) VALUES
(
    'a1000000-0000-4000-8000-000000000001', 'default.researcher', 'Research Specialist',
    'Finds and summarizes current or retained information with source-aware evidence.', 'cognitive',
    'Research the assigned question using only approved sources. Separate sourced findings from inference and return concise evidence links or references.', NULL,
    '["web_search","search_memory"]'::jsonb, '["web_search","search_memory"]'::jsonb,
    '[{"kind":"public_web","access":"search"},{"kind":"approved_local_data","access":"search"}]'::jsonb,
    '{"selection":"automatic","scope":"workspace"}'::jsonb, '[]'::jsonb, '["research_summary","source_refs"]'::jsonb,
    'semantic', '["Sources are attributable","Claims distinguish evidence from inference"]'::jsonb, 'built_in', TRUE
),
(
    'a1000000-0000-4000-8000-000000000002', 'default.context-analyst', 'Context Analyst',
    'Finds relevant workspace, mounted, and long-lived organizational context.', 'cognitive',
    'Locate only context relevant to the assigned work. Respect source boundaries, identify the source used, and avoid treating retained context as autonomous authority.', NULL,
    '["search_memory","read_file","read_text_file"]'::jsonb, '["search_memory","read_file","read_text_file"]'::jsonb,
    '[{"kind":"deployment_context","access":"search"},{"kind":"approved_mount","access":"read"}]'::jsonb,
    '{"selection":"automatic","scope":"workspace"}'::jsonb, '[]'::jsonb, '["context_brief","source_refs"]'::jsonb,
    'semantic', '["Context is relevant","Source boundaries are disclosed"]'::jsonb, 'built_in', TRUE
),
(
    'a1000000-0000-4000-8000-000000000003', 'default.builder', 'Output Builder',
    'Creates retained files or packages inside the assigned team workspace.', 'actuation',
    'Build the assigned deliverable inside the team workspace. Keep working material separate from user-facing output and report retained paths plus validation evidence.', NULL,
    '["write_file","read_file","store_artifact"]'::jsonb, '["write_file","read_file","store_artifact"]'::jsonb,
    '[{"kind":"team_workspace","access":"write"},{"kind":"outcome_sources","access":"read"}]'::jsonb,
    '{"selection":"suggested","scope":"team"}'::jsonb, '[]'::jsonb, '["retained_output","proof_refs"]'::jsonb,
    'empirical', '["Output exists in the assigned workspace","Requested behavior is validated"]'::jsonb, 'built_in', TRUE
),
(
    'a1000000-0000-4000-8000-000000000004', 'default.media-creator', 'Media Creator',
    'Creates image or media outputs when an approved media capability is available.', 'actuation',
    'Create media that matches the assigned brief using approved media capabilities. Retain the output, disclose the capability used, and report blockers without inventing media.', NULL,
    '["generate_image","save_cached_image"]'::jsonb, '["generate_image","save_cached_image"]'::jsonb,
    '[{"kind":"outcome_sources","access":"read"},{"kind":"team_workspace","access":"write"}]'::jsonb,
    '{"selection":"suggested","scope":"team"}'::jsonb, '[]'::jsonb, '["media_output","proof_refs"]'::jsonb,
    'empirical', '["Media is retained","Output matches the approved brief"]'::jsonb, 'built_in', TRUE
),
(
    'a1000000-0000-4000-8000-000000000005', 'default.reviewer', 'Quality Reviewer',
    'Reviews retained work against requested acceptance and proof requirements.', 'ledger',
    'Review the assigned output independently. State what passed, what remains uncertain, and what must be repaired before the result can be trusted.', NULL,
    '["read_file","read_text_file","search_memory"]'::jsonb, '["read_file","read_text_file","search_memory"]'::jsonb,
    '[{"kind":"outcome_outputs","access":"read"},{"kind":"run_proof","access":"read"}]'::jsonb,
    '{"selection":"suggested","scope":"outcome"}'::jsonb, '[]'::jsonb, '["review_result","proof_assessment"]'::jsonb,
    'semantic', '["Acceptance criteria are addressed","Uncertainty and failed checks are explicit"]'::jsonb, 'built_in', TRUE
)
ON CONFLICT DO NOTHING;
-- END SOURCE: core/migrations/056_agent_profile_library.up.sql

-- BEGIN SOURCE: core/migrations/057_team_work_recovery_deadline.up.sql blob=5cea71b23494c6da89e08a40d662ee0ccab83623 source_bytes=331 source_sha256=b09a011d100c63cc427c6329cdbd76025a49a21ad0eb8b183385e1505d81307e rendered_bytes=331 rendered_sha256=b09a011d100c63cc427c6329cdbd76025a49a21ad0eb8b183385e1505d81307e transform=none
-- Migration 057: Queryable recovery deadlines for durable asynchronous work.

ALTER TABLE team_work_items
    ADD COLUMN IF NOT EXISTS recovery_deadline_at TIMESTAMPTZ NULL;

CREATE INDEX IF NOT EXISTS idx_team_work_items_recovery_deadline
    ON team_work_items (recovery_deadline_at)
    WHERE recovery_deadline_at IS NOT NULL;
-- END SOURCE: core/migrations/057_team_work_recovery_deadline.up.sql

-- BEGIN SOURCE: core/migrations/058_qa_fixture_ownership.up.sql blob=b453bbba19dc21b8dffe8b07e33403391d722034 source_bytes=1894 source_sha256=750436114bb8932be09f857726a11c0ffddcb75684cb79295027d723c056ab1b rendered_bytes=1894 rendered_sha256=750436114bb8932be09f857726a11c0ffddcb75684cb79295027d723c056ab1b transform=none
-- Migration 058: Explicit ownership for automated acceptance fixtures.
--
-- QA cleanup is intentionally separate from operator retention controls. A
-- fixture scope names both its owner and one execution, while resource rows
-- enumerate the exact records or governed workspace paths that execution may
-- remove. Shared NATS storage is never a fixture resource kind.

CREATE TABLE IF NOT EXISTS qa_fixture_scopes (
    id UUID PRIMARY KEY,
    tenant_id TEXT NOT NULL DEFAULT 'default',
    owner_ref TEXT NOT NULL,
    execution_ref TEXT NOT NULL,
    status TEXT NOT NULL DEFAULT 'open',
    expires_at TIMESTAMPTZ NOT NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    CONSTRAINT chk_qa_fixture_scope_owner CHECK (BTRIM(owner_ref) <> ''),
    CONSTRAINT chk_qa_fixture_scope_execution CHECK (BTRIM(execution_ref) <> ''),
    CONSTRAINT chk_qa_fixture_scope_status CHECK (status IN ('open', 'partial', 'purged')),
    CONSTRAINT uq_qa_fixture_scope_execution UNIQUE (tenant_id, owner_ref, execution_ref)
);

CREATE TABLE IF NOT EXISTS qa_fixture_resources (
    id UUID PRIMARY KEY,
    scope_id UUID NOT NULL REFERENCES qa_fixture_scopes(id) ON DELETE CASCADE,
    resource_kind TEXT NOT NULL,
    resource_ref TEXT NOT NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    CONSTRAINT chk_qa_fixture_resource_ref CHECK (BTRIM(resource_ref) <> ''),
    CONSTRAINT chk_qa_fixture_resource_kind CHECK (
        resource_kind IN ('organization', 'group', 'team', 'run', 'outcome', 'artifact', 'workspace_path')
    ),
    CONSTRAINT uq_qa_fixture_resource UNIQUE (scope_id, resource_kind, resource_ref)
);

CREATE INDEX IF NOT EXISTS idx_qa_fixture_scopes_expiry
    ON qa_fixture_scopes (status, expires_at);
CREATE INDEX IF NOT EXISTS idx_qa_fixture_resources_scope
    ON qa_fixture_resources (scope_id, resource_kind);
-- END SOURCE: core/migrations/058_qa_fixture_ownership.up.sql

-- BEGIN SOURCE: core/migrations/059_qa_fixture_ownership_hardening.up.sql blob=740d7a7b51f7241a8338330d9569f616b296c01b source_bytes=1848 source_sha256=99871bdf70c76eb9ea0990fa3406ade6e1c4f6c3c1afd5d78e659ffc78e753d2 rendered_bytes=1848 rendered_sha256=99871bdf70c76eb9ea0990fa3406ade6e1c4f6c3c1afd5d78e659ffc78e753d2 transform=none
-- Enforce one destructive owner per fixture resource and lock scopes while
-- producers are stopped and durable state is removed.

ALTER TABLE qa_fixture_scopes
    DROP CONSTRAINT IF EXISTS chk_qa_fixture_scope_status;

ALTER TABLE qa_fixture_scopes
    ADD CONSTRAINT chk_qa_fixture_scope_status
    CHECK (status IN ('open', 'purging', 'partial', 'purged'));

-- Release historical terminal leases before adding the global ownership rule.
DELETE FROM qa_fixture_resources AS resource
USING qa_fixture_scopes AS scope
WHERE resource.scope_id = scope.id
  AND scope.status = 'purged';

-- Pre-index development databases may contain ambiguous duplicate claims.
-- Fail closed: mark every affected scope partial and remove every ambiguous
-- claim rather than assigning destructive ownership by insertion order.
WITH duplicate_refs AS (
    SELECT resource_kind, resource_ref
    FROM qa_fixture_resources
    GROUP BY resource_kind, resource_ref
    HAVING COUNT(*) > 1
), affected_scopes AS (
    SELECT DISTINCT resource.scope_id
    FROM qa_fixture_resources AS resource
    JOIN duplicate_refs AS duplicate
      ON duplicate.resource_kind = resource.resource_kind
     AND duplicate.resource_ref = resource.resource_ref
)
UPDATE qa_fixture_scopes AS scope
SET status = 'partial', updated_at = NOW()
FROM affected_scopes AS affected
WHERE scope.id = affected.scope_id;

WITH duplicate_refs AS (
    SELECT resource_kind, resource_ref
    FROM qa_fixture_resources
    GROUP BY resource_kind, resource_ref
    HAVING COUNT(*) > 1
)
DELETE FROM qa_fixture_resources AS resource
USING duplicate_refs AS duplicate
WHERE duplicate.resource_kind = resource.resource_kind
  AND duplicate.resource_ref = resource.resource_ref;

CREATE UNIQUE INDEX IF NOT EXISTS uq_qa_fixture_resource_claim
    ON qa_fixture_resources (resource_kind, resource_ref);
-- END SOURCE: core/migrations/059_qa_fixture_ownership_hardening.up.sql

-- BEGIN SOURCE: core/migrations/060_release_purged_qa_fixture_claims.up.sql blob=952386134a3f316c9606bbc5901fcad9fed1285b source_bytes=222 source_sha256=509d000fe9ab73fe836afe8a631b7a9bbad93c6bc0201221d334fa7e76548646 rendered_bytes=222 rendered_sha256=509d000fe9ab73fe836afe8a631b7a9bbad93c6bc0201221d334fa7e76548646 transform=none
-- QA ownership rows are active cleanup leases, not permanent resource locks.
DELETE FROM qa_fixture_resources AS resource
USING qa_fixture_scopes AS scope
WHERE resource.scope_id = scope.id
  AND scope.status = 'purged';
-- END SOURCE: core/migrations/060_release_purged_qa_fixture_claims.up.sql

-- BEGIN SOURCE: core/migrations/061_config_documents.up.sql blob=316973e849a68fc9e3234e882e5fb6f923845cd4 source_bytes=3786 source_sha256=756ee06a040e72933d45157f9159df79bf9ffcc6c33c4a7d2db4e57b240eae41 rendered_bytes=3786 rendered_sha256=756ee06a040e72933d45157f9159df79bf9ffcc6c33c4a7d2db4e57b240eae41 transform=none
-- 061: File-authoritative configuration revisions and scoped activation history.

CREATE TABLE IF NOT EXISTS config_documents (
    record_id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    tenant_id TEXT NOT NULL DEFAULT 'default',
    document_id TEXT NOT NULL,
    api_version TEXT NOT NULL,
    kind TEXT NOT NULL,
    name TEXT NOT NULL,
    version TEXT NOT NULL,
    owner_id TEXT NOT NULL,
    scope_kind TEXT NOT NULL,
    scope_ref TEXT NOT NULL DEFAULT '',
    enabled BOOLEAN NOT NULL DEFAULT TRUE,
    source_kind TEXT NOT NULL,
    source_ref TEXT NOT NULL DEFAULT '',
    secret_refs JSONB NOT NULL DEFAULT '[]'::jsonb,
    governance JSONB NOT NULL DEFAULT '{}'::jsonb,
    spec JSONB NOT NULL,
    digest TEXT NOT NULL,
    validation_state TEXT NOT NULL DEFAULT 'valid',
    created_by TEXT NOT NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    CONSTRAINT config_documents_scope_kind_check
        CHECK (scope_kind IN ('operator', 'workspace', 'organization', 'built_in')),
    CONSTRAINT config_documents_source_kind_check
        CHECK (source_kind IN ('built_in', 'file', 'soma', 'api')),
    CONSTRAINT config_documents_validation_state_check
        CHECK (validation_state IN ('valid', 'invalid')),
    CONSTRAINT config_documents_revision_unique
        UNIQUE (tenant_id, document_id, version, digest)
);

CREATE INDEX IF NOT EXISTS idx_config_documents_lookup
    ON config_documents(tenant_id, kind, scope_kind, scope_ref, document_id, created_at DESC);

CREATE TABLE IF NOT EXISTS config_document_activations (
    tenant_id TEXT NOT NULL DEFAULT 'default',
    kind TEXT NOT NULL,
    document_id TEXT NOT NULL,
    scope_kind TEXT NOT NULL,
    scope_ref TEXT NOT NULL DEFAULT '',
    config_document_record_id UUID NOT NULL REFERENCES config_documents(record_id),
    activated_by TEXT NOT NULL,
    activated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    PRIMARY KEY (tenant_id, kind, document_id, scope_kind, scope_ref)
);

ALTER TABLE config_document_activations ADD COLUMN IF NOT EXISTS kind TEXT;
UPDATE config_document_activations activation
SET kind = document.kind
FROM config_documents document
WHERE activation.config_document_record_id = document.record_id
  AND activation.kind IS NULL;
ALTER TABLE config_document_activations ALTER COLUMN kind SET NOT NULL;
ALTER TABLE config_document_activations DROP CONSTRAINT IF EXISTS config_document_activations_pkey;
ALTER TABLE config_document_activations
    ADD PRIMARY KEY (tenant_id, kind, document_id, scope_kind, scope_ref);

CREATE TABLE IF NOT EXISTS config_document_activation_history (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    tenant_id TEXT NOT NULL DEFAULT 'default',
    kind TEXT NOT NULL,
    document_id TEXT NOT NULL,
    scope_kind TEXT NOT NULL,
    scope_ref TEXT NOT NULL DEFAULT '',
    from_record_id UUID NULL REFERENCES config_documents(record_id),
    to_record_id UUID NOT NULL REFERENCES config_documents(record_id),
    action TEXT NOT NULL,
    actor_id TEXT NOT NULL,
    audit_event_id UUID NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    CONSTRAINT config_document_activation_action_check
        CHECK (action IN ('activate', 'rollback'))
);

ALTER TABLE config_document_activation_history ADD COLUMN IF NOT EXISTS kind TEXT;
UPDATE config_document_activation_history history
SET kind = document.kind
FROM config_documents document
WHERE history.to_record_id = document.record_id
  AND history.kind IS NULL;
ALTER TABLE config_document_activation_history ALTER COLUMN kind SET NOT NULL;

DROP INDEX IF EXISTS idx_config_document_activation_history;
CREATE INDEX IF NOT EXISTS idx_config_document_activation_history
    ON config_document_activation_history(tenant_id, kind, document_id, scope_kind, scope_ref, created_at DESC);
-- END SOURCE: core/migrations/061_config_documents.up.sql

-- BEGIN SOURCE: core/migrations/062_qa_fixture_config_documents.up.sql blob=a0f5055401cd083ae6d7b352fed78211352d3f75 source_bytes=426 source_sha256=f513b130c9a8e228627c7b34382f8cff0334248b7089d177d7e804aaf57922ab rendered_bytes=426 rendered_sha256=f513b130c9a8e228627c7b34382f8cff0334248b7089d177d7e804aaf57922ab transform=none
-- 062: Allow exact ConfigDocument revisions to participate in QA cleanup.

ALTER TABLE qa_fixture_resources
    DROP CONSTRAINT IF EXISTS chk_qa_fixture_resource_kind;

ALTER TABLE qa_fixture_resources
    ADD CONSTRAINT chk_qa_fixture_resource_kind CHECK (
        resource_kind IN (
            'organization', 'group', 'team', 'run', 'outcome', 'artifact',
            'config_document', 'workspace_path'
        )
    );
-- END SOURCE: core/migrations/062_qa_fixture_config_documents.up.sql

-- BEGIN SOURCE: core/migrations/063_runtime_team_manifests.up.sql blob=c368b14172e59842aa52c53f9d47f3ad6c0cbfc0 source_bytes=1006 source_sha256=039785839ab7fdf1d8454038788ff9f775df213dbd5f13ffd75362adcd8c4784 rendered_bytes=991 rendered_sha256=b31b879796337911122febb269907553fb8994a56a87e257de309fedb1d96b31 transform=strip_outer_transaction

-- Migration 063: Exact runtime-team manifest persistence.
-- Runtime-created teams keep their approved agent/profile snapshots across
-- Core restarts instead of being reconstructed from partial work metadata.

CREATE TABLE IF NOT EXISTS runtime_team_manifests (
    tenant_id TEXT NOT NULL DEFAULT 'default',
    team_id TEXT NOT NULL,
    schema_version TEXT NOT NULL DEFAULT 'v1',
    manifest_digest TEXT NOT NULL,
    manifest JSONB NOT NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    PRIMARY KEY (tenant_id, team_id),
    CONSTRAINT chk_runtime_team_id_not_blank
        CHECK (BTRIM(team_id) <> ''),
    CONSTRAINT chk_runtime_team_manifest_object
        CHECK (jsonb_typeof(manifest) = 'object'),
    CONSTRAINT chk_runtime_team_manifest_identity
        CHECK (manifest->>'id' = team_id)
);

CREATE INDEX IF NOT EXISTS idx_runtime_team_manifests_updated
    ON runtime_team_manifests (tenant_id, updated_at DESC);

-- END SOURCE: core/migrations/063_runtime_team_manifests.up.sql

-- BEGIN SOURCE: core/migrations/064_code_context.up.sql blob=30c70732b13f4261c5eb080db6aebf38389e751a source_bytes=4264 source_sha256=9a4e72d26777983ef704d54cf280033057c10acb0432f273c6c0f9551a5758fe rendered_bytes=4264 rendered_sha256=9a4e72d26777983ef704d54cf280033057c10acb0432f273c6c0f9551a5758fe transform=none
-- 064: Native Mycelis code context source registration and deterministic index storage.

CREATE TABLE IF NOT EXISTS code_context_sources (
    id                  TEXT NOT NULL,
    tenant_id           TEXT NOT NULL DEFAULT 'default',
    name                TEXT NOT NULL,
    source_type         TEXT NOT NULL,
    root_path           TEXT NOT NULL,
    scope_kind          TEXT NOT NULL DEFAULT 'workspace',
    scope_ref           TEXT,
    include_globs       JSONB NOT NULL DEFAULT '[]'::jsonb,
    exclude_globs       JSONB NOT NULL DEFAULT '[]'::jsonb,
    languages           JSONB NOT NULL DEFAULT '[]'::jsonb,
    config_record_id    UUID NULL REFERENCES config_documents(record_id),
    config_digest       TEXT,
    extraction_version  TEXT NOT NULL DEFAULT 'code-context-fixture-v1',
    sensitivity_class   TEXT NOT NULL DEFAULT 'restricted',
    trust_class         TEXT NOT NULL DEFAULT 'trusted_internal',
    status              TEXT NOT NULL DEFAULT 'available',
    recovery            TEXT,
    created_at          TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at          TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    PRIMARY KEY (tenant_id, id),
    CONSTRAINT code_context_sources_type_check
        CHECK (source_type IN ('repository', 'local_code_folder')),
    CONSTRAINT code_context_sources_scope_check
        CHECK (scope_kind IN ('all', 'group', 'host', 'operator', 'workspace', 'organization', 'built_in')),
    CONSTRAINT code_context_sources_scope_ref_check
        CHECK ((scope_kind IN ('all', 'built_in') AND COALESCE(scope_ref, '') = '') OR scope_kind NOT IN ('all', 'built_in')),
    CONSTRAINT code_context_sources_extraction_check
        CHECK (extraction_version = 'code-context-fixture-v1')
);

CREATE INDEX IF NOT EXISTS idx_code_context_sources_scope
    ON code_context_sources(tenant_id, scope_kind, scope_ref);

CREATE TABLE IF NOT EXISTS code_context_snapshots (
    id                  TEXT NOT NULL,
    tenant_id           TEXT NOT NULL DEFAULT 'default',
    source_id           TEXT NOT NULL,
    snapshot_ref        TEXT NOT NULL,
    commit_or_digest    TEXT NOT NULL,
    extraction_version  TEXT NOT NULL,
    created_at          TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    PRIMARY KEY (tenant_id, id),
    FOREIGN KEY (tenant_id, source_id) REFERENCES code_context_sources(tenant_id, id) ON DELETE CASCADE
);

CREATE INDEX IF NOT EXISTS idx_code_context_snapshots_source
    ON code_context_snapshots(tenant_id, source_id, created_at DESC);

CREATE TABLE IF NOT EXISTS code_context_files (
    snapshot_id TEXT NOT NULL,
    tenant_id   TEXT NOT NULL DEFAULT 'default',
    path        TEXT NOT NULL,
    language    TEXT NOT NULL,
    digest      TEXT NOT NULL,
    bytes       BIGINT NOT NULL DEFAULT 0,
    PRIMARY KEY (snapshot_id, tenant_id, path),
    FOREIGN KEY (tenant_id, snapshot_id) REFERENCES code_context_snapshots(tenant_id, id) ON DELETE CASCADE
);

CREATE INDEX IF NOT EXISTS idx_code_context_files_path
    ON code_context_files(tenant_id, path);

CREATE TABLE IF NOT EXISTS code_context_symbols (
    snapshot_id TEXT NOT NULL,
    tenant_id   TEXT NOT NULL DEFAULT 'default',
    symbol_id   TEXT NOT NULL,
    name        TEXT NOT NULL,
    kind        TEXT NOT NULL,
    path        TEXT NOT NULL,
    start_line  INTEGER NOT NULL,
    end_line    INTEGER NOT NULL,
    PRIMARY KEY (snapshot_id, tenant_id, symbol_id),
    FOREIGN KEY (snapshot_id, tenant_id, path) REFERENCES code_context_files(snapshot_id, tenant_id, path) ON DELETE CASCADE
);

CREATE INDEX IF NOT EXISTS idx_code_context_symbols_name
    ON code_context_symbols(tenant_id, name);

CREATE TABLE IF NOT EXISTS code_context_edges (
    id          BIGSERIAL PRIMARY KEY,
    snapshot_id TEXT NOT NULL,
    tenant_id   TEXT NOT NULL DEFAULT 'default',
    from_ref    TEXT NOT NULL,
    to_ref      TEXT NOT NULL,
    kind        TEXT NOT NULL,
    provenance  TEXT NOT NULL,
    FOREIGN KEY (tenant_id, snapshot_id) REFERENCES code_context_snapshots(tenant_id, id) ON DELETE CASCADE,
    CONSTRAINT code_context_edges_provenance_check
        CHECK (provenance IN ('extracted', 'inferred'))
);

CREATE INDEX IF NOT EXISTS idx_code_context_edges_refs
    ON code_context_edges(tenant_id, snapshot_id, from_ref, to_ref);
-- END SOURCE: core/migrations/064_code_context.up.sql

-- P0.10 Slice A: Core-owned durable authority for external worker evidence.
-- This current-schema block is intentionally outside the immutable 773e534c
-- source manifest. Historical migrations are not recreated after convergence.
CREATE TABLE IF NOT EXISTS worker_run_bindings (
    run_id UUID PRIMARY KEY REFERENCES mission_runs(id) ON DELETE CASCADE,
    backend TEXT NOT NULL DEFAULT 'framework_runs',
    protocol TEXT NOT NULL DEFAULT 'runs_api',
    intent_proof_id UUID NOT NULL REFERENCES intent_proofs(id) ON DELETE CASCADE,
    execution_contract_id UUID NOT NULL REFERENCES execution_contracts(id) ON DELETE CASCADE,
    team_id TEXT NULL,
    work_item_id UUID NOT NULL REFERENCES team_work_items(id) ON DELETE CASCADE,
    outcome_id TEXT NULL,
    idempotency_key TEXT NOT NULL UNIQUE,
    graph_revision TEXT NOT NULL,
    source_kind TEXT NOT NULL,
    source_channel TEXT NOT NULL,
    payload_kind TEXT NOT NULL,
    request_digest TEXT NOT NULL,
    service_version BIGINT NULL,
    state TEXT NOT NULL DEFAULT 'create_pending',
    run_status TEXT NULL,
    last_event_sequence BIGINT NOT NULL DEFAULT 0,
    cursor_version BIGINT NOT NULL DEFAULT 0,
    binding_version BIGINT NOT NULL DEFAULT 0,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    bound_at TIMESTAMPTZ NULL,
    CONSTRAINT chk_worker_binding_backend CHECK (backend = 'framework_runs'),
    CONSTRAINT chk_worker_binding_protocol CHECK (protocol = 'runs_api'),
    CONSTRAINT chk_worker_binding_digest CHECK (request_digest ~ '^[0-9a-f]{64}$'),
    CONSTRAINT chk_worker_binding_state CHECK (state IN ('create_pending', 'bound', 'reconcile_required', 'terminal')),
    CONSTRAINT chk_worker_binding_run_status CHECK (run_status IS NULL OR run_status IN ('accepted', 'running', 'approval_needed', 'completed', 'failed', 'cancelled')),
    CONSTRAINT chk_worker_binding_versions CHECK (last_event_sequence >= 0 AND cursor_version >= 0 AND binding_version >= 0 AND (service_version IS NULL OR service_version >= 1)),
    CONSTRAINT chk_worker_binding_payload_kind CHECK (payload_kind = 'command'),
    CONSTRAINT chk_worker_binding_source_kind CHECK (source_kind IN ('workspace_ui', 'web_api', 'automation_trigger', 'scheduler', 'sensor', 'iot', 'internal_tool', 'mcp', 'system')),
    CONSTRAINT chk_worker_binding_canonical_text CHECK (
        idempotency_key = BTRIM(idempotency_key) AND idempotency_key <> '' AND
        graph_revision = BTRIM(graph_revision) AND graph_revision <> '' AND
        source_channel = BTRIM(source_channel) AND source_channel <> ''
    )
);

CREATE INDEX IF NOT EXISTS idx_worker_run_bindings_reconcile
    ON worker_run_bindings(state, updated_at) WHERE state IN ('create_pending', 'reconcile_required');

CREATE TABLE IF NOT EXISTS worker_event_receipts (
    id UUID PRIMARY KEY,
    run_id UUID NOT NULL REFERENCES worker_run_bindings(run_id) ON DELETE CASCADE,
    event_id TEXT NOT NULL,
    sequence BIGINT NOT NULL,
    event_kind TEXT NOT NULL,
    service_version BIGINT NOT NULL,
    payload_digest TEXT NOT NULL,
    normalized_payload JSONB NOT NULL,
    projected_mission_event_id UUID NULL REFERENCES mission_events(id) ON DELETE SET NULL,
    received_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    projected_at TIMESTAMPTZ NULL,
    CONSTRAINT chk_worker_event_sequence CHECK (sequence > 0 AND service_version >= 1),
    CONSTRAINT chk_worker_event_digest CHECK (payload_digest ~ '^[0-9a-f]{64}$'),
    CONSTRAINT uq_worker_event_receipt_id UNIQUE (run_id, event_id),
    CONSTRAINT uq_worker_event_receipt_sequence UNIQUE (run_id, sequence)
);

CREATE INDEX IF NOT EXISTS idx_worker_event_receipts_run
    ON worker_event_receipts(run_id, sequence);

CREATE TABLE IF NOT EXISTS worker_approval_requests (
    id UUID PRIMARY KEY,
    run_id UUID NOT NULL REFERENCES worker_run_bindings(run_id) ON DELETE CASCADE,
    approval_id TEXT NOT NULL,
    request_digest TEXT NOT NULL,
    kind TEXT NOT NULL,
    summary TEXT NOT NULL,
    risk_level TEXT NOT NULL,
    requested_action TEXT NOT NULL,
    state TEXT NOT NULL DEFAULT 'pending',
    decision TEXT NULL,
    decided_by TEXT NULL,
    decision_reason TEXT NULL,
    expires_at TIMESTAMPTZ NULL,
    version BIGINT NOT NULL DEFAULT 0,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    CONSTRAINT chk_worker_approval_digest CHECK (request_digest ~ '^[0-9a-f]{64}$'),
    CONSTRAINT chk_worker_approval_state CHECK (state IN ('pending', 'decided', 'expired', 'withdrawn')),
    CONSTRAINT chk_worker_approval_decision CHECK (decision IS NULL OR decision IN ('approve', 'deny')),
    CONSTRAINT chk_worker_approval_decided CHECK ((state = 'decided' AND decision IS NOT NULL AND decided_by IS NOT NULL) OR (state <> 'decided' AND decision IS NULL)),
    CONSTRAINT chk_worker_approval_version CHECK (version >= 0),
    CONSTRAINT uq_worker_approval_identity UNIQUE (run_id, approval_id)
);

CREATE INDEX IF NOT EXISTS idx_worker_approval_requests_pending
    ON worker_approval_requests(run_id, created_at) WHERE state = 'pending';

CREATE TABLE IF NOT EXISTS worker_control_commands (
    command_id UUID PRIMARY KEY,
    run_id UUID NOT NULL REFERENCES worker_run_bindings(run_id) ON DELETE CASCADE,
    approval_request_id UUID NULL REFERENCES worker_approval_requests(id) ON DELETE SET NULL,
    idempotency_key TEXT NOT NULL UNIQUE,
    kind TEXT NOT NULL,
    expected_service_version BIGINT NOT NULL,
    payload_digest TEXT NOT NULL,
    payload JSONB NOT NULL DEFAULT '{}'::jsonb,
    state TEXT NOT NULL DEFAULT 'staged',
    version BIGINT NOT NULL DEFAULT 0,
    last_error TEXT NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    acknowledged_at TIMESTAMPTZ NULL,
    CONSTRAINT chk_worker_command_kind CHECK (kind IN ('approve', 'deny', 'stop')),
    CONSTRAINT chk_worker_command_state CHECK (state IN ('staged', 'pending', 'acknowledged', 'failed', 'uncertain')),
    CONSTRAINT chk_worker_command_digest CHECK (payload_digest ~ '^[0-9a-f]{64}$'),
    CONSTRAINT chk_worker_command_versions CHECK (expected_service_version >= 1 AND version >= 0),
    CONSTRAINT chk_worker_command_approval CHECK ((kind IN ('approve', 'deny') AND approval_request_id IS NOT NULL) OR (kind = 'stop' AND approval_request_id IS NULL))
);

CREATE INDEX IF NOT EXISTS idx_worker_control_commands_pending
    ON worker_control_commands(run_id, state, created_at)
    WHERE state IN ('staged', 'pending', 'uncertain');

COMMIT;

name: E2E Tests

on:
  push:
    branches: ["main", "develop"]
    paths:
      - "interface/**"
      - "core/**"
      - ".github/workflows/e2e-ci.yaml"
  pull_request:
    branches: ["main", "develop"]
    paths:
      - "interface/**"
      - "core/**"
      - ".github/workflows/e2e-ci.yaml"

jobs:
  e2e:
    name: Playwright E2E
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      # ── Backend ────────────────────────────────────────
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.26"
          cache-dependency-path: core/go.sum

      - name: Build Core Binary
        working-directory: ./core
        run: go build -o ../bin/server ./cmd/server

      # ── Frontend ───────────────────────────────────────
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: interface/package-lock.json

      - name: Install Dependencies
        working-directory: ./interface
        run: npm ci

      - name: Install Playwright Browsers
        working-directory: ./interface
        run: npx playwright install --with-deps chromium firefox webkit

      - name: Build Next.js
        working-directory: ./interface
        run: npm run build

      # ── Run ────────────────────────────────────────────
      - name: Start Core Server
        run: ./bin/server &
        env:
          MYCELIS_API_KEY: "mycelis-dev-key-change-in-prod"
          OLLAMA_HOST: "http://localhost:11434"
          DATABASE_URL: ""

      - name: Wait for Servers
        run: |
          for i in $(seq 1 30); do
            curl -s http://localhost:8081/healthz > /dev/null 2>&1 && break
            sleep 1
          done

      - name: Run Playwright Tests
        working-directory: ./interface
        run: npx playwright test
        env:
          INTERFACE_HOST: 127.0.0.1
          INTERFACE_PORT: "3000"
          MYCELIS_API_KEY: "mycelis-dev-key-change-in-prod"
          PLAYWRIGHT_LIVE_BACKEND: "1"
          PLAYWRIGHT_UI_SERVER_COMMAND: npm run start -- --hostname 127.0.0.1 --port 3000

      - name: Upload Test Results
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-results
          path: interface/test-results/
          retention-days: 7

name: Core CI

on:
  push:
    branches: ["main", "develop"]
    paths:
      - "core/**"
      - "ops/core.py"
      - ".github/workflows/core-ci.yaml"
  pull_request:
    branches: ["main", "develop"]
    paths:
      - "core/**"
      - "ops/core.py"
      - ".github/workflows/core-ci.yaml"

jobs:
  test-and-lint:
    name: Test & Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.23"
          cache-dependency-path: core/go.sum

      - name: Test
        working-directory: ./core
        run: go test -v ./...

      # Optional context: if we had a linter config, we'd run it here.
      # - name: GolangCI-Lint
      #   uses: golangci/golangci-lint-action@v4
      #   with:
      #     version: v1.55.2
      #     working-directory: core

  build:
    name: Build Binary
    needs: test-and-lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.23"
          cache-dependency-path: core/go.sum

      - name: Build
        working-directory: ./core
        run: go build -v -o ../bin/server ./cmd/server

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: mycelis-core-binary
          path: bin/server

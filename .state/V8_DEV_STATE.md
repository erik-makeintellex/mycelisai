# Mycelis V8 - Active Development State
> Navigation: [Project README](../README.md) | [Docs Home](../docs/README.md)

> Updated: 2026-06-28
> Canonical active scoreboard for V8 delivery. Historical V7/V8 migration evidence now lives in Git history; active architecture truth lives in `docs/architecture-library/MYCELIS_CANONICAL_PRD.md`.

## Current Checkpoint

| Field | State |
| --- | --- |
| Current committed checkpoint | `V8.3 Soma service-inventory language active` |
| Working tree at checkpoint | ACTIVE MVP UI/runtime delivery train: architecture doctrine has been consolidated into `docs/architecture-library/MYCELIS_CANONICAL_PRD.md`, which now owns product thesis, Soma UX, governance, runtime architecture, output packages, capability settings, recovery, MVP scope, P0 plan, release gates, and the prime trust rule that runtime correctness must also make the system easier to trust without exposing unnecessary complexity. The Dashboard now has the first light threaded-workspace shell with a compact Quick Actions shelf, `Talk to Soma` workspace, and on-demand Outcome Vault overlay so Soma keeps the main width by default while deliverables/recovery remain one click away; secondary setup/environment chrome no longer stacks below the primary thread. The current architecture update explicitly prevents quick actions or registered workflows from replacing exploratory/iterative Soma conversation: users can explore, shape, then execute with approval when meaningful. P0.1 output package standardization is IN_REVIEW with shared runtime language, package actions, Soma/Groups package cards, mocked success/retry browser proof, Resources re-entry proof, and headless/headed live package proof green; P0.6 run receipt standard is IN_REVIEW with receipt-first Events proof green. |
| Release proof | Current local release-hardening proof remains focused on direct-team ask transactionality, correlation safety, docs contracts, native service health, live Active Work API proof, P0 output-package re-entry proof, P0.4 review-inbox simplification, P0.5 capability-catalog usability, P0.6 run receipt readability, P0.7 recovery queue usability, P0.8 Trusted Outcome Journey proof, and V8.3 threaded-workspace UI binding. Headless/headed P0.1/P0.3 package acceptance, P0.4 Dashboard plus `/teams?view=work` list-detail proof, P0.5 Resources/Capabilities proof, P0.6 docs/runs proof, P0.7 Teams/Resources/Runs/Dashboard compression proof, deterministic P0.8 full journey plus Outcome Ownership/default recovery ownership proof, live P0.8 source-stack smoke with non-empty run-event readback green, focused Soma proposal browser proof, the natural-conversation/threaded-workspace docs plus headed browser proof, compact natural-governance headed `soma-proposal-mode.spec.ts` proof, WorkIntent/started-handoff proof, on-demand Outcome Vault overlay and dashboard density proof, alert target proof, typed alert record proof, max-lines cleanup proof, outcome summary proof, confirm-action run/work correlation proof, explicit OutcomeProject/TeamRegistry API/runtime proof, durable OutcomeProject-backed Vault binding, focused-team fallback regression proof, headed live Trusted Outcome Journey proof, operational alert frame normalization proof, quiet typed rail target proof, API/runtime target-ref propagation proof, live headed API-backed rail click-through proof, Button Studio headed dashboard proof, headed capability-permission Resources proof, Resources workspace listing sanitization proof, Outcome Vault user-language proof, typed Soma thread-event bridge proof, dashboard composition cleanup proof, compact thread-card density proof, recovery-card density proof, and live Resources contributor-tab proof are green. `git diff --check`, focused Soma/proposal/team/recovery unit tests, focused Resources unit tests, focused Button Studio unit tests, focused Outcome Vault/Soma/team tests, Go target-ref tests, typed thread-event tests, compact thread-card unit tests, recovery-card unit tests, docs tests, `uv run inv quality.max-lines --limit 300`, `npx tsc --noEmit`, headed `soma-proposal-mode.spec.ts`, headed `missions.spec.ts`, headed `desktop-mobile-compression.spec.ts`, headed `navigation.spec.ts`, headed `docs-and-runs.spec.ts`, headed `resources-workspace-files.spec.ts`, headed `teams.spec.ts`, headed `focused-team-output-dashboard.spec.ts`, gated headed `dashboard-rail-target-live.spec.ts`, and headed `mcp-toolset-layers.spec.ts` are green. Current gate: headed live route console/page-error sweep across Dashboard, Resources, Groups, Docs, Settings, and gated/admin-enabled Activity, Runs, Memory, and System has no console or page-error findings after the shared Admin tools hydration fix. |
| Focused browser proof | Full Chromium UI pass on 2026-06-03 covered headed live dashboard approval/output scroll behavior, current proposal failure/recovery copy, retained-output reload, Active Work API persistence/status/interactions, live team-generated playable output, mocked first-demo package success/retry, mocked ComfyUI degradation, and mocked retained media output controls. Current focused proof on 2026-06-08 shows the compact `Working in` picker, current-work lane, no pre-chat output dock, bounded Soma conversation, docs/runs navigation, focused-team latest-output digest, workspace file access, and live backend retained-output proof with open-file, reveal-folder, review-rail verification, dashboard digest, and Groups/team readback paths green. Active windowed GUI reachability proof now confirms the Dashboard Soma composer remains physically click/fill reachable at `1366x720`, `1280x640`, and `1280x560` after the current-work lane appears. Active tool-posture guidance proof on 2026-06-09 confirms `show me currently configured tools` returns a bounded read-only answer with no run confirmation and no missing-plan failure. Active headed Review Queue cleanup proof on 2026-06-09 confirms a live-seeded stale failed-plan work item presents `Clear from review` first, leaves active review after clearing, and remains retained as archived history. Active headed P0 generated-app proof on 2026-06-09 confirms a natural Soma/browser ask creates a governed proposal, approval executes a team-owned browser game `project_package`, the retained app opens from the UI, and the code-generated canvas game exposes movement, collision, hazards/enemies, health, key, locked door, win/fail states, and restart. Active headless and headed P0.1/P0.3 package proof on 2026-06-14 confirms Soma ask -> approval -> retained browser package -> opened app -> Resources folder readback -> Groups retained-output review on the live source stack. Active P0.7 GUI proof on 2026-06-17 confirms `/teams?view=work`, Resources/Capabilities, Runs receipts, and Dashboard compression pass headed Chromium proof; active screenshot inspection removed the Resources debug-like inner-scroll badge and fixed the empty Soma composer scrollbar. Active navigation/Groups/Resources proof on 2026-06-17 confirms Groups and Resources are default rail entries, Admin tools gating is preserved for deeper routes, the Groups create path is available as a direct action, group create/broadcast workflows pass headed Chromium proof, and Resources output-file browsing opens headed with one intentional live-backend skip. Active 2026-06-26 headed Dashboard density proof confirms the contained threaded workspace, compact Quick Actions, header-only Outcomes/Vault overlay opener, no setup/environment stack below Soma, themed scrollbars, and reachable composer across desktop, laptop, tablet, and mobile with zero blocking overlap/clipping findings. Active 2026-06-28 proposal-mode proof confirms the new conversational approval pause (`I can start that`, `Approve this?`/`Start this?`, short intended-work list, `Details`, `Approve`/`Start`, `Adjust`) with proposal tool chips hidden from the default surface, work-bus handoff feedback after approval, and proof links preserved. Active 2026-06-28 output-continuation proof confirms a retained output/package reply can preload Soma's composer with title, workspace reference, proof id, and update/alternate/follow-up cue while keeping the input focused and editable. Active 2026-06-28 full user-view sweep confirms Dashboard, Resources, Groups, Docs, Settings, and gated/admin-enabled Activity, Runs, Memory, and System load without console/page errors after the Admin tools hydration stabilization. Active 2026-06-26 clean UI certification keeps the full Chromium suite green after refreshing stale expectations for compact governance, collapsed Outcomes/Vault, Groups output tabs, Resources listing stability, Settings controls, and normalized operational alerts. The legacy `v8-organization-entry.*` setup path still expects the removed dashboard setup affordance and should be retired or rewritten around the current Soma-first entry instead of treated as release proof. |
| Product posture | Governed schedule proposals are durable and reviewable; scheduler ticks do not execute work autonomously; future autonomy is control-first and cannot bypass Soma, ExecutionContract, policy, governed runs, proof, review, or recovery |
| Main release risk | Async team ask acceptance, confirmed delegation run/work correlation, correlated status/result projection, retained output-ref projection, focused output access, live team output review, dark first-paint stability, deterministic returned-output recovery ownership, and live confirm-action run-event reconstruction are operator-visible. Remaining release-candidate risk is keeping the Trusted Outcome Journey green under clean release gates, making outcome/project/team ownership explicit in API/runtime state, finishing only the highest-friction UI simplification needed for usability, retiring stale legacy organization-entry specs, and running live provider media proof only when ComfyUI/gateway flags are intentionally enabled. |
| Environment blocker | ComfyUI is reachable again at `http://127.0.0.1:8188`, and the local media gateway reports healthy at `http://127.0.0.1:8001`; if either endpoint drops, Soma reports a recoverable media dependency blocker rather than implying the approved action silently ran. |

## Delivery Status

| Lane | Status | Where We Are | Next Target | Proof Gate |
| --- | --- | --- | --- | --- |
| Scheduler/Cadence | `COMPLETE` | Schedule due ticks persist idempotent handoff records with `handoff_key`, intent proof, execution contract, proposal status, bounded payload, and explicit terminal handoff approval transitions (`approved`, `rejected`, `cancelled`). | Keep the no-autonomous-execution boundary intact while later slices connect handoff approval into live run creation. | Go scheduler/history/triggers tests, API history proof, autonomous-execution guard review. |
| UI Workflow/Expression | `IN_REVIEW` | Schedule Rules and Approvals are complete. Dashboard team focus now appears only when there is focused or active/output work, so standing teams no longer crowd the clean root Soma surface. Ask Team now appears as queued non-blocking work immediately, Active Work polls active durable rows until terminal state, async asks record running dispatch evidence, sync readable replies retain text output/proof refs, correlated team signals can advance queued rows to output-ready/degraded, retained `outputs[]`/`output_refs[]` project back onto the original row, and focused team contexts now keep chat/work/output/proof scoped together with explicit Root Soma vs Team chat copy in the compact `Working in` picker and chat header. Generated package cards now include `Open in Resources` alongside entrypoint/folder access, while support files include a manifest for re-entry. Retained output and package cards now include `Reply`, which returns the selected output title, workspace reference, and proof id into Soma's composer so users can request updates, alternates, downstream generation, or team handoff without manually copying paths. Dashboard entry now passes the actual organization id into Soma chat scope, clears stale selected-team focus on root re-entry, and rejects unknown `team_id` URL focus instead of passing stale focus into Soma. Chat sends prefer the visible workspace team scope over stale selected-team state, so the displayed team focus and outbound request payload stay aligned. Proposal approval now defaults to a compact `Approve this?` or `Start this?` conversational pause with a short intended-work list, one primary approval action, immediate work-bus handoff feedback, and risk/cost/resources/capability proof behind `Details`. Malformed proposals still cannot update proof-linked lifecycle. The dashboard current-work lane now summarizes active/output-ready work above Soma chat, keeps the latest generated file-like output openable with plain `Open file` / `Open folder` guidance, and opens review detail only when the operator asks for it. Simple Soma chat now shows compact execution receipts for trusted results with the latest-output action and small media preview, while failed/degraded results keep visible boundaries and full output lists/capabilities/proof remain in the overlay Work panel or full pages. Closed review panels no longer mount bulky Work/Output/Trust content; when queued/running/degraded/operator-needed work exists, Soma opens Work first and avoids duplicate pre-chat output docks so the next action stays obvious; finished-output flows remain output-first. Groups and Resources are now first-class default rail entries because users naturally inspect collaboration lanes, outputs, and connected capabilities during normal work; the footer control is relabeled `Admin tools` for Activity/Runs, Memory, System, and deeper admin inspection. Folder reveal feedback now uses direct user language (`Folder opened` / `Open failed`). Dark theme is now the no-preference first-paint default in CSS, the root boot script, and the client store, while explicit `aero-light` and `system` preferences remain supported. Broad managed Chromium UI certification is green with obsolete `v8-organization-entry.*` setup specs skipped until they are rewritten for Soma-first entry. `/dashboard?fresh=1` clears browser-persisted Soma chat/session scopes for fresh interaction testing. | Continue the enterprise workflow cleanup across Teams/Groups, Resources, Activity/Runs, System, Settings, and Docs with browser proof after each visible slice. Soma can asynchronously own review/audit/schedule-handoff work, but not hidden mutations, live provider execution, or deployment proof without explicit approval. | Focused Vitest, TypeScript, max-lines, and headed/browser proof where the workflow is visible. |
| API/Runtime Contract | `IN_REVIEW` | Trigger execution history includes schedule handoff proof fields, and `POST /api/v1/triggers/{id}/history/{executionId}/approval` transitions persisted schedule handoff state only. Focused `/api/v1/chat` requests preserve selected `team_id` for proposal wiring and conversation turns. Direct `/api/v1/teams/{id}/work/ask` now records queued lifecycle state transactionally, records async dispatch evidence with subject/channel and recovery deadline hints before publishing to NATS, flushes published command envelopes to NATS, and keeps multiple pending team-command correlations FIFO for plain responses while preserving explicit response `work_item_id` values. Confirm-action delegated commands now propagate the approved `run_id` in the governed NATS envelope, annotate the delegated ask with reusable `work_item_id`/proof/contract context, and persist the same `work_item_id` into durable team-work refs. Direct ask dispatch/output/degraded lifecycle writes persist status event, run-linked mission event where applicable, work-item update, and interaction in one DB transaction. Bounded sync replies retain a `text_reply` output ref tied to status-event and interaction proof. Durable team `output_refs[].storage_ref` now stores workspace paths/folders instead of viewer URLs for both signal-projected refs and confirm-action deliverables, including viewer URLs that arrive through execution-output `folder` fields. TeamWorkItem, TeamStatusEvent, run-linked mission-event payloads, and OutcomeProject responses now carry additive `target_ref` identity for quiet UI links to run receipts, work items, recovery items, and outcome workspaces. Confirm-action success responses now include `data.team_work_refs[]` for newly persisted create-team, delegated, and deliverable work visibility plus `data.outcome_project` when team-work/output refs produce a durable outcome owner. Core now exposes a curated read-only docs surface through `/api/v1/docs`, `/api/v1/docs/{slug}`, and `/api/v1/docs/search`, and Soma has `list_docs`, `read_doc`, and `search_docs` tools that return citable slug/path metadata without promoting docs into memory or Deployment Context. Service-inventory answers now distinguish built-in Mycelis `web_search` from optional `fetch` MCP explicit-URL retrieval, so an unhealthy Fetch server no longer implies web access itself is unavailable. Live headed P0.8 now verifies non-empty run events and API-backed `OutcomeProject` creation for the confirmed package path. | Prove API-backed target click-through in live headed UI, then extend richer media/package journeys. | Go API tests, docs/API reference review, history reload proof, focused live GUI proof. |
| Deployment/Proof | `ACTIVE` | Baseline source release-preflight is green; local Go reports `go1.26.3`. Native PostgreSQL, NATS, Core API, Frontend, and Ollama are up and healthy on the source stack; `ollama list` confirms local models are available. Current GUI proof ran against the live external UI on port `3000`. Fresh Soma interaction reset cleared volatile run/team context, retained memory vectors, artifacts, collaboration groups, exchange items, workspace outputs, and reports while keeping auth, providers, capability manifests, and deployment config intact. Latest focused live proof creates retained markdown output, exposes it in the current-work lane with Open file/Open folder, opens the review rail, and verifies path/readback/checksum proof. | Keep P0.1/P0.3 source proof green, then run broader release promotion proof after P0.4 review inbox simplification. | `ci.release-preflight`, focused live GUI, `compose.health`, `wsl.validate --lane=release`, K8s proof. |
| Docs/State | `ACTIVE` | Architecture docs have been collapsed into one canonical PRD, with old versioned doctrine removed from the active tree. API reference, Teams/Soma user docs, automation user docs, Resources output docs, and this scoreboard reflect the committed scheduler handoff slice, next-phase kickoff, live GUI proof, live ComfyUI media proof, current max-line convergence proof, focused-team context propagation, workspace-path output-ref semantics, clearer output re-entry behavior, MCP layered configuration, the in-app Help manifest shift to user-first sections, and the new docs/context boundary. The Soma Chat, Resources, Memory, API reference, and canonical PRD now state that Soma can read curated Mycelis docs with citations, while durable source material still belongs in governed Deployment Context and already-retained recall belongs in Memory. | Keep docs/state synchronized in the same slice as behavior, API, runtime, workflow, testing, terminology, or in-app Help structure changes. | docs/workflow tests, docs manifest proof, `git diff --check`, max-lines. |
| MVP UI Runtime Delivery | `ACTIVE` | Competitive/product review confirms the current architecture direction but identifies fragmentation across Dashboard, Teams, Groups, Resources, Runs, and recovery views. The canonical PRD now evaluates P0 through the Trusted Outcome Journey: Ask, Understand, Approve, Execute, Deliver, Trust, Recover, and Revisit, with Outcome as the product object users own later. The threaded Soma workspace, natural pre-execution conversation, compact governance cards, action shelf/Button Studio, Outcome Vault, execution modes, OutcomeProject/TeamRegistry summaries, zero-friction capability settings, typed event binding, and target visual system now live in one PRD instead of split briefs. P0.1 output packages strengthen Deliver/Revisit, P0.4 review inbox strengthens Understand/Approve, P0.5 capability catalog strengthens Trust, P0.6 run receipts strengthen Trust, P0.7 recovery queue strengthens Recover, and Resources/Groups convergence strengthens Revisit. Shared delivery-runtime language and reusable fixtures are supporting groundwork inside P0.1, headless/headed package proof is green, Dashboard work review opens a focused inbox-style panel, `/teams?view=work` now uses a bounded list-detail inbox, Resources now presents Capabilities first with MCP topology behind Inspect, Run Events now begin with a receipt that states what happened, what to trust, and the next step before raw events, P0.7 has shared recovery projection plus headed Teams/Resources/Runs/Dashboard proof, deterministic P0.8 proves the full journey with returning-user Outcome Ownership and default recovery ownership readback, live P0.8 proves source-stack package delivery with proof/contracts and revisit paths, proposal cards now understand WorkIntent/ExecutionMode while showing immediate started-handoff feedback, Outcomes/Vault alerts expose accessible typed target links where stable targets exist, confirmed work now persists explicit API/runtime OutcomeProject/TeamRegistry records, the Vault now prefers durable `/api/v1/outcome-projects` summaries with projected work/output state as fallback, operational alerts now render as conversation-safe recovery frames with still-safe/safe-next guidance and diagnostics hidden behind Details, right-rail alerts now use quiet API-backed target attributes plus deep-link review focus without adding visible IDs, the action shelf now persists Button Studio saved Soma asks through conversation templates with a local fallback while preserving conversational adjustment before execution, Outcomes/Vault now opens as an overlay drawer instead of a default width-stealing rail, Resources now frames MCP toolsets as user-readable capability permission groups for Everyone, Group, and Host boundaries while keeping raw refs/topology inspectable, capability settings now add common choices for Workspace files, Web research, Team coordination, and Local host/media so operators can configure by intent before inspecting refs, typed SSE `thread_event` payloads map into compact Soma thread cards while ordinary bus noise stays out of chat, and the Dashboard now uses a bounded Quick Actions grid plus a quiet current-work strip that shows output title/actions without default file-path/proof clutter. | Next implementation slice is headed GUI proof for common-choice capability settings, then remaining dashboard/mobile simplification. | Focused unit tests per slice, headed GUI proof for visible paths, docs/state sync, release proof from clean committed state. |
| Media Lane | `COMPLETE` | Media gateway unit coverage, direct ComfyUI gateway smoke, mocked retained-media browser proof, live retained-media UI proof, and focused ComfyUI journey proof are green. Windows-host optional Diffusers/vLLM helpers remain unsupported, so local image generation uses Pinokio ComfyUI through the gateway. Runtime media calls now fail closed on provider HTTP/parse/no-image errors so stale cached media is not saved as a fresh output. | Keep media visible in Soma/team output views and carry the same proof standard into richer team-generated media packages. | `tests/test_media_gateway.py`, direct gateway generation smoke, mocked retained-media browser proof, live retained-output proof, focused ComfyUI journey proof, and `go test ./internal/swarm -run "TestHandleGenerateImage"`. |

## Proof Evidence

Latest green proof through next-phase native-services engagement:

1. `go test ./...`
2. `go test ./internal/triggers -run "Test(LogExecution_WithHandoffRefs|GetExecutionByHandoffKey|TransitionScheduleHandoffApproval)"`
3. `go test ./internal/server -run "TestHandle(TriggerHistory|ScheduleHandoffApproval)"`
4. `npm test -- ScheduleRulesTab.test.tsx ScheduleRulesHandoffActions.test.tsx ScheduleHandoffStore.test.ts ApprovalsTab.test.tsx`
5. `npx tsc --noEmit`
6. `npm run build`
7. `uv run pytest tests/test_docs_links.py tests/test_workflow_contracts.py tests/test_runtime_deploy_contract_text.py -q`
8. `uv run inv quality.max-lines --limit 300`
9. `uv run inv interface.e2e --server-mode=start --project=chromium --workers=1 --spec=e2e/specs/schedule-rules.spec.ts`
10. `uv run pytest tests/test_media_gateway.py -q`
11. `uv run inv ci.release-preflight --lane=baseline --no-e2e`
12. `uv run inv lifecycle.up --frontend --build`
13. `uv run inv lifecycle.status`
14. `uv run inv lifecycle.health`
15. `uv run inv native-infra.status`
16. `uv run inv db.clear-runtime-context --yes`
17. `uv run inv interface.e2e --live-backend --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/homepage.spec.ts`
18. `uv run inv interface.e2e --live-backend --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/dashboard-workbench-live-review.spec.ts`
19. `uv run inv interface.e2e --project=chromium --workers=1 --spec=e2e/specs/soma-media-retained-output-live.spec.ts`
20. `uv run inv lifecycle.up --frontend`
21. `curl.exe -fsS http://127.0.0.1:8188/system_stats`
22. `curl.exe -fsS http://127.0.0.1:8001/health`
23. `Invoke-RestMethod -Method Post -Uri http://127.0.0.1:8001/v1/images/generations ...` returned `data[0].b64_json`
24. `$env:PLAYWRIGHT_LIVE_MEDIA_RETAINED_OUTPUT='1'; uv run inv interface.e2e --live-backend --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/soma-media-retained-output-live.spec.ts`
25. `uv run inv interface.e2e --live-backend --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/ui-finalization-browser-package-live.spec.ts`
26. `uv run inv interface.e2e --live-backend --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/soma-governance-live.spec.ts`
27. `uv run inv interface.e2e --project=chromium --workers=1 --spec=e2e/specs/soma-proposal-mode.spec.ts`
28. `uv run inv lifecycle.up --frontend`
29. `uv run pytest tests/test_media_gateway.py -q`
30. `npx tsc --noEmit`
31. `uv run inv quality.max-lines --limit 300`
32. `$env:PLAYWRIGHT_LIVE_MEDIA_RETAINED_OUTPUT='1'; uv run inv interface.e2e --live-backend --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/soma-media-comfyui-journey.spec.ts`
33. `npm test -- SomaOperatingSurface.actions.test.tsx SomaOperatingSurface.test.ts CentralSomaHome.test.tsx MissionControlChat.header.test.tsx OutputWorkbench.test.tsx ActiveWorkLane.test.tsx`
34. `npx tsc --noEmit`
35. `uv run inv quality.max-lines --limit 300`
36. `uv run inv interface.e2e --live-backend --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/desktop-mobile-compression.spec.ts`
37. `uv run inv interface.e2e --live-backend --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/dashboard-workbench-live-review.spec.ts`
38. `go test ./internal/server -run "TestHandleTeamWorkAsk|TestTeamWorkSignal"`
39. `npm test -- TeamsPage.ask.test.tsx ActiveWorkLane.test.tsx SomaOperatingSurface.actions.test.tsx useDurableTeamWork.test.tsx`
40. `npx tsc --noEmit`
41. `uv run pytest tests/test_docs_links.py tests/test_workflow_contracts.py tests/test_runtime_deploy_contract_text.py -q`
42. `uv run inv quality.max-lines --limit 300`
43. `git diff --check`
44. `go test ./internal/swarm -run "TestTeam_ResponseDelivery|TestTeam_StatusDelivery|TestTeam_TriggerLogic_UnwrapsCommandEnvelope|TestNormalizeCommandPayload"`
45. `go test ./internal/server -run "TestTeamWorkSignalProjection|TestHandleTeamWorkAsk"`
46. `uv run pytest tests/test_docs_links.py tests/test_workflow_contracts.py tests/test_runtime_deploy_contract_text.py -q`
47. `uv run inv quality.max-lines --limit 300`
48. `git diff --check`
49. `go test ./internal/server -run "TestTeamWorkSignalProjection|TestHandleTeamWorkAsk"` proves correlated result `outputs[]` and `output_refs[]` now persist openable team output refs on the original Active Work item.
50. `go test ./internal/swarm -run "TestTeam_ResponseDelivery|TestTeam_StatusDelivery|TestTeam_TriggerLogic_UnwrapsCommandEnvelope|TestNormalizeCommandPayload"` remains green for team signal delivery.
51. `npm test -- SomaOperatingSurface.actions.test.tsx ActiveWorkLane.test.tsx` proves focused team retained outputs appear before the work panel is opened while compact Active Work evidence remains green.
52. `npx tsc --noEmit` remains green after adding focused team output access.
53. `npm test -- SomaOperatingSurface.actions.test.tsx ActiveWorkLane.test.tsx OutputWorkbench.test.tsx useDurableTeamWork.test.tsx TeamsPage.ask.test.tsx`
54. `npx tsc --noEmit`
55. `uv run inv interface.e2e --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/desktop-mobile-compression.spec.ts`
56. `uv run inv interface.e2e --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/homepage.spec.ts`
57. `uv run inv interface.e2e --live-backend --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/dashboard-workbench-live-review.spec.ts`
58. `uv run inv interface.e2e --live-backend --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/soma-governance-live.spec.ts`
59. `uv run inv interface.e2e --live-backend --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/team-output-content-live.spec.ts`
60. `$env:PLAYWRIGHT_TEAM_WORK_GUI_LIVE='1'; $env:PLAYWRIGHT_TEAM_WORK_API_TEAM_ID='admin-core'; uv run inv interface.e2e --live-backend --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/active-work-ask-live.spec.ts`
61. `$env:PLAYWRIGHT_LIVE_MEDIA_RETAINED_OUTPUT='1'; uv run inv interface.e2e --live-backend --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/soma-media-retained-output-live.spec.ts`
62. `$env:PLAYWRIGHT_LIVE_MEDIA_RETAINED_OUTPUT='1'; uv run inv interface.e2e --live-backend --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/soma-media-comfyui-journey.spec.ts`
63. `go test ./internal/server` proves confirmation summary, focused team latest-output context, and server regressions remain green.
64. `npm test -- --run __tests__/dashboard/ProposedActionBlock.test.tsx __tests__/dashboard/OutputWorkbench.test.tsx __tests__/store/useCortexStore.confirm-proposal.pending-proof.test.ts __tests__/dashboard/MissionControlChat.executionSummaryMedia.test.tsx` proves immediate approval feedback, pending-proof state, latest-output workbench, and media output actions.
65. `npm run build` proves the production Interface bundle after the approval/output UX correction.
66. `npx playwright test e2e/specs/desktop-mobile-compression.spec.ts e2e/specs/soma-media-artifacts.spec.ts --project=chromium` proves browser-level dashboard compression, focused team output access, and media artifact rendering.
67. `uv run inv db.clear-runtime-context --yes --include-memory-vectors` cleared volatile Soma/team state and retained context vectors for fresh interaction proof.
68. `DELETE FROM exchange_items; DELETE FROM artifacts; DELETE FROM collaboration_groups;` cleared old generated/test content that remained visible outside run tables.
69. `uv run inv clean.reports` removed Playwright/test report residue.
70. `uv run inv interface.restart` rebuilt and restarted the Interface; page health checks passed for `/`, `/dashboard`, `/teams`, `/approvals`, and other core routes.
71. `npm test -- --run __tests__/dashboard/SomaOperatingSurface.actions.test.tsx __tests__/dashboard/MissionControlChat.header.test.tsx __tests__/dashboard/OutputWorkbench.test.tsx __tests__/dashboard/ProposedActionBlock.test.tsx __tests__/store/useCortexStore.confirm-proposal.pending-proof.test.ts` proves root dashboard context compression, `?fresh=1` client chat reset, approval feedback, and output workbench behavior.
72. `npx tsc --noEmit` remains green after the fresh-dashboard cleanup.
73. `npx playwright test e2e/specs/desktop-mobile-compression.spec.ts --project=chromium` proves browser-level dashboard compression remains green.
74. `npx tsc --noEmit` remains green after adding the business-owner fresh dashboard/proposal e2e contract.
75. `$env:PLAYWRIGHT_LIVE_BACKEND='1'; $env:PLAYWRIGHT_SKIP_WEBSERVER='1'; $env:PLAYWRIGHT_PORT='3000'; npx playwright test e2e/specs/dashboard-workbench-live-review.spec.ts --project=chromium --grep "fresh business-owner"` passes after rebuilding Core and restarting the visible Interface: clean fresh dashboard state, proposal approval, retained output, and generated workspace file `E:\random\test\generated\business-owner-flow-1780423995673\owner-note.md`.
76. `go test ./internal/swarm -run "TestHandleGenerateImage"` proves media generation now fails closed on provider HTTP failure and missing image data instead of allowing stale cached media to be saved as fresh output.
77. `go test ./internal/server -run "TestBuildConfirmActionExecutionSummaryNamesTeamMediaDeliverable|TestExecutionOutputsFromArtifactsUsesWorkspaceViewerForSavedMedia"` proves retained media/file output summary wiring remains green after artifact-id handoff tightening.
78. `npm test -- --run __tests__/store/useCortexStore.confirm-proposal.execution.test.ts __tests__/dashboard/OutputWorkbench.test.tsx __tests__/dashboard/MissionControlChat.executionSummary.test.tsx __tests__/dashboard/MissionControlChat.executionSummaryMedia.test.tsx __tests__/dashboard/SomaOperatingSurface.actions.test.tsx __tests__/dashboard/CentralSomaHome.test.tsx` proves clicked proposal token precedence, result-card compression, larger output actions, and simplified cold dashboard tests.
79. `npx tsc --noEmit` remains green after the result-card, context-switcher, output-action, and confirmation-store changes.
80. `$env:PLAYWRIGHT_LIVE_BACKEND='1'; $env:PLAYWRIGHT_SKIP_WEBSERVER='1'; $env:PLAYWRIGHT_PORT='3000'; npx playwright test e2e/specs/dashboard-workbench-live-review.spec.ts --project=chromium --headed` passes in a visible browser: clean business-owner entry, approval/running feedback, retained output, output digest with `Open file` and `Open folder`, generated workspace file `E:\random\test\generated\business-owner-flow-1780453021517\owner-note.md`, review rail showing the current file instead of `Guided proposal`, Output-first review opening, and contained side-rail scrolling with page scroll remaining at `0`.
81. `npm test -- --run __tests__/dashboard/SomaCausalSummary.test.tsx` and `npx tsc --noEmit` pass after extracting `SomaCausalSummaryModel.ts`; `SomaCausalSummary.tsx` is no longer a max-line violation.
82. `npm test -- --run __tests__/dashboard/SomaWorkspaceFrame.test.tsx __tests__/dashboard/OutputWorkbench.test.tsx __tests__/dashboard/ProposedActionBlock.test.tsx` and `npx tsc --noEmit` pass after adding the output digest and extracting `ProposedActionDetails.tsx`/`proposedActionCopy.ts`. `ProposedActionBlock.tsx` is no longer a max-line violation.
83. `go test ./internal/server`, `uv run inv quality.max-lines --limit 300`, and `git diff --check` pass after splitting `cognitive_chat_helpers_test.go`, `cognitive_tool_plan_parse.go`, and `confirm_action_visibility_test.go` into focused helper files. The max-line gate now has no uncapped violations; legacy capped files remain recorded under `ops/quality_legacy_caps.txt`.
84. `go test ./internal/server`, `npm test -- --run __tests__/dashboard/OutputWorkbench.test.tsx __tests__/dashboard/SomaOperatingSurface.actions.test.tsx __tests__/dashboard/useDurableTeamWork.test.tsx`, `npx tsc --noEmit`, and `uv run inv quality.max-lines --limit 300` pass after focused-team context propagation, workspace-path `output_refs` normalization, and focused-output newest-first UI priority.
85. `npx playwright test e2e/specs/desktop-mobile-compression.spec.ts --project=chromium` passes, proving focused team output access stays scannable in browser proof after the output-priority/context changes.
86. `npm test -- --run __tests__/dashboard/OutputWorkbench.test.tsx __tests__/dashboard/SomaOperatingSurface.actions.test.tsx __tests__/dashboard/SomaWorkspaceFrame.test.tsx __tests__/dashboard/CentralSomaHome.test.tsx __tests__/resources/WorkspaceExplorer.test.tsx`, `npx tsc --noEmit`, `uv run inv quality.max-lines --limit 300`, and `uv run inv interface.e2e --project=chromium --workers=1 --spec=e2e/specs/focused-team-output-dashboard.spec.ts` pass after dashboard copy compression, workspace path hints, clearer folder reveal feedback, and focused-team reload browser proof.
87. `npm test -- --run __tests__/dashboard/OutputWorkbench.test.tsx __tests__/dashboard/SomaWorkspaceFrame.test.tsx` and `uv run inv interface.e2e --live-backend --server-mode=start --project=chromium --workers=1 --spec=team-output-content-live.spec.ts` pass after latest-output selection now prefers generated file artifacts over team folders. The live proof creates a new team, confirms the governed action, opens the generated playable HTML file, verifies interaction, reveals the containing folder, verifies focused dashboard re-entry, and confirms Groups retention.
88. `npm test -- SomaOperatingSurface.actions.test.tsx`, `npm test -- --run __tests__/dashboard/SomaWorkspaceFrame.test.tsx __tests__/dashboard/SomaOperatingSurface.actions.test.tsx __tests__/dashboard/OutputWorkbench.test.tsx`, `npx tsc --noEmit`, `go test ./internal/server -run "Test(OutputRefsForTeamWork_NormalizesViewerURLFolderForDeliverable|PersistConfirmedActionTeamWork_DeliverableOutputReadyHasRefs|OutputRefFromMapNormalizesViewerURLToWorkspaceStorageRef|OutputRefFromMapDerivesFilePathFromViewerHrefForMedia)" -count=1`, `uv run inv core.compile`, and `uv run inv interface.e2e --live-backend --server-mode=start --project=chromium --workers=1 --spec=team-output-content-live.spec.ts` pass after team-focus wording cleanup, attention-first work review behavior, and live API readback coverage. The first live readback run caught confirm-action deliverables persisting viewer URLs in `output_refs[].storage_ref`; backend normalization now decodes viewer URLs from execution-output `folder` fields, and the rerun passed.
89. `npm test -- --run __tests__/dashboard/ProposedActionBlock.test.tsx __tests__/store/useCortexStore.confirm-proposal.execution.test.ts __tests__/dashboard/OutputWorkbench.test.tsx __tests__/dashboard/SomaWorkspaceFrame.test.tsx`, `npx tsc --noEmit`, and `uv run inv quality.max-lines --limit 300` pass after the closed workbench panel stopped mounting bulky review content, latest output discovery gained plain file/folder guidance with verification collapsed, and proposal approval gained proof-link guards plus explicit run-started feedback.
90. `uv run inv interface.e2e --live-backend --server-mode=start --project=chromium --workers=1 --spec=e2e/specs/dashboard-workbench-live-review.spec.ts` passes after the compact latest-output digest stopped duplicating exact path text and the browser proof opens collapsed verification details before asserting path/readback/hash proof. The proof covers fresh business-owner entry, approval, retained output, file/folder controls, output review rail, collapsed verification, Trust rail wording, and contained rail scrolling without page scroll.
91. `go test ./internal/server -run 'Test(HandleConfirmAction_CompletesVerifiedExecutionWithPlannedToolCalls|PersistConfirmedActionTeamWork_|ConfirmActionResponseDataIncludesTeamWorkRefs)'`, `go test ./internal/server -run 'Test.*(ConfirmAction|TeamWork)'`, `go test ./internal/server -count=1`, `npm test -- --run __tests__/store/useCortexStore.confirm-proposal.execution.test.ts __tests__/store/useCortexStore.confirm-proposal.team-work-refs.test.ts`, `npx tsc --noEmit`, `uv run pytest tests/test_docs_links.py tests/test_workflow_contracts.py tests/test_runtime_deploy_contract_text.py -q`, `uv run inv quality.max-lines --limit 300`, and `git diff --check` pass after confirm-action success responses gained explicit durable team-work refs and the frontend started surfacing concise Active Work cues from those refs.
92. `uv run inv interface.e2e --live-backend --server-mode=start --project=chromium --workers=1 --spec=e2e/specs/dashboard-workbench-live-review.spec.ts --headed`, `uv run inv interface.e2e --project=chromium --workers=1 --spec=e2e/specs/soma-proposal-mode.spec.ts`, `uv run inv interface.e2e --project=chromium --workers=1 --spec=e2e/specs/workflow-output.reload-review.spec.ts`, `$env:PLAYWRIGHT_TEAM_WORK_API='1'; uv run inv interface.e2e --live-backend --server-mode=start --project=chromium --workers=1 --spec=e2e/specs/active-work-api.spec.ts`, `uv run inv interface.e2e --live-backend --server-mode=start --project=chromium --workers=1 --spec=e2e/specs/team-output-content-live.spec.ts`, `uv run inv interface.e2e --project=chromium --workers=1 --spec=e2e/specs/first-demo-success.spec.ts`, `uv run inv interface.e2e --project=chromium --workers=1 --spec=e2e/specs/ui-finalization-browser-package-retry.spec.ts`, `uv run inv interface.e2e --project=chromium --workers=1 --spec=e2e/specs/soma-media-comfyui-journey.spec.ts`, `uv run inv interface.e2e --project=chromium --workers=1 --spec=e2e/specs/soma-media-retained-output-live.spec.ts`, and `npx tsc --noEmit` pass after browser specs were aligned with current `run` copy, compact recovery language, and output-first file/folder controls. `v8-organization-entry.template-creation.spec.ts` remains a known stale legacy setup proof because it expects the removed dashboard `Set up an AI Organization` affordance instead of the current Soma-first dashboard.
93. `npm test -- --run __tests__/dashboard/MissionControlInputs.test.tsx __tests__/store/useCortexStore.confirm-proposal.failure.test.ts __tests__/soma/ExecutionSummaryCardModel.test.ts` passes, proving multiline Soma composers preserve new lines with Shift+Enter, submit on Enter, cap at `180px`, scroll internally after the cap, and convert local/private ComfyUI 503 failures into actionable recovery copy across both confirm failure and result card models.
94. `npx tsc --noEmit` remains green after changing dashboard/Soma composer refs from single-line inputs to autosizing textareas and tightening confirm-action/result-card media dependency handling.
95. `uv run inv interface.check` passes for `/`, `/wiring`, `/architect`, `/dashboard`, `/catalogue`, `/teams`, `/memory`, `/settings/tools`, and `/approvals` after the UI input/recovery changes.
96. `curl.exe -s http://127.0.0.1:8188/system_stats` returns ComfyUI system/device stats, `curl.exe -s http://127.0.0.1:8001/health` returns `{"status":"ok","backend":"comfyui","upstream":"http://127.0.0.1:8188"}`, and a direct `POST http://127.0.0.1:8001/v1/images/generations` returns `data[0].b64_json`, proving the local media gateway can transmit to ComfyUI and retrieve an image payload.
97. `npm test -- --run __tests__/dashboard/SomaOperatingSurface.actions.test.tsx __tests__/dashboard/MissionControlInputs.test.tsx __tests__/store/useCortexStore.confirm-proposal.failure.test.ts __tests__/soma/ExecutionSummaryCardModel.test.ts` and `npx tsc --noEmit` pass after the focused-team context switcher was changed from card-like controls into a compact selected-context control with work/output/attention badges, and the result-card trust detail now suppresses raw ComfyUI engine errors.
98. `npm test -- --run __tests__/dashboard/SomaOperatingSurface.actions.test.tsx __tests__/soma/SomaTeamContextSwitcher.test.tsx __tests__/dashboard/MissionControlInputs.test.tsx __tests__/store/useCortexStore.confirm-proposal.failure.test.ts __tests__/soma/ExecutionSummaryCardModel.test.ts`, `npm run build`, `npx tsc --noEmit`, and `git diff --check` pass after the current-workflow control was changed from a horizontal tab strip into a bounded picker/listbox. The picker keeps Soma root and team workflow selection in one compact row, shows selected work/output/review badges, and allows all team workflows to live inside a capped vertical list instead of expanding the dashboard.
99. `node workspace/tmp/codex_ui_review_session.mjs` completed a live authenticated browser review on the running local stack and wrote comparison evidence to `workspace/logs/codex-ui-review-2026-06-05T18-00-13-594Z/report.md`. The session created a retained markdown output through Soma, confirmed proposal approval feedback, inspected output/file/folder controls, opened the review rail, tested a focused-team workflow picker, checked Dashboard/Teams/Groups/Resources/Activity/Runs/System/Settings/Docs, captured mobile Dashboard/System screenshots, and scored the current UI as operational but still too internal-console-like for a first-time business owner.
100. Gemini/Antigravity UX benchmarking and Codex browser evidence were reconciled into a P0 execution train: hydration/load stability first, then operator-vs-builder shell split, then a dashboard `Current workflow -> Active task -> Latest output -> Next action` lane, then outcome-first proposal/result cards, then page IA cleanup for Resources, Activity/Runs, Teams/Groups, System, Settings, and Docs. Parallel team planning identified ownership and conflict boundaries for Shell/IA, Dashboard Lane, Outcome Cards, and Hydration/Stability; no implementation edits were made from that planning pass.
101. `npx tsc --noEmit` and `npm test -- MissionControlChat.executionSummary SomaWorkspaceFrame OutputWorkbench` pass after simple-mode Soma result cards were compressed to latest-output actions plus small media preview and visible degraded-run boundaries. Full output lists, proof details, capability rows, and verbose recovery remain behind Work panel/run/resource detail surfaces.
102. `npm test -- MissionControlChat.executionSummary MissionControlChat.executionSummaryMedia SomaWorkspaceFrame` passes after trusted simple-mode execution summaries became concise receipts with direct latest-output actions, media preview, and full proof/detail preserved outside the main chat lane.
103. `npx tsc --noEmit`, `uv run pytest tests/test_docs_links.py tests/test_workflow_contracts.py tests/test_runtime_deploy_contract_text.py -q`, `uv run inv quality.max-lines --limit 300`, and `uv run inv interface.e2e --server-mode=start --project=chromium --workers=1 --spec=e2e/specs/desktop-mobile-compression.spec.ts` pass after the result-receipt slice.
104. `npm test -- MissionControlChat.header SomaOperatingSurface.actions` and `npx tsc --noEmit` pass after Root Soma/team context copy was made explicit in the switcher and team-scoped chat header.
105. `npm test -- MissionControlChat.header SomaOperatingSurface.actions useCortexStore.mission-chat-send` and `npx tsc --noEmit` pass after team-scoped chat sends were made workspace-scope authoritative over stale selected-team state.
106. `uv run inv interface.e2e --server-mode=start --project=chromium --workers=1 --spec=e2e/specs/desktop-mobile-compression.spec.ts` and `uv run inv interface.e2e --server-mode=start --project=chromium --workers=1 --spec=e2e/specs/focused-team-output-dashboard.spec.ts` pass after the Root Soma/focused-team copy and current verification disclosure labels were aligned with browser proof.
107. `npm test -- CentralSomaHome useCortexStore.mission-chat-send MissionControlChat.header SomaOperatingSurface.actions` and `npx tsc --noEmit` pass after dashboard organization id propagation, root re-entry stale-team clearing, unknown `team_id` fallback, and removal of dead Soma surface message derivation.
108. `uv run inv interface.e2e --server-mode=start --project=chromium --workers=1 --spec=e2e/specs/desktop-mobile-compression.spec.ts` and `uv run inv interface.e2e --server-mode=start --project=chromium --workers=1 --spec=e2e/specs/focused-team-output-dashboard.spec.ts` pass after the dashboard scope/re-entry cleanup, confirming the compressed dashboard and focused-team output lane still load from a managed production build.
109. `uv run inv interface.e2e --server-mode=start --project=chromium --workers=1` passes with 87 tests passed and 46 explicitly skipped after current Soma-first browser copy, package output controls, Settings setup guidance, Groups broadcast summary, and legacy organization-entry skip posture were aligned with the production Interface bundle.
110. `go test ./internal/server`, focused Interface unit tests for Soma surface/context/input/failure cards, `npx tsc --noEmit`, `npm run build`, docs contract tests, `uv run inv interface.e2e --server-mode=start --project=chromium --workers=1 --spec=e2e/specs/desktop-mobile-compression.spec.ts`, `uv run inv interface.e2e --server-mode=start --project=chromium --workers=1 --spec=e2e/specs/focused-team-output-dashboard.spec.ts`, and `node workspace/tmp/team_focus_action_retry_session.mjs` pass after removing the stacked focused-team focus/output dock, moving team selection to the compact `Working in` picker, keeping review/output detail in the overlay Work panel, and tightening backend planning so text-only focused-team file asks do not create a team or generate media. Live proof report: `workspace/logs/team-focus-action-retry-2026-06-07T00-46-04-763Z/report.md`.
111. `npm test -- --run __tests__/dashboard/ProposedActionBlock.test.tsx __tests__/dashboard/SomaOperatingSurface.actions.test.tsx __tests__/dashboard/MissionControlInputs.test.tsx __tests__/store/useCortexStore.confirm-proposal.failure.test.ts __tests__/soma/ExecutionSummaryCardModel.test.ts`, `npx tsc --noEmit`, `npm run build`, docs contract tests, `uv run inv interface.e2e --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/soma-proposal-mode.spec.ts`, and live `$env:PLAYWRIGHT_LIVE_BACKEND='1'; $env:PLAYWRIGHT_SKIP_WEBSERVER='1'; $env:PLAYWRIGHT_PORT='3000'; npx playwright test e2e/specs/dashboard-workbench-live-review.spec.ts --project=chromium --grep "fresh business-owner"` pass after proposal cards were simplified into a single run-confirmation question with risk/cost/resources/capability details collapsed behind review details. The live dashboard proof creates a retained markdown output and shows visible `Latest output`, `Open file`, and `Open folder` controls.
112. `go test ./internal/server -count=1`, `uv run inv interface.test`, `npx tsc --noEmit`, `npm run build`, docs contract tests, `uv run inv quality.max-lines --limit 300`, `uv run inv lifecycle.up --frontend --build`, `uv run inv lifecycle.status`, `uv run inv lifecycle.health`, `uv run inv interface.e2e --server-mode=external --project=chromium --workers=1 --spec=interface/e2e/specs/desktop-mobile-compression.spec.ts`, `uv run inv interface.e2e --server-mode=external --project=chromium --workers=1 --spec=interface/e2e/specs/docs-and-runs.spec.ts`, `uv run inv interface.e2e --server-mode=external --project=chromium --workers=1 --spec=interface/e2e/specs/focused-team-output-dashboard.spec.ts`, `uv run inv interface.e2e --server-mode=external --project=chromium --workers=1 --spec=interface/e2e/specs/resources-workspace-files.spec.ts`, and `uv run inv interface.e2e --server-mode=external --project=chromium --workers=1 --live-backend --spec=interface/e2e/specs/team-output-content-live.spec.ts` pass after the delivery-team triage updated tests/docs for compressed Soma-first interaction, fixed the current-workflow picker hook-order risk, restored first-output proof copy, and aligned live generated-output proof with the current `Open local folder` control.
113. `npm test -- --run __tests__/dashboard/OutputWorkbench.test.tsx __tests__/dashboard/OutputWorkbenchPathProjection.test.ts __tests__/dashboard/SomaWorkspaceFrame.test.tsx __tests__/dashboard/SomaOperatingSurface.actions.test.tsx`, the broader focused dashboard/team suite, `npx tsc --noEmit`, `npm run build`, docs contract tests, `uv run inv quality.max-lines --limit 300`, `git diff --check`, `uv run inv lifecycle.restart --frontend --build`, `uv run inv lifecycle.status`, `uv run inv lifecycle.health`, `uv run inv interface.e2e --server-mode=external --project=chromium --workers=1 --spec=interface/e2e/specs/desktop-mobile-compression.spec.ts`, `uv run inv interface.e2e --server-mode=external --project=chromium --workers=1 --spec=interface/e2e/specs/focused-team-output-dashboard.spec.ts`, and `uv run inv interface.e2e --server-mode=external --project=chromium --workers=1 --live-backend --spec=interface/e2e/specs/dashboard-workbench-live-review.spec.ts` pass after adding the compact current-work lane, projecting file-like output IDs into openable workspace paths, and aligning live browser proof with the current single-output review flow.
114. Active windowed GUI probe `ACTIVE_GUI_HEADLESS=0 node workspace/tmp/active_dashboard_reachability_session.mjs` produced `workspace/logs/active-dashboard-reachability-2026-06-08T19-57-17-810Z/report.md` after the layout repair. The report proves the Dashboard Soma textarea is present, enabled, inside the viewport, and receives the click/fill hit target at `1366x720`, `1280x640`, and `1280x560` after the current-work lane appears; `uv run inv interface.e2e --server-mode=external --project=chromium --workers=1 --spec=interface/e2e/specs/desktop-mobile-compression.spec.ts` now includes the matching short-viewport regression and passes with 5 tests.
115. Active windowed GUI probe `ACTIVE_GUI_HEADLESS=0 node workspace/tmp/active_review_panel_session.mjs` produced `workspace/logs/active-review-panel-2026-06-09T02-43-09-017Z/report.md` after compacting the Dashboard review rail. The report proves the panel now shows a plain queued-recovery heading/copy, hides the durable-team-work banner and technical source badge, removes disabled Start/Pause controls, and avoids duplicate Ask/Respond controls; focused unit/type/build proof also passed for the compact Active Work review state.
116. Active windowed GUI probe `ACTIVE_GUI_HEADLESS=0 node workspace/tmp/active_review_panel_session.mjs` produced `workspace/logs/active-review-panel-2026-06-09T03-05-44-176Z/report.md` after routing Dashboard review escalation to `/teams?view=work`. The report proves the side rail stays compact and the full-page handoff opens `Work to Review` with the active work item before team setup/roster context; focused Teams/Active Work tests, TypeScript, production build, docs contract tests, max-line gate, and service restart proof passed.
117. Active windowed GUI probe `ACTIVE_GUI_HEADLESS=0 node workspace/tmp/tool_posture_guidance_session.mjs` produced `workspace/logs/tool-posture-guidance-2026-06-09T03-38-38-496Z/report.md` after the tool-posture repair. The report proves `show me currently configured tools` submits through the live Dashboard, returns a read-only bounded answer listing internal and MCP tools, and does not show run confirmation, `CAN RUN NOW`, `server_error`, or `no approved execution plan was stored for this proposal`.
118. Active headed GUI probe `ACTIVE_GUI_HEADLESS=0 node workspace/tmp/review_queue_cleanup_session.mjs` produced `workspace/logs/review-queue-cleanup-2026-06-09T06-14-37-607Z/report.md` after the Review Queue cleanup. The report proves a live API-seeded stale failed-plan work item shows `Clear from review`, `Open details`, and `Retry recovery` in that order, clearing it removes it from active review (`include_archived=false` returned zero active rows), and retained history keeps the item as `archived`.
119. `npm test -- --run __tests__/store/useCortexStore.data-fetch.test.ts __tests__/pages/SettingsPage.test.tsx`, `npx tsc --noEmit`, `npm run build`, `uv run inv lifecycle.restart --frontend --build`, `uv run inv lifecycle.status`, `uv run inv interface.e2e --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/homepage.spec.ts`, docs contract tests, `uv run inv quality.max-lines --limit 300`, and `git diff --check` pass after the hydration/load-stability slice. The browser proof covers fresh auth redirect, local owner login, authenticated root redirect, stored dark first paint, and no-preference dark first paint with `Advanced: On` and no hydration console noise.
120. `npm test -- --run __tests__/shared/AdvancedModeRoute.test.tsx __tests__/shell/ZoneA_Rail.test.tsx __tests__/pages/PrimaryRoutes.test.tsx`, `npx tsc --noEmit`, `npm run build`, docs contract tests, `uv run inv quality.max-lines --limit 300`, `uv run inv lifecycle.restart --frontend --build`, `uv run inv lifecycle.status`, `uv run inv interface.e2e --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/navigation.spec.ts`, and `git diff --check` pass after the operator shell split slice. Browser proof confirms default nav hides advanced entries and direct `/groups`, `/activity`, and `/runs` show explicit Advanced-mode gates while Advanced is off.
121. `npm test -- --run __tests__/teams/ActiveWorkLane.test.tsx __tests__/teams/ActiveWorkLane.review.test.tsx __tests__/teams/TeamsPage.test.tsx __tests__/teams/TeamsPage.review.test.tsx __tests__/teams/ActiveWorkLane.compact.test.tsx __tests__/resources/WorkspaceExplorer.test.tsx __tests__/pages/ResourcesPage.test.tsx __tests__/dashboard/SomaWorkspaceFrame.test.tsx __tests__/dashboard/OutputWorkbench.test.tsx __tests__/dashboard/SomaOperatingSurface.actions.test.tsx`, `npx tsc --noEmit`, `npm run build`, docs contract tests, `uv run inv quality.max-lines --limit 300`, `git diff --check`, `uv run inv lifecycle.restart --frontend --build`, `uv run inv lifecycle.status`, `uv run inv lifecycle.health`, headed `uv run inv interface.e2e --headed --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/resources-workspace-files.spec.ts`, and headed `uv run inv interface.e2e --headed --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/teams.spec.ts` pass after the Teams Review Work and Resources Output Files IA slice. Proof covers review queue summary counts, concise decision labels, review-first page ordering, Output Files default access, workspace output panes, generated-file preview/write flow, and no dashboard output-regression in the focused component suite.
122. `npm test -- --run __tests__/resources/WorkspaceExplorer.test.tsx __tests__/pages/ResourcesPage.test.tsx __tests__/teams/useTeamWorkActionHandler.test.tsx __tests__/teams/ActiveWorkLane.test.tsx __tests__/teams/ActiveWorkLane.review.test.tsx __tests__/teams/TeamsPage.test.tsx __tests__/teams/TeamsPage.review.test.tsx`, `npx tsc --noEmit`, `npm run build`, docs contract tests, `uv run inv quality.max-lines --limit 300`, `git diff --check`, `uv run inv lifecycle.status`, `uv run inv lifecycle.health`, and `uv run inv interface.e2e --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/resources-workspace-files.spec.ts` pass after the MCP/output-access smoothness slice and the Ask Team immediate-proof projection cleanup. Proof covers filesystem-missing and filesystem-disconnected Output Files recovery with `Open Connected Tools` plus `View storage roots`, retained team ask output/proof/audit refs before the next durable poll, existing review queue behavior, and live Resources generated-file browse/preview/write access.
123. `go test ./internal/server -run "TestBuildPlannedToolCalls_StartsTeamGameDeliverable|TestBuildPlannedToolCalls_GameDeliverableIncludesReadmeWhenRequested|TestDeterministicGovernedMutationResult_BuildsFirstDemoTeamGameProposal|TestBuildProposalDisplayContractExplainsTeamDeliverable|TestExecutionOutputsFromToolResultsAddsReadmeFromProjectPackageContract|TestOutputRefsForTeamWorkProjectPackageUsesFolderStorageRef"`, `go test ./internal/swarm -run "TestHandleWriteFileCreatesProjectPackageSupportFiles"`, `npm test -- --run __tests__/dashboard/OutputWorkbench.test.tsx __tests__/dashboard/SomaWorkspaceFrame.test.tsx`, `npx tsc --noEmit`, `npm run build`, docs contract tests, `uv run inv quality.max-lines --limit 300`, `git diff --check`, `uv run inv lifecycle.health`, and headed live `uv run inv interface.e2e --headed --live-backend --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/soma-browser-game-p0-live.spec.ts` pass after the P0 generated-app benchmark. Proof covers a natural Soma browser ask, governed approval, Soma/team-owned `project_package` output, retained README/PROOF support files, UI open-file routing to the playable app entrypoint, and browser-level mechanics for code-generated graphics, movement, collision, hazards/enemies, health, key, locked door, win/fail states, and restart.
124. `go test ./internal/server -run "TestHandleTeamWorkAsk|TestTeamWorkAskFollowupContext"`, `go test ./internal/swarm ./internal/server`, `npm test -- OutputWorkbench.test.tsx WorkspaceExplorer.test.tsx ResourcesPage.test.tsx`, `npx tsc --noEmit`, `npm run build`, docs contract tests, `uv run inv quality.max-lines --limit 300`, `git diff --check`, `uv run inv lifecycle.up --frontend --build`, `uv run inv lifecycle.status`, `uv run inv lifecycle.health`, headed `uv run inv interface.e2e --headed --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/resources-workspace-files.spec.ts`, and live `$env:PLAYWRIGHT_ACTIVE_WORK_API_LIVE='1'; uv run inv interface.e2e --live-backend --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/active-work-api.spec.ts` pass after the direct-team/package usability slice. Proof covers async Ask Team dispatch events/interactions with subject/channel and recovery hints, sync readable team replies with retained `text_reply` output/proof refs, generated `project-package.json` manifests, improved package README/PROOF support text, package `Open in Resources` deep-linking, and live direct `/work/ask` API continuity.
125. Runtime review of the direct-team/package usability slice found that overlapping async asks could overwrite the single pending team-command correlation, allowing a plain response to attach to the wrong work item. The follow-up fix changes team runtime correlation to FIFO for plain responses, preserves explicit response `work_item_id` without consuming pending correlations, and flushes async NATS publishes before reporting `dispatch_state=published`. Proof: `go test ./internal/swarm -run "TestTeam_ResponseDelivery|TestTeam_StatusDelivery" -count=1`, `go test ./internal/server -run "TestHandleTeamWorkAsk|TestTeamWorkAskFollowupContext" -count=1`, docs contract tests, `uv run inv quality.max-lines --limit 300`, `uv run inv lifecycle.up --frontend --build`, `uv run inv lifecycle.status`, `uv run inv lifecycle.health`, and live `$env:PLAYWRIGHT_ACTIVE_WORK_API_LIVE='1'; uv run inv interface.e2e --live-backend --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/active-work-api.spec.ts` pass.
126. Release-gate review of direct team ask persistence found partial lifecycle writes could leave proof/output references without the matching interaction record if a later insert failed. The follow-up fix persists direct ask dispatch, output-ready, and degraded lifecycle records in one DB transaction and leaves in-memory `LastEvent` unset when the transaction rolls back. Proof: `go test ./internal/server -run "TestHandleTeamWorkAsk|TestRecordTeamWorkAskOutput|TestTeamWorkAskFollowupContext" -count=1`, `go test ./internal/server ./internal/swarm`, docs contract tests, `uv run inv quality.max-lines --limit 300`, `git diff --check`, service restart/status/health, and live Active Work API proof.
127. Runtime trust review found that direct Ask Team queued lifecycle creation still used separate DB writes, and async publish could happen before durable dispatch tracking existed. The follow-up fix wraps initial team-work lifecycle creation in one DB transaction, keeps `LastEvent` post-commit, records async dispatch tracking before publish, and proves failed dispatch persistence does not publish a command. Release proof also fixed the non-idempotent `007_team_fabric` migration and hardened web auth signing so live Playwright setup can log in after restart. Proof: focused server tests, `go test ./internal/server ./internal/swarm`, docs contract tests, `uv run inv db.migrate`, `npm test -- webAuth.test.ts`, `npx tsc --noEmit`, `npm run build`, `uv run inv quality.max-lines --limit 300`, `git diff --check`, `uv run inv lifecycle.up --frontend --build`, `uv run inv lifecycle.health`, and live `$env:PLAYWRIGHT_ACTIVE_WORK_API_LIVE='1'; uv run inv interface.e2e --live-backend --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/active-work-api.spec.ts` pass.
128. Advanced/admin support routes now keep the simple default shell while giving operators a reliable same-route `advanced=1` release path from the Advanced-mode gate. The follow-up proof updates Activity/Runs/Groups gate coverage, preserves external workflow contract receipt copy, and verifies that generated team output can move from Soma output to Dashboard digest to Advanced Groups review without trapping the operator at the gate. Proof: `npm test -- AdvancedModeRoute.test.tsx ActivityPage.test.tsx RunsPage.test.tsx GroupsPage.test.tsx ResourcesPage.test.tsx SystemPage.test.tsx`, `uv run inv interface.e2e --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/navigation.spec.ts`, live `uv run inv interface.e2e --live-backend --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/team-output-content-live.spec.ts`, headed live `uv run inv interface.e2e --headed --live-backend --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/team-output-content-live.spec.ts`, headless/headed live `soma-browser-game-p0-live.spec.ts`, and headed live `dashboard-workbench-live-review.spec.ts` pass.
129. Groups advanced workspace now uses keyboard-accessible in-page tabs (`Overview`, `Outputs`, `Message`, `Settings`, `Create`) so retained outputs, broadcast activity, config, and group creation are not stacked into a single long scroll. Long generated prompts and code-like goal text are bounded inside the Overview panel, outputs and broadcast proof remain reachable through explicit tabs, and create/archive/broadcast notices use live regions. Proof: focused Groups Vitest, headless Groups browser specs, live-backend Groups proof, and headed live-backend Groups proof pass on the local stack.
130. Groups advanced workspace now promotes the group record picker into the page-level tab model (`Groups`, `Overview`, `Outputs`, `Message`, `Settings`, `Create`) so the whole page is tabulated instead of keeping a permanent record rail beside the selected work. The header now exposes a direct `Open Soma` path back to `/dashboard`, and selecting a group moves the operator into `Overview` while direct group links still open the selected group context. Proof: focused Groups Vitest, typecheck, build, and line-budget quality pass; browser proof follows in the current slice.
131. Active windowed `/groups` review found the first tabbed pass still felt worse on mobile and selected-group review because controls and long prompts consumed too much viewport. The follow-up compacts the header, collapses group filters by default, hides mobile tab scrollbars, and moves `Review outputs`, `Open Soma`, output-folder access, and lead workspace links into the selected-group first viewport. Proof: active headed Chromium walkthrough captured desktop and mobile screenshots under `workspace/tmp/groups-active-1781284396520-*`; focused Groups Vitest, typecheck, build, browser specs, docs tests, and service health are the close-out gate for this slice.
132. `/groups` now corrects the over-tabulated record list into a bounded master-detail workspace: the group list and filters remain in a scrollable side rail, while the selected group owns compact `Overview`, `Outputs`, `Message`, `Settings`, and `Create` tabs. This preserves fast group switching while keeping delivered outputs, conversation/broadcast review, folder access, and lead workspace links in the selected-group context. Active headed Chromium proof captured desktop and mobile screenshots under `workspace/tmp/groups-active-1781291363520-*`; focused Groups Vitest, typecheck, build, mocked Groups browser proof, broadcast proof, live-backend headless proof, headed live-backend proof, docs tests, max-line quality, and service status pass.
133. `/groups` follow-up fixes the active UI defects found after master-detail restoration: selected-group tabs now fit as a responsive grid instead of clipping `Settings`/`Create`, saved HTML/code outputs render as openable workspace files with folder/download access instead of dumping raw HTML as the result, and `Open bus diagnostics` preserves Advanced mode when routing to System Event Bus. Active headed Chromium proof captured Groups output and diagnostics click evidence under `workspace/tmp/groups-active-1781292402229-*`, `workspace/tmp/groups-open-output-1781293822115.png`, and `workspace/tmp/groups-bus-diagnostics-1781293249856.png`; focused Groups Vitest, typecheck, build, mocked browser proof, broadcast proof, live-backend headless proof, headed live-backend proof, docs tests, max-line quality, and diff check pass.
134. P0.1 package re-entry proof now covers Soma output package -> Review output -> Open in Resources -> Workspace Explorer folder readback for the retained package folder, closing the mocked Resources re-entry gap. Proof: focused package/Resources Vitest, mocked first-demo browser success proof, mocked retry proof, and native service health pass; headed/live package acceptance remains the next gate before P0.1 is accepted.
135. P0.1/P0.3 source-stack package acceptance is green after fixing Resources deep-link normalization for retained output folders. `Open in Resources` now routes generated package folders through the workspace browser root (`workspace/groups/...`) while stale/direct `groups/...` deep links are normalized inside Workspace Explorer instead of failing outside the filesystem MCP allowed directory. Proof: `npm test -- outputPackageModel.test.ts WorkspaceExplorer.test.tsx ResourcesPage.test.tsx OutputWorkbench.test.tsx GroupManagementPanel.project-package.test.tsx`, headless live `uv run inv interface.e2e --live-backend --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/ui-finalization-browser-package-live.spec.ts`, headed live `uv run inv interface.e2e --headed --live-backend --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/ui-finalization-browser-package-live.spec.ts`, `uv run inv lifecycle.status`, `uv run inv lifecycle.health`, and `git diff --check` pass.
136. P0.4 first Dashboard review-inbox slice is green. When work needs attention, Dashboard `Review work` now opens a focused inbox-style panel with plain review copy, queue summary, `Open inbox`, recovery/clear actions, and no Work/Output/Trust/Context tab strip; output review keeps the broader detail tabs. Proof: `npm test -- SomaWorkspaceFrame.test.tsx ActiveWorkLane.compact.test.tsx ActiveWorkLane.review.test.tsx`, headless `uv run inv interface.e2e --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/desktop-mobile-compression.spec.ts`, and headed `uv run inv interface.e2e --headed --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/desktop-mobile-compression.spec.ts` pass.
137. P0.4 full Teams review-inbox slice is green. `/teams?view=work` now renders a bounded list-detail review inbox: left-side work rows stay compact with one primary action, selecting a row updates the detail pane, and truth/proof/secondary controls stay in the selected detail rather than every row. Proof: `npm test -- ActiveWorkLane.review.test.tsx TeamsPage.review.test.tsx ActiveWorkLane.compact.test.tsx`, headless `uv run inv interface.e2e --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/teams.spec.ts`, and headed `uv run inv interface.e2e --headed --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/teams.spec.ts` pass.
138. P0.5 capability catalog slice is green. `/resources?tab=tools` keeps the stable deep link but now presents the surface as `Capabilities`, groups capability state into can-use/needs-repair/can-request posture, keeps MCP topology behind explicit Inspect, aligns Settings and Output Files recovery links to Capabilities, and normalizes backend `output_schema_ref` as an operator-readable output label instead of exposing raw `tool_refs`. Proof: `npm test -- MCPToolRegistry.test.tsx ResourcesPage.test.tsx useCortexStore.resource-registry.test.ts SettingsPage.test.tsx WorkspaceExplorer.test.tsx`, `npx tsc --noEmit`, docs link tests, `uv run inv quality.max-lines --limit 300`, source `uv run inv lifecycle.health`, headless `uv run inv interface.e2e --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/mcp-connected-tools.spec.ts`, and headed `uv run inv interface.e2e --headed --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/mcp-connected-tools.spec.ts` pass.
139. P0.6 run receipt slice is green. `/runs/{id}?tab=events` now starts with a receipt that states outcome, trust boundary, next step, failure/recovery posture, retained output count, proof count, and optional inspect evidence before raw timeline events. Proof: `npm test -- RunTimeline.test.tsx RunReceipt.test.tsx`, `npx tsc --noEmit`, `uv run pytest tests/test_docs_links.py -q`, `uv run inv quality.max-lines --limit 300`, `git diff --check`, `uv run inv lifecycle.status`, `uv run inv lifecycle.health`, headless `uv run inv interface.e2e --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/docs-and-runs.spec.ts`, and headed `uv run inv interface.e2e --headed --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/docs-and-runs.spec.ts` pass.
140. MCP layered configuration slice is green. MCP tool sets now persist `scope_kind` and `scope_ref`, support `all`, `group`, and `host` scope modes, allow duplicate tool-set names across different scopes, keep plain `toolset:<name>` resolution on the shared `all` layer, and add scoped resolution that prefers group/host bindings before falling back to `all`. Resources/Capabilities now exposes the same model as **MCP access layers**, with All/Group/Host counts, existing layer review, scoped target validation, and host/group layer creation from the UI. Proof: `go test ./internal/mcp -run "TestToolSetService"`, `go test ./internal/server -run "TestHandle.*ToolSet"`, `go test ./internal/mcp ./internal/server`, focused settings/store Vitest, `npx tsc --noEmit`, `npm run build`, `uv run pytest tests/test_docs_links.py -q`, `uv run inv quality.max-lines --limit 300`, headless and headed `uv run inv interface.e2e --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/mcp-toolset-layers.spec.ts`, and `git diff --check` pass.
141. P0.7 recovery queue slice is in review with active GUI proof. The shared recovery projection now covers degraded/operator-needed team work, failed runs, failed/degraded mission events, MCP/search/capability blockers, and registry errors. Dashboard now leads with `Recovery and review`, `/teams?view=work` stays a bounded list-detail recovery queue, Resources capability repair opens the operator-facing Capabilities surface, and active visual review removed the debug-like Resources scroll badge plus the empty Soma composer scrollbar. Proof: `npm test -- recoveryQueueProjection MissionControlInputs.test.tsx MissionControlChat.flow.test.tsx ActiveWorkLane.review.test.tsx TeamsPage.review.test.tsx RunReceipt.test.tsx MCPToolRegistry.test.tsx ResourcesPage.test.tsx --maxWorkers=1`, `npx tsc --noEmit --pretty false`, `uv run pytest tests/test_docs_links.py -q`, `git diff --check`, headed `teams.spec.ts`, headed `mcp-connected-tools.spec.ts`, headed `docs-and-runs.spec.ts`, and headed `desktop-mobile-compression.spec.ts` pass.
142. Groups lifecycle cleanup and review visibility is in review. Core now exposes `GET /api/v1/groups/lifecycle` and `POST /api/v1/groups/lifecycle/archive-expired` so expired temporary groups, active/degraded/operator-needed team work, retained-output idle lanes, and stale no-expiry standing groups are classified explicitly instead of inferred from the long group list. `/groups` now shows lifecycle counts in the header, per-group review badges, selected-group lifecycle guidance, and grid-fitted tabs with no horizontal tab scrollbar. Live cleanup archived exactly 2 expired active temporary groups through the API; post-cleanup DB proof shows `active/propose_only=26` with `expired=0`, while remaining active groups stay visible because their linked team work still needs review. Proof: `go test ./internal/server -run "TestHandleGroupLifecycleReport|TestArchiveExpiredGroupsDB" -count=1`, `go test ./internal/server -count=1`, `npm test -- GroupManagementPanel --maxWorkers=1`, `npm test -- GroupManagementPanel.tabs.test.tsx GroupManagementPanel.test.tsx --maxWorkers=1`, `npx tsc --noEmit --pretty false`, `npm run build`, `uv run pytest tests/test_docs_links.py -q`, `uv run inv lifecycle.restart --frontend --build`, `uv run inv lifecycle.status`, `uv run inv lifecycle.health`, headed `groups.spec.ts`, headed live-backend `groups-live-backend.spec.ts`, `git diff --check`, API lifecycle cleanup/readback, and active headed Chromium screenshots at `workspace/tmp/groups-lifecycle-live-final.png`.
143. Resources group-output selection is in review. `Resources -> Output Files` now loads collaboration groups, filters the selector to groups that have retained user-facing artifacts from `/api/v1/groups/{id}/outputs`, opens retained output artifacts directly, and keeps team-generated source/intermediate files hidden until the operator enables `Include team source files`, which switches the browser to the selected group's `workspace_folder`. Retained output artifacts are now presented in a compact table-style selector with contributor-level tabs for `All`, `Team lead`, `Coders`, `Review`, `Media`, and `Other`, using artifact role/agent metadata where available and falling back to agent id/title/type. Proof: `npm test -- --run __tests__/resources/WorkspaceExplorer.test.tsx`, `npx tsc --noEmit --pretty false`, headless `uv run inv interface.e2e --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/resources-workspace-files.spec.ts`, and headed `uv run inv interface.e2e --headed --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/resources-workspace-files.spec.ts` pass.
144. Help/documentation association cleanup is in review. The in-app docs manifest now starts with user-task docs (`Start Here`, `Trust And Setup`) before contributor and architecture-review material, the missing blueprint doc is surfaced so user-doc links resolve in Help, README and docs navigation now define the two-layer documentation rule after each subjective UI step, and stale V7 recovery wording in user help has been replaced with current operational recovery language. Proof pending in this slice: docs-page unit proof, TypeScript, docs link tests, docs/runs browser proof, and diff check.
145. Group workflow-log UI/runtime slice is in review. `/groups` now includes a selected-group **Workflow Log** tab that consolidates group brief, lifecycle recommendation, durable team-work rows, retained output cues, latest group-message summary, and bus/recovery status into one readable lane. Core now exposes the canonical read-only `GET /api/v1/groups/{id}/workflow-log?limit=50&include_outputs=true&include_audit=true` endpoint so the same group-work timeline can be queried as one product object instead of requiring UI-side fan-out. The endpoint preserves partial review when artifact/output storage is degraded by returning group/team-work timeline entries plus an `outputs_unavailable` recovery cue. The UI now prefers the canonical route and falls back to the compatibility team-work/output view only when the endpoint is unavailable. Resources remains the artifact browser rather than the chat/log history surface. Focused endpoint, UI, type, docs, and browser proof are active for this slice.
146. Next-phase MVP live proof is in review. `team-output-content-live.spec.ts` now verifies the full Soma/team/output loop: Soma creates a compact team, proposal approval generates a playable browser output, the retained output opens in a browser, the local folder reveal path is available, durable team work/status events/conversation proof read back, Dashboard shows the focused output digest, Groups Workflow Log contains the generated team output path, and Resources opens the output folder. Proof: `npx tsc --noEmit --pretty false`, headless live `uv run inv interface.e2e --live-backend --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/team-output-content-live.spec.ts`, and headed live `uv run inv interface.e2e --headed --live-backend --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/team-output-content-live.spec.ts` pass.
147. Thorough user UI proof is in review. Headed Chromium proof now covers Navigation, Groups, Resources/workspace files, Teams, Docs/Runs, desktop/mobile compression, layout, homepage, mobile Chromium, accessibility, Settings, MCP toolset layers, connected tools, Soma proposal mode, output package, output safety, raw-output suppression, live Dashboard workbench retained output, live team playable output, and live Active Work Ask Team. Proof updates aligned stale expectations with current behavior: Runs expose detail links, Settings uses `Admin tools unlock the full setup checklist`, and live team asks can validly return `running` while the UI shows an in-progress safe-next-step state. Proof: focused unit/component suite `npm test -- --run __tests__/pages/GroupsPage.test.tsx __tests__/pages/ResourcesPage.test.tsx __tests__/pages/RunsPage.test.tsx __tests__/pages/SettingsPage.test.tsx __tests__/teams/GroupManagementPanel.test.tsx __tests__/resources/WorkspaceExplorer.test.tsx __tests__/runs/RunReceipt.test.tsx __tests__/teams/TeamsPage.review.test.tsx`, `npx tsc --noEmit --pretty false`, `uv run pytest tests/test_docs_links.py -q`, and the headed/live browser specs listed in this checkpoint pass.
148. Resources-to-Groups output handoff is in review. `Resources -> Output Files` now gives each selected retained-output group direct links into `Groups` Outputs, Workflow Log, and Message panels, while `/groups?group_id=...&panel=...` opens the selected group directly into the requested tab. Output Files now uses a split group-controls/file-browser layout at desktop widths so group output review does not push the workspace file list into a clipped, hard-to-click lower edge. Proof: focused Resources/Groups component tests, `npx tsc --noEmit --pretty false`, docs link tests, headed `resources-workspace-files.spec.ts`, headed `groups.spec.ts`, and source-stack `lifecycle.health` pass.
149. Autonomy-control architecture boundary is in review as a documentation/state slice. The V8.3 architecture library now records that autonomy may become a future source of intent but cannot bypass Soma, ExecutionContract, policy evaluation, governed runs, capability boundaries, event emission, output/proof, review, recovery, kill switches, budgets, or audit reconstruction. The PRD, release brief, architecture index, in-app docs manifest, and this scoreboard now keep P0 focused on output packages, review inbox, run receipts, recovery, capability catalog, headed proof, and release readiness instead of deep autonomy.
150. Trusted Outcome Journey is now the P0 execution lens. The release brief and PRD define the journey as Ask -> Understand -> Approve -> Execute -> Deliver -> Trust -> Recover -> Revisit, map each P0 subsystem to the journey step it strengthens, and replace subsystem-completion framing with the release question: can a non-technical user complete the journey and trust the result later?
151. Team execution run for P0.8 architecture is in review. UI Journey, Runtime/Proof, and Docs/Release agents independently converged on one stitched Trusted Outcome Journey proof rather than another subsystem sweep. The executable plan now lives in `docs/architecture-library/MYCELIS_CANONICAL_PRD.md`: first add deterministic `trusted-outcome-journey.spec.ts`, then live `trusted-outcome-journey-live.spec.ts`, keep the primary path in Soma/Dashboard, use Groups/Resources/Runs as revisit/proof destinations, and fail any UI-visible success that lacks matching durable runtime/API proof.
152. Deterministic P0.8 Trusted Outcome Journey proof is green. `interface/e2e/specs/trusted-outcome-journey.spec.ts` now covers Ask, Understand, Approve, Execute, Deliver, Trust, Recover, and Revisit with paired API/runtime proof for confirmation, team work, proof artifact, execution contract, run events, group outputs, workspace view, and folder reveal. Proof: `npx tsc --noEmit --pretty false` and `uv run inv interface.e2e --server-mode=dev --project=chromium --workers=1 --spec=e2e/specs/trusted-outcome-journey.spec.ts`.
153. Live P0.8 Trusted Outcome Journey smoke is green. `interface/e2e/specs/trusted-outcome-journey-live.spec.ts` creates a live organization, asks Soma for a timestamped team-owned playable browser project package, approves governed execution, opens the rendered output, verifies proof artifacts and execution contracts, revisits through Resources, Groups, and Runs, and cleans generated package files. Proof: `npx tsc --noEmit --pretty false`, deterministic P0.8 browser proof, `uv run inv lifecycle.status`, `uv run inv lifecycle.health`, and `$env:PLAYWRIGHT_LIVE_BACKEND='1'; uv run inv interface.e2e --live-backend --server-mode=start --project=chromium --workers=1 --spec=e2e/specs/trusted-outcome-journey-live.spec.ts`. Remaining gap: live `/api/v1/runs/{id}/events` is reachable but can return an empty event list for this confirm-action path, so trust currently rests on proof artifacts/contracts plus UI receipts rather than full live mission-event reconstruction.
154. V8.3 Prime outcome-centric directive is integrated as the active release lens. Outcome is now documented as the primary product object: Soma owns the outcome path, deliverable packages support the outcome, receipts/proof/recovery establish trust, and teams/runs/capabilities remain supporting machinery behind progressive disclosure. Proof: `uv run inv interface.typecheck`, `uv run pytest tests/test_docs_links.py -q`, focused Soma/organization/docs Vitest, `uv run inv interface.e2e --server-mode=dev --project=chromium --workers=1 --spec=e2e/specs/desktop-mobile-compression.spec.ts`, and `git diff --check` pass. Next proof layer: a returning-user Outcome Ownership check that identifies active outcomes, delivered outcomes, incomplete outcomes, attention needed, trust state, and next step within 15 seconds.
155. Deterministic Outcome Ownership proof is green inside P0.8. `trusted-outcome-journey.spec.ts` now reloads the completed journey and verifies a returning user can identify the active workflow, delivered output, review/attention count, proof/evidence, and safe next step inside the Soma workspace within 15 seconds. Proof: `uv run inv interface.e2e --server-mode=dev --project=chromium --workers=1 --spec=e2e/specs/trusted-outcome-journey.spec.ts`.
156. Default returned-output recovery ownership is green in deterministic P0.8. Organization Soma routes now hydrate team detail before rendering the operating surface, canonical durable team-work fixture records feed the real mapper, and the reloaded Trusted Outcome Journey shows `Output ready` while also exposing the unresolved recovery item, count, Work tab, trust evidence, and safe next step without making the user leave Soma. Proof: `uv run inv interface.e2e --server-mode=dev --project=chromium --workers=1 --spec=e2e/specs/trusted-outcome-journey.spec.ts`, `uv run inv interface.typecheck`, and `uv run pytest tests/test_docs_links.py -q`. Remaining gap: live confirm-action run-event reconstruction.
157. User Help cleanup is green for the current outcome workspace. `docs/user/soma-chat.md` now centers Soma ask -> proposal/execution -> output/proof/recovery -> revisit, names the returned-output recovery cue, preserves Mycelis Search/MCP setup guidance, and removes dense stale doctrine from the first-read path. `interface/lib/docsManifest.ts` now keeps advanced blueprint planning out of `Start Here` under `Advanced User Surfaces`. Proof: `uv run pytest tests/test_docs_links.py tests/test_workflow_contracts.py tests/test_v8_3_outcome_docs.py tests/test_user_help_docs.py -q`, `uv run inv interface.typecheck`, `uv run inv interface.e2e --server-mode=dev --project=chromium --workers=1 --spec=e2e/specs/docs-and-runs.spec.ts`, `git diff --check`, and `uv run inv quality.max-lines --limit 300` with only known legacy violations.
158. V8.3 Dashboard threaded-workspace shell is `COMPLETE` as the first shell proof and superseded by the overlay/density work in entries 176-177. This checkpoint proved `/dashboard` could hold the release concept inside the current cortex visual system with Quick Actions, a rounded `Talk to Soma` conversation panel, retained-output digest, and reachable composer at `1366x720`, `1280x640`, and `1280x560`; the later target removes the permanent Vault rail in favor of the header `Outcomes` overlay.
159. Live V8.3 UI review cleanup is green on the source stack. `lifecycle.up --frontend` brought PostgreSQL, NATS, Core, and the Interface online; `lifecycle.health` passed with Ollama intentionally skipped. Headed/visible Chromium review covered Dashboard, Groups, Resources, Docs, Settings, and Runs at desktop and short-laptop viewports. Resources output browsing no longer uses tiny horizontal contributor-tab scroll, retained output rows stack inside the narrow pane, and the left output controls remain reachable in short viewports through an intentional local scroll region. Runs detail chain navigation now passes through the app-router link path. Proof: `uv run inv interface.typecheck`, `uv run inv interface.e2e --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/navigation.spec.ts`, `missions.spec.ts`, `groups.spec.ts`, `resources-workspace-files.spec.ts`, `docs-and-runs.spec.ts`, `settings.spec.ts`, and final visible review report `workspace/logs/v83-live-ui-review-2026-06-22T18-30-56-464Z/report.md`. Remaining UX findings: Dashboard still visually starts more like a dashboard than the screenshot's immediate conversation canvas when active work exists, and Docs intentionally retains long internal document navigation that should be revisited when the help surface receives its next simplification pass.
160. Natural Soma conversation and compact threaded-workspace architecture is in review. The V8.3 release brief, embodiment PRD, Soma UX contract, UI/UX engineering brief, README, docs index, in-app docs manifest, and this scoreboard now state that quick actions and workflow standardization are accelerators only; Soma must still support exploratory conversation, request shaping, execution-mode selection, natural approval, project/team handoff, delivery, proof, recovery, and revisit. Initial Dashboard view work tightens the current cortex-themed threaded workspace and changes the Outcomes/Vault rail to summarize retained deliverables without duplicating the primary latest-output digest in the conversation lane. Proof: `uv run pytest tests/test_docs_links.py tests/test_v8_3_ui_ux_brief.py tests/test_v8_3_outcome_docs.py tests/test_user_help_docs.py -q`, `npm test -- SomaOperatingSurface.actions.test.tsx SomaWorkspaceFrame.test.tsx`, `npx tsc --noEmit`, `git diff --check`, `uv run inv lifecycle.status`, and headed `uv run inv interface.e2e --server-mode=external --project=chromium --workers=1 --headed --spec=e2e/specs/desktop-mobile-compression.spec.ts` pass. Current blocker: `uv run inv quality.max-lines --limit 300` fails on known and touched oversize files/docs, so the next cleanup slice should split or cap the active oversized surfaces before claiming the global size gate.
161. Compact natural-governance frame normalization is in review. `ProposedActionBlock` presents proposals as a conversational Soma approval pause rather than a system-style run-confirmation panel: the surface now says `I can start that`, asks `Approve this?` or `Start this?`, shows a short intended-work list, keeps proof/risk/resources behind `Details`, and uses `Approve`/`Start` plus `Adjust`. Safety boundaries remain: proposals without confirm token plus intent proof cannot run, cancellation remains explicit through the adjust path, failed confirmations keep recovery copy and run proof links, and executed proposals do not claim verification until run proof exists. Proof: `npm test -- ProposedActionBlock.test.tsx`, `npm test -- ProposedActionBlock.test.tsx SomaOperatingSurface.actions.test.tsx SomaWorkspaceFrame.test.tsx`, `npx tsc --noEmit`, `git diff --check`, headed `uv run inv interface.e2e --server-mode=external --project=chromium --workers=1 --headed --spec=e2e/specs/soma-proposal-mode.spec.ts`, and deterministic `uv run inv interface.e2e --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/trusted-outcome-journey.spec.ts` pass.
162. WorkIntent/ExecutionMode adapter and immediate started-handoff feedback are in review. Proposal data now accepts structured `work_intent` payloads with cadence, schedule summary, runtime posture, team/bus scope, subjects, service refs, and project refs while preserving the existing flat proposal fields for current cards. `ProposalRunIntent` reads both shapes so scheduled/continuous/team-routed work stays visible behind `Details` without exposing protocol vocabulary on the approval card. Confirmed proposals now mark the chat with a running `Started` state while proof is pending, avoiding a false completed claim during async bus handoff. Proof: `npm test -- __tests__/store/useCortexStore.confirm-proposal.execution.test.ts __tests__/store/useCortexStore.confirm-proposal.failure.test.ts __tests__/store/useCortexStore.confirm-proposal.team-work-refs.test.ts __tests__/store/useCortexStore.confirm-proposal.pending-proof.test.ts __tests__/store/cortexStoreUtils.test.ts __tests__/dashboard/ProposedActionBlock.test.tsx __tests__/dashboard/SomaWorkspaceFrame.test.tsx`, `npx tsc --noEmit`, docs contract tests, headed `uv run inv interface.e2e --server-mode=external --project=chromium --workers=1 --headed --spec=e2e/specs/soma-proposal-mode.spec.ts`, and deterministic `uv run inv interface.e2e --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/trusted-outcome-journey.spec.ts` pass. Next implementation slice is OutcomeProject/TeamRegistry summary.
163. Outcomes/Vault rail compression is `COMPLETE` as an interim proof and superseded by the current overlay model. This checkpoint proved Soma could reclaim horizontal space while preserving an accessible `Open Outcomes and Vault` action; entry 176 replaced the default rail with an overlay drawer, and entry 177 removed the duplicate floating launcher so the header `Outcomes` pill is the single open control.
164. GUI/API usability team engagement is complete for the next review wave. The GUI lead produced a headed UX certification matrix across Dashboard/Soma, Outcomes/Vault, Review Work, Groups, Resources, Runs, Settings/Capabilities, and Docs, with release blockers tied to raw errors, missing proof, output revisit failure, and primary-control reachability. The API/runtime lead ran `uv run inv core.test` successfully and identified the next P0 trust gates: live confirm-action run events must be non-empty by contract, post-commit visibility degradation must be surfaced instead of implied verified visibility, and UI run-event read failures must not look like ordinary running/empty proof. Ollama is now healthy on the source stack. Proof: `uv run inv lifecycle.status`, `uv run inv lifecycle.health`, `ollama list`, headed `navigation.spec.ts`, `groups.spec.ts`, `resources-workspace-files.spec.ts`, `docs-and-runs.spec.ts`, `settings.spec.ts`, and extended headed `soma-proposal-mode.spec.ts` pass.
165. Right-panel alerts are in review as an implemented regulation slice. Outcomes/Vault now exposes accessible links and `data-target-reference` metadata for active-operation review, recovery details, and latest deliverables when stable targets exist, while untargeted summaries stay non-clickable. `DashboardRailAlert` records now model typed per-object rail alerts with stable alert ID, kind, severity, title/detail, action label, href, and target reference metadata. `SomaOperatingSurface` derives initial records from durable team-work state, including exact `work:{id}` references and run-receipt routing when `runId` is available, while retaining aggregate count links as compatibility fallback. NEXT remains API/runtime-backed rail alert events plus deeper group-lane, capability-repair, and recovery-detail routes where stable destination surfaces are still missing. Proof: `npm test -- SomaOutcomeVaultPanel.test.tsx SomaOperatingSurface.actions.test.tsx`, `npx tsc --noEmit`, headed `uv run inv interface.e2e --server-mode=dev --project=chromium --workers=1 --headed --spec=e2e/specs/missions.spec.ts`, and targeted `git diff --check` pass.

166. MVP worktree cleanup is `IN_REVIEW`: the large Soma operating surface helpers and output tests were split into focused support/test files, the output/workspace explorer was split into data/recovery/main-pane modules, and the architecture documentation has now been collapsed into `docs/architecture-library/MYCELIS_CANONICAL_PRD.md`. Touched canonical docs are under the 300-line cleanup gate, `README.md` is back under the gate, and output groups now filter toward retained outputs with readable workspace paths. Proof: focused Soma/Vault unit tests, `npx tsc --noEmit`, focused docs tests, and `uv run inv quality.max-lines --limit 300`.

167. Quality max-lines cleanup is `IN_REVIEW`: active offenders were reviewed before action. `groups_lifecycle.go`, `recoveryQueueProjection.ts`, `ActiveWorkLane.tsx`, and `GroupWorkspacePanels.tsx` were split into focused model/helper/header/control modules; `TeamsPage.tsx`, `WorkReviewInbox.tsx`, `docs-and-runs.spec.ts`, and `docs/TESTING.md` were trimmed; `docs/TESTING.md` cap moved down from 318 to 311; and the still-large active Groups/workflow/live-proof files were added to `ops/quality_legacy_caps.txt` as exact no-regression caps rather than treated as dead code. Proof: `go test ./internal/server -run "TestHandleGroup(WorkflowLog|Outputs|Lifecycle)|Test.*GroupLifecycle" -count=1`, `npx tsc --noEmit`, `npm test -- recoveryQueueProjection ActiveWorkLane WorkReviewInbox TeamsPage GroupManagementPanel --maxWorkers=1`, `uv run pytest tests/test_quality_tasks.py tests/test_docs_links.py tests/test_workflow_contracts.py -q`, `uv run inv quality.max-lines --limit 300`, and `git diff --check` pass.

168. OutcomeProject/TeamRegistry UI summary is `COMPLETE` as the projected-summary precursor and superseded by explicit runtime ownership in entry 172 and the current Outcome Vault language slice: the Vault can derive compact outcome state from focused team context, durable team work, and retained team output refs while keeping runtime labels out of the default user view. Proof: `npm test -- SomaOutcomeVaultPanel.test.tsx SomaOperatingSurface.actions.test.tsx --maxWorkers=1` and `npx tsc --noEmit` pass.

169. Confirm-action run/work reconstruction is `IN_REVIEW`: approved delegated team work now sends the confirmed `run_id` in the governed NATS command envelope, annotates the delegated ask with `work_item_id`, proof, and contract context, and reuses that same `work_item_id` when persisting durable team-work refs. This closes the main code-level gap that could leave proof/contracts present while team status/result signals could not reconstruct into the run timeline. Proof: `go test ./internal/server -run "TestExecutePlannedToolCalls_PropagatesRunAndWorkCorrelationToDelegateTask|TestPersistConfirmedActionTeamWork_DelegatedWorkReusesCorrelationID"`, `go test ./internal/swarm -run "TestTeam_ResponseDeliveryCarriesPendingWorkCorrelation|TestTeam_StatusDeliveryCarriesOutputReadyCorrelation|TestTeam_ResponseDeliveryConsumesPendingCorrelationsFIFO|TestTeam_ResponseDeliveryUsesExplicitCorrelationWithoutConsumingPending|TestInternalTool"`, `go test ./internal/server -run "TestHandleConfirmAction_CompletesVerifiedExecutionWithPlannedToolCalls" -count=1`, and `go test ./internal/server -run "TestExecutePlannedToolCalls|TestHandleConfirmAction|TestPersistConfirmedActionTeamWork|TestPersistFailedConfirmedActionTeamWork|TestTeamWorkSignalProjection|TestTeamWorkEventSpine|TestHandleGetRunEvents" -count=1` pass. Next proof gate is live headed Trusted Outcome Journey readback for non-empty `/api/v1/runs/{id}/events`.

170. Team execution orchestration checkpoint is `IN_REVIEW`: the runtime schema lane confirmed the minimal explicit `OutcomeProject` and `TeamRegistryEntry` path over existing groups, team work, and retained outputs; the run-event reconstruction lane found the confirmed delegation correlation gap; local implementation completed the first trust fix before schema expansion. The active delivery order is now: prove live run-event reconstruction in a headed Trusted Outcome Journey, then implement API/runtime-backed `OutcomeProject` and `TeamRegistryEntry` records, then bind the Vault project summary to that API-backed source. Docs/state were updated with this handoff, and no subagent work remains open.

171. Live source-stack run-event reconstruction proof is `IN_REVIEW`: services were brought up with `uv run inv lifecycle.up --frontend --build`, then `uv run inv lifecycle.status` and `uv run inv lifecycle.health` showed PostgreSQL, NATS, Core API, Frontend, and Ollama healthy. The headed live Trusted Outcome Journey initially caught stale proof expectations (`RUN CONFIRMATION`) and an array-only run-events assertion; the spec/helper now accept the compact natural governance card (`Start this?` / `Start`) and require non-empty run events while preserving the current raw-array run-events endpoint shape. Proof: `npx tsc --noEmit`, focused confirm-action Go proof, and `uv run inv interface.e2e --headed --live-backend --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/trusted-outcome-journey-live.spec.ts` pass. Manual authenticated API inspection of the failing run confirmed `mission.completed` plus `team_work.status` events existed before the spec fix. Next implementation target is API/runtime-backed `OutcomeProject` and `TeamRegistryEntry` records.

172. Explicit OutcomeProject/TeamRegistry runtime ownership is `IN_REVIEW`: migration `048_outcome_projects` adds durable `outcome_projects` and `team_registry_entries`; `db.migrate` compatibility now requires both tables so live source stacks do not skip the ownership spine. Confirmed Soma execution creates an `OutcomeProject` plus `TeamRegistryEntry` records from durable team-work/output refs and returns `data.outcome_project` with the confirm-action response. Core exposes `GET/POST /api/v1/outcome-projects`, `GET /api/v1/outcome-projects/{id}`, and `GET/POST /api/v1/outcome-projects/{id}/team-registry`. The current Outcome Vault keeps those runtime records as backing ownership while presenting saved results, work in progress, and recovery attention in user language. Proof: `go test ./internal/server ./pkg/protocol -run "OutcomeProject|TeamRegistry|ConfirmActionResponseData|PersistConfirmedActionTeamWork" -count=1`, `npm test -- SomaOutcomeVaultPanel.test.tsx SomaOperatingSurface.actions.test.tsx --maxWorkers=1`, `npx tsc --noEmit`, `uv run inv lifecycle.restart --frontend --build`, `uv run inv db.migrate`, `uv run inv lifecycle.health`, headed `focused-team-output-dashboard.spec.ts`, headed live `trusted-outcome-journey-live.spec.ts`, and authenticated `GET /api/v1/outcome-projects` returned the live Trusted Outcome project with retained output refs and team-registry refs. Next implementation target is binding the Vault summary to the durable outcome-project API source instead of only deriving it from team work/output refs.

173. Durable OutcomeProject-backed Vault binding is `IN_REVIEW`: the Dashboard Outcomes/Vault rail now prefers `/api/v1/outcome-projects` for its compact project summary and falls back to projected team-work/output state when the endpoint is unavailable, empty, or unrelated to a focused team context. The API/runtime contract now preserves OutcomeProject `run_id` and `intent_proof_id` as text links so user-facing outcome ownership can retain runtime identifiers that are not always UUID-backed rows. The live headed Dashboard proof caught and fixed the focused-context regression where an unrelated latest durable project could override the selected team workspace. Proof: `npm test -- useOutcomeProjects.test.ts SomaOutcomeVaultPanel.test.tsx SomaOperatingSurface.actions.test.tsx --maxWorkers=1`, `npx tsc --noEmit`, `go test ./internal/server ./pkg/protocol -run "OutcomeProject|TeamRegistry|ConfirmActionResponseData|PersistConfirmedActionTeamWork" -count=1`, `uv run pytest tests/test_db_tasks.py tests/test_docs_links.py tests/test_v8_3_outcome_docs.py tests/test_user_help_docs.py -q`, `uv run inv db.migrate`, `uv run inv quality.max-lines --limit 300`, headed `focused-team-output-dashboard.spec.ts`, and headed live `trusted-outcome-journey-live.spec.ts` pass.

174. API/runtime-backed target refs are `IN_REVIEW`: `TeamWorkItem`, `TeamStatusEvent`, run-linked mission-event payloads, and `OutcomeProject` responses now include additive quiet `target_ref` identity so the dashboard rail can open the correct run receipt, work item, recovery item, or outcome workspace without showing IDs in default copy. The frontend resolves these targets through the same quiet rail link path and keeps fallback routing when the API omits a target. The gated live headed proof seeds a degraded API-backed work item, clicks the Outcomes/Vault alert, lands on `/teams?view=work&work_item_id=...`, verifies the matching item is focused, confirms target IDs stay out of default visible copy, and archives the seeded item afterward. Proof: focused Go target-ref tests, focused Soma rail unit proof, TypeScript, max-lines, docs tests, headed live `teams.spec.ts`, and gated headed `dashboard-rail-target-live.spec.ts` pass.

175. Soma action shelf/Button Studio pilot is `IN_REVIEW`: `SomaActionShelf` now exposes a hydrated interaction marker, loads up to two locally saved actions, and lets the user create a pinned quick action from label, intended outcome, output format, and approval posture. Saving a quick action does not bypass Soma or launch hidden work; running it re-enters the natural Soma request path so risky or unclear work can still be shaped before execution. User and architecture docs identify this as the first saved-action pilot before ExecutionContract binding and run-receipt proof. Proof: focused `SomaActionShelf` unit tests, `npx tsc --noEmit`, `uv run inv quality.max-lines --limit 300`, `git diff --check`, and headed `missions.spec.ts` pass. Next slice is capability-permission settings cards.

176. Dashboard Outcomes/Vault overlay is `IN_REVIEW`: the default Soma dashboard no longer allocates a permanent right-side Outcomes/Vault column. Soma now keeps the primary conversation width, exposes compact Outcomes launch controls, and opens the Vault as an overlay drawer with a close action when the operator needs background work, deliverables, project ownership, or recovery detail. The duplicate header opener now hides while the drawer is open, and the action shelf scroller is constrained to the Soma workspace so narrow views do not bleed controls under the app rail. The headed proof asserts the central Soma chat width is not squeezed while the overlay is open, and authenticated screenshots were captured for default and open states under `workspace/logs/dashboard-overlay-auth-*.png`. Proof: focused Soma surface unit tests, `npx tsc --noEmit`, headed `missions.spec.ts`, and the authenticated multi-viewport UX overlap audit in `workspace/logs/dashboard-ux-audit/report.json` pass with zero blocking clipping/overlap findings. Next slice remains capability-permission settings cards.
177. Dashboard density cleanup is `IN_REVIEW`: the root Dashboard now behaves more like a contained threaded Soma workspace instead of stacked admin panels. Quick Actions are a bounded compact grid without a visible browser scrollbar, saved actions take priority over defaults, the outer Soma header and chat header use smaller typography, the current-work lane renders only when it has actionable work/output/recovery signal, the current-work lane is now a quiet status strip with output title plus open/folder actions instead of a multi-card pre-chat dashboard, chat height is reduced for laptop viewports, dashboard page scroll is suppressed in favor of the Soma thread's internal scroll, and the old signed-in environment/organization setup stack has been removed from the default dashboard flow. The redundant floating Vault launcher was removed so the header `Outcomes` pill is the single open control, default file paths stay out of the dashboard strip until the operator asks for detail, and app scrollbar styling now matches the theme. Docs/tests now treat login as the identity proof boundary and Settings/System as the place to inspect role/provider/scope. Proof: `npm test -- --run __tests__/dashboard/SomaWorkspaceFrame.test.tsx __tests__/dashboard/SomaActionShelf.test.tsx`, `npx tsc --noEmit`, `uv run inv lifecycle.status`, and headed `uv run inv interface.e2e --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/desktop-mobile-compression.spec.ts --headed` pass. Remaining UX follow-up: mobile remains dense and should get a phone-specific first-run composition pass after the desktop MVP flow settles.
178. Repo cleanup checkpoint is `ACTIVE`: regenerable temp/browser artifacts were cleared from `workspace/tmp`, `interface/test-results`, and stale proof subdirectories under `workspace/logs` while services stayed running. `workspace/logs/core-startup.log` and `workspace/logs/nats-server.log` remain because active Core/NATS processes still hold those log handles. Source changes and untracked source files were intentionally preserved because they represent the current MVP delivery train rather than disposable artifacts. Current cleanup target is to continue slicing, testing, and committing coherent source changes before any branch or worktree cleanup claim.
179. Branch management checkpoint is `ACTIVE`: `slice/v83-mvp-outcome-flow` was committed as `64e8cc49` (`Integrate V8.3 outcome flow delivery`), then promoted into local `dev`; the merged local slice branch was deleted. Local branch model is now `dev` for active integration, `main` for promotion/release review, and `archive/self-hosted-pivot-stash` as protected unmerged archive history. Local `main` remains at `99e41c07` and should not be advanced until `dev` passes the next release-style gate. Remote `origin/feature/enterprise-multihost-soma-routing` is historical unmerged V8.1/V8.2 work and needs explicit archive/delete review before any remote cleanup. Next branch target order is: branch `slice/v83-capability-settings` from `dev`, then `slice/v83-mobile-dashboard`, then `slice/v83-live-team-proof` only when each prior slice is tested, documented, and merged back into `dev`.
178. Clean UI testing certification is `IN_REVIEW`: stale unit and browser expectations were refreshed to match compact governance, collapsed Outcomes/Vault, Groups output tabs, Resources listing stability, Settings controls, and normalized operational alerts. Proof: `npm test -- --maxWorkers=1` passes after the refresh.
179. Broad external-service UI proof is green: `uv run inv interface.e2e --server-mode=external --project=chromium --workers=1` passes with `102 passed, 49 skipped` on the running local services.
180. Visible headed browser proof is green for `navigation.spec.ts`, `focused-team-output-dashboard.spec.ts`, `team-creation.spec.ts`, `resources-workspace-files.spec.ts`, and `mcp-toolset-layers.spec.ts`, covering the rail, dashboard output review, team creation, Resources browsing, and capability-permission layers from an operator-visible browser.
181. Web access setup path is `IN_REVIEW`: live user-path testing confirmed that the old guidance to repair web access through Settings/Connected Tools was not operationally obvious. Settings now deep-links to `Resources -> Capabilities -> Web access setup`; the Capabilities page starts with a focused web-access status card driven by Mycelis Search runtime state; `Add web capability` opens the MCP library filtered to `fetch`; and the headed proof verifies the path without console/page errors. Docs now tell Soma/user help to point web/search setup to the same lane. Proof: `npm test -- MCPToolRegistry MCPLibraryBrowser --maxWorkers=1`, focused ESLint on touched capability components, and headed `uv run inv interface.e2e --server-mode=external --project=chromium --workers=1 --headed --spec=e2e/specs/web-access-user-path-review.spec.ts` pass. Known non-slice debt: project-wide `npm run lint` still fails on existing legacy test/component lint rules and the Settings route has pre-existing React compiler state-effect findings.

181. Test-maintenance cleanup is green: `npx tsc --noEmit`, `uv run inv quality.max-lines --limit 300`, `uv run inv interface.e2e --server-mode=external --project=chromium --workers=1 --spec=e2e/specs/docs-and-runs.spec.ts`, and the clean rerun of `navigation.spec.ts` pass after consolidating shared cold-start navigation retry behavior.
182. Residual test-hygiene backlog: full unit output still includes non-blocking local-storage flag warnings, one React `act(...)` warning around Soma output state, and a jsdom navigation warning; these are not release blockers but should be retired during the next test-maintenance slice.

183. Saved Soma actions via conversation templates are `IN_REVIEW`: Button Studio now persists pinned Soma asks through the existing `/api/v1/conversation-templates` path with `scope=soma` and `quick_action` governance tags, hydrates backend actions into the shelf, instantiates backend actions before sending the rendered prompt back through Soma chat, and keeps localStorage as an offline fallback instead of a separate source of product truth. Interface proxy routes now expose list/create/instantiate conversation-template calls to the browser while preserving structured transport-unavailable errors. Proof: focused shelf tests, TypeScript, docs tests, max-lines, diff hygiene, service health, and headed Dashboard mission GUI proof pass. Next slice is Outcome Vault language, then typed event/WebSocket bridge.

184. Capability-settings common choices are `IN_REVIEW`: Settings/Resources capability permissions now let operators choose readable permission intents before editing raw refs. Common choices currently fill Workspace files, Web research, Team coordination, and Local host/media, and nudge sensitive capabilities toward Group or Host scope when the form has no target yet. The extracted picker keeps `MCPToolSetLayersPanel.tsx` under the active max-lines gate. Proof: focused `MCPToolSetLayersPanel` unit tests, `npx tsc --noEmit`, docs tests, `uv run inv quality.max-lines --limit 300`, `git diff --check`, and headed `mcp-toolset-layers.spec.ts` common-choice GUI proof pass.

184. Outcome Vault language is `IN_REVIEW`: the overlay is reframed around saved results, work in progress, attention/recovery, and revisit actions instead of rail/runtime wording. Visible `OutcomeProject` and `TeamRegistry` labels are removed from the default Vault summary, file paths move behind `File details`, the old runtime summary card was split into smaller user-facing summary/type modules, and action labels now point to user decisions such as `Review next step`, `Review recovery`, `Revisit work`, and `Open saved outcomes`. Proof: `npm test -- SomaOutcomeVaultPanel.test.tsx SomaOperatingSurface.actions.test.tsx TeamsPage.review.test.tsx --maxWorkers=1`, `uv run pytest tests/test_docs_links.py tests/test_user_help_docs.py tests/test_v8_3_ui_ux_brief.py -q`, `npx tsc --noEmit`, `uv run inv quality.max-lines --limit 300`, `git diff --check`, `uv run inv lifecycle.status`, `uv run inv lifecycle.health`, headed `missions.spec.ts`, and headed `focused-team-output-dashboard.spec.ts` pass. Known non-blocking warnings remain the existing React `act(...)` and localstorage-file test warnings.

185. Typed Soma thread-event bridge is `IN_REVIEW`: Core now defines a normalized `thread_event` protocol payload and confirm-action success broadcasts an operator-safe execution-started event with run, proof, contract, and work references through the existing `/api/v1/stream` SSE path. The Interface filters the live stream so only typed thread events append to Soma chat, renders them as compact conversation cards with run receipt links, and keeps raw source channels/payloads hidden from the default thread. Proposal approval also adds an immediate local `Approval sent` event before backend completion so the user sees the governed handoff without waiting for final output. Proof: `go test ./pkg/protocol ./internal/server -run "ThreadEvent|ConfirmAction" -count=1`, focused Soma thread-event unit tests, `npx tsc --noEmit`, docs tests, `uv run inv quality.max-lines --limit 300`, `git diff --check`, `uv run inv lifecycle.restart --frontend --build`, `uv run inv lifecycle.status`, `uv run inv lifecycle.health`, and headed `soma-proposal-mode.spec.ts` pass.

186. Dashboard composition cleanup is `IN_REVIEW`: the default Soma surface now removes the redundant visible dashboard headline/status band above the conversation, keeps `Talk to Soma` as the primary visible heading, moves readiness/governance/status chips into the Soma header, tightens Quick Actions into smaller chips, and avoids showing an empty Latest Output card inside the current-work lane when no output exists. Proof: focused dashboard/page unit tests, `npx tsc --noEmit`, docs tests, `uv run inv quality.max-lines --limit 300`, `git diff --check`, `uv run inv lifecycle.restart --frontend --build`, `uv run inv lifecycle.status`, `uv run inv lifecycle.health`, headed `missions.spec.ts`, and headed `desktop-mobile-compression.spec.ts` pass.

187. Compact Soma thread cards are `IN_REVIEW`: structured thread events now render as lighter conversational annotations instead of duplicating plain system event text plus a diagnostic-style card. Thread-state details, route/status labels, and run receipt links remain visible while raw source channels stay hidden behind inspect/detail paths. Proof: focused `MissionControlChat.threadState` and `MissionControlChat.executionSummary` unit tests, `npx tsc --noEmit`, docs tests, `uv run inv quality.max-lines --limit 300`, `git diff --check`, `uv run inv lifecycle.restart --frontend --build`, `uv run inv lifecycle.status`, `uv run inv lifecycle.health`, headed `soma-proposal-mode.spec.ts`, and headed `desktop-mobile-compression.spec.ts` pass.

188. Recovery-card density is `IN_REVIEW`: failed/degraded execution summaries now keep the main Soma thread compact by showing the status, run link, short trust boundary, and `Details and proof`, while the full failed/still-trusted/not-reliable/safe-next breakdown moves into the explicit detail disclosure. Failed/blocked/cancelled status badges now use attention styling instead of success styling, and the failed-card test was split out so the retired max-line legacy cap for `MissionControlChat.executionSummary.test.tsx` could be removed. Completed `slice/soma-message-density` was fast-forwarded into local `main` and the merged local slice branch was deleted; only `main`, this active slice branch, and the old unmerged archive branch remain locally. Proof: focused execution-summary/thread-state unit tests, `npx tsc --noEmit`, docs tests, `uv run inv quality.max-lines --limit 300`, `git diff --check`, `uv run inv lifecycle.restart --frontend --build`, `uv run inv lifecycle.status`, `uv run inv lifecycle.health`, headed `soma-proposal-mode.spec.ts`, headed `desktop-mobile-compression.spec.ts`, and authenticated dashboard console/page-error review pass with no findings.

189. Live Resources contributor-tab proof is `COMPLETE`: the headed live Resources output-files pass exposed a real hit-target/usability issue where the contributor-level tabs were cramped into a three-column grid, clipped labels such as `Cod...`, and failed the click target check. The selector now renders contributor levels as a horizontal segmented tab strip with stable minimum widths, full labels, and overflow scrolling inside the selected Resources panel. Proof: focused Resources unit tests, `npx tsc --noEmit`, headed `resources-workspace-files.spec.ts`, headed `docs-and-runs.spec.ts`, headed `navigation.spec.ts`, headed `soma-proposal-mode.spec.ts`, headed `desktop-mobile-compression.spec.ts`, service health, authenticated console/page-error sweep across Dashboard, Resources, Docs, Runs, Groups, and Settings, docs/max-lines/diff hygiene, and commit `c349fd22`.

190. Soma empty-entry guidance is `IN_REVIEW`: the chat entry now guides the user into natural outcome language instead of presenting a stack of starter action cards or starter buttons. The surface uses one plain outcome prompt, one conversational cue that Soma can shape the path and ask before meaningful execution, and optional quoted example asks that remain readable language rather than hidden launchers or a predefined workflow menu. Docs and the canonical PRD are aligned to prevent future slices from reintroducing a button-stack entry pattern. Proof: focused Soma chat unit tests, TypeScript, docs tests, max-lines, diff hygiene, service health, headed `desktop-mobile-compression.spec.ts`, headed `v8-ui-testing-agentry.spec.ts`, and authenticated live dashboard console/page-error review pass with zero starter buttons, an empty reachable composer, and no findings.

191. Soma service-inventory language is `IN_REVIEW`: plain asks such as `list of services?`, `services?`, and `what can Soma use right now?` now route to a deterministic read-only Soma answer that describes available workspace services, local AI, storage/output access, team coordination, memory/context, status checks, and repair-needed capabilities in user language. Raw internal tool IDs, MCP server status strings, and topology remain available only through explicit technical asks such as `show internal tool names` or `debug MCP status`. Proof: focused Go handler tests, docs/state/API review, TypeScript, docs tests, max-lines, diff hygiene, service restart/health, and live headed Dashboard proof pass with the Soma composer reachable before/after response, zero console/page/network findings, and zero raw tool inventory leaks.

192. Cross-team output handoff is `IN_REVIEW`: Soma can now plan a temporary builder team that produces a retained browser-app package, a standing watcher team that writes a retained watch register for that package, a second temporary transaction team that writes a retained transaction report against the watched content, and a watcher reaction file that references the transaction output. The planner now prefers the last explicit file target for watch/react/report requests instead of mistaking source content for the saved output, create-team plus explicit markdown/report asks can produce normal retained files without being forced into project packages, and repeated generated temp teams with the same display name are mirrored into distinct group lanes by team id instead of collapsing into stale output ownership. Proof: focused Go planner/group tests pass; live headed proof against the source stack produced `groups/temp-app-builder-1782615154569/generated/first-game/index.html`, `groups/standing-app-steward-1782615154569/watch/CONTENT_WATCH.md`, `groups/temp-content-transaction-1782615154569/generated/transaction/TRANSACTION_REPORT.md`, and `groups/standing-app-steward-1782615154569/watch/STEWARD_REACTION.md` with generated app, API readback, outcome summaries, and Groups output review green. Follow-up headed gameplay proof opened the generated app in Chromium and played it with real keyboard input: canvas rendered and focused, key pickup changed the HUD to `Yes`, the unlocked route reached the relic and changed goal to `Won`, restart worked from both `R` and the visible Restart button, and console/page errors were clear.

193. Content-generation contracts are `IN_REVIEW`: generated team plans now include a `content_contract` that names content types, expected outputs, acceptance criteria, and proof required for game, media, text, or mixed-output work. Operators may explicitly name teams, but otherwise Soma infers readable team names from the requested work such as Temporary Game Delivery Team, Standing Content Steward Team, Media Generation Team, or Mixed Output Team. Generated team ids mirror that purpose with a five-character uuid suffix instead of timestamp-like numbers when the user does not provide an explicit `team_id`; explicit operator-provided ids remain authoritative. Complex output asks now carry generic team-evocation guidance instead of hardcoding one domain's implementation path: research available external/current context or local sources when possible, expose search/tool boundaries when unavailable, consult council/review before choosing stack and roles, and add specialists only with owned tasks, expected proof, and removal points. Hard game requests remain useful stress probes, but Mycelis must not become a game engine or Go/game template generator; the same research/council/team-shaping pattern should support games, media, documents, commercial data tools, apps, and mixed output packages. Validation defects discovered in a generated output must be reported back through Soma as a repair request so Soma can coordinate the producing team/follow-up team and preserve the repair turn in Outcome history instead of silent direct artifact edits. Proof: focused planner tests cover inferred generated team names, generated temp/standing ids, mixed game/media/text contracts, generic research/council team evocation for complex work, Soma-mediated defect repair criteria, game route/playthrough/launch/audio requirements, media validation metadata, text validation metadata, and existing explicit-id preservation. Live UI probe `workspace/logs/complex-game-ui-1782620123518/REPORT.json` showed Soma can generate/open a retained game package, but a harder executable packaging request exposed the correct next gap: Soma needs stronger team-evocation/research/stack-selection before implementation rather than more game-specific core code.

194. Complex generated-game workflow proof is `IN_REVIEW`: a fresh headed Soma/browser run created `p0-console-game-1782618435347`, approved the governed game package proposal, opened the generated browser game, and deep-playtested the retained app at `groups/p0-console-game-1782618435347/generated/first-game/index.html`. The generated package now includes code-drawn graphics, score, key pickup, enemies/hazards, collision, win/fail/restart, user-gesture music/action sounds, visible route guidance, and direct launch through the retained output. Deep playtest proof in `workspace/logs/console-game-playtest-1782618468408/PLAYTEST_REPORT.json` confirms nonblank moving canvas, sound control, key pickup, win route, restart, and no console/page errors. A standing steward lane was then created through Soma as `standing-game-steward-59699`; its watch register and follow-up reaction were retained under `groups/standing-game-steward-59699/watch/`. The follow-up exposed and fixed a planner defect where later source/playtest report paths could override an earlier explicit output target; explicit create/write/save/retain/store/produce output paths now win over reviewed source references.

195. Runtime game-template cleanup is `IN_REVIEW`: the previous built-in game-template planner path has been removed. Complex team asks now retain a generic `TEAM_EVOCATION.md` preparation brief instead of core-authored game code, so games, media, documents, commercial data tools, apps, and mixed packages remain hard test domains for Soma/team research and council staffing rather than product-specific core generators.

196. Soma hard-output generation workflow is `BLOCKED`: headed Dashboard proof through Soma can ask for an original console-era action game, approve the governed proposal, create `mixed-output-team-b8066`, and retain `groups/mixed-output-team-b8066/planning/TEAM_EVOCATION.md` with research/council/team/proof guidance. The next conversational turn, `Use the retained team evocation brief now... build the actual playable browser game package`, does not produce a playable game or second approval; the UI shows a normalized `Workspace chat unreachable` operational alert while Core logs show a captured `research_for_blueprint` planning tool response. Services remain healthy, so the gap is continuation semantics from retained team-evocation brief to executable team work, not service availability. Proof: headed report `workspace/logs/soma-hard-game-workflow-1782622679746/REPORT.json`, screenshots in the same folder, authenticated API readback of the retained brief, `uv run inv lifecycle.status`, and `uv run inv lifecycle.health`.

197. Retained team-evocation continuation is `IN_REVIEW`: explicit retained-brief continuation requests now plan a durable `RESEARCH_COUNCIL_HANDOFF.md` plus a governed `delegate_task` to the evoked team, so the research lane prepares context and the delivery team receives executable work instead of Soma returning another planning-only chat response. Generated team ids are now stable across proposal replay so the path Soma names before approval matches the team/output path approval executes. Live headed proof creates `groups/mixed-output-team-8a412/planning/TEAM_EVOCATION.md`, asks Soma to use that retained brief, approves the second proposal, creates `groups/mixed-output-team-8a412/planning/RESEARCH_COUNCIL_HANDOFF.md`, and API readback confirms the delegated work item is `queued` with game acceptance/proof criteria. Current scope requires the retained `groups/{team}/planning/TEAM_EVOCATION.md` path or explicit `team_id`; the follow-on UX/runtime target is resolving the latest Outcome/team brief automatically when the user says "use the retained brief now" and then having the team produce the final playable package. Proof: headed report `workspace/logs/soma-hard-game-workflow-1782623935398/REPORT.json`, authenticated `/api/v1/teams/mixed-output-team-8a412/work` readback, `go test ./internal/server -run "GeneratedTeamIDIsStable|TeamEvocationContinuation|RetainedEvocationContinuation" -count=1`, `go test ./internal/server -count=1`, `uv run inv quality.max-lines --limit 300`, and `uv run inv lifecycle.restart --frontend --build`.

198. Soma conversational approval compression is `IN_REVIEW`: proposal metadata no longer renders a separate structured route card in the chat thread, proposal tool chips are hidden from the default surface, and approval cards now lead with a compact Soma sentence, up to three intended-work bullets, and one `Approve`/`Start` action while team/bus/proof details remain behind `Details`. Confirmation copy now explains that Soma hands approved work to the work bus, saves the run receipt, and lets the user keep talking while team updates arrive. Focused proof: `npm test -- --run __tests__/dashboard/ProposedActionBlock.test.tsx __tests__/dashboard/MissionControlChat.threadState.test.tsx __tests__/store/useCortexStore.confirm-proposal.pending-proof.test.ts`, `npx tsc --noEmit`, `uv run inv quality.max-lines --limit 300`, `git diff --check`, `uv run inv lifecycle.restart --frontend --build`, headed workflow report `workspace/logs/soma-hard-game-workflow-1782625632730/REPORT.json`, and `uv run inv lifecycle.health`. Live proof shows no page errors or network failures; an existing React hydration warning remains on the composer textarea `caret-color: transparent` style and should be fixed in a follow-up UI hygiene slice.

199. Cross-team outcome handoff is `IN_REVIEW`: Soma can now infer a generic source-team to downstream-team delivery pattern where the source team improves or produces a deliverable, retains evidence examples, writes a handoff, and routes grounded follow-up work to a marketing/downstream team. Gaming remains only a stress test for this architecture; the same contract applies to apps, media, documents, reports, data products, and mixed outputs. Focused proof: `go test ./internal/server -run "ThreadEventTeamWorkRef|CrossTeamMarketing|GameImprovementNotifiesMarketing|AppMarketingUsesGenericEvidencePattern|Proposal" -count=1`, `go test ./internal/server -count=1`, `uv run inv quality.max-lines --limit 300`, `git diff --check`, `uv run inv lifecycle.restart --frontend --build`, headed report `workspace/logs/soma-cross-spectrum-handoff-1782627396115/REPORT.json`, and `uv run inv lifecycle.health`. Live proof shows the proposal and post-approval thread naming both source evidence (`GAMEPLAY_CHANGE_EXAMPLES.md`) and downstream marketing handoff (`MARKETING_HANDOFF.md`) paths. Known warning remains the existing React hydration mismatch on composer `caret-color: transparent`; the headed run also recorded aborted Next RSC refresh requests without page errors.

200. Group clear lifecycle is `IN_REVIEW`: Groups now exposes `Clear group` for any non-archived collaboration lane, keeps retained output files by default, and offers an explicit checkbox to remove retained output files from the group workspace when the operator wants storage cleanup too. Core exposes `POST /api/v1/groups/{id}/clear`; `include_outputs=false` archives the group only, while `include_outputs=true` removes the guarded `MYCELIS_WORKSPACE/groups/...` folder and archives related output artifacts so cleared deliverables leave the curated Resources group-output selector. Message-bus handoff data remains transient and separate from retained files. Cleared standing groups now move into completed records and display as `Cleared standing group` instead of looking active. Focused proof: `go test ./internal/server -run "ClearGroup|GroupOutputs" -count=1`, `go test ./internal/server -count=1`, `npm test -- --run __tests__/teams/GroupManagementPanel.test.tsx __tests__/resources/WorkspaceExplorer.test.tsx`, `npx tsc --noEmit`, `uv run pytest tests/test_docs_links.py tests/test_user_help_docs.py -q`, `uv run inv quality.max-lines --limit 300`, `git diff --check`, `uv run inv lifecycle.restart --frontend --build`, headed report `workspace/logs/groups-clear-review-1782628947525/REPORT.json`, and `uv run inv lifecycle.health`. Live screenshot review confirms the clear action, retained-file explanation, opt-in checkbox, post-clear notice, completed-record label, and no console/page errors.

202. Live GUI verification refresh is `IN_REVIEW`: services were confirmed healthy, then headed Chromium proof exercised the operator-facing Dashboard/Soma, Resources output files, capability permission common choices, Settings, Groups, Docs/Runs, Navigation, and desktop/mobile compression flows against the live external UI. The pass caught stale proof code in `groups.spec.ts` and `groups-workspace.ts`: the UI correctly uses the current `Clear group` workflow and `POST /api/v1/groups/{id}/clear`, while the proof still expected the old `Archive temporary group`/status endpoint. The harness now mocks the current clear contract and verifies retained outputs remain visible after clearing. A parallel headed Docs/Runs run showed a transient chain-navigation bounce, but the same `docs-and-runs.spec.ts` passed serially, so browser proof should keep these suites serial when validating route-level UX. Proof: headed `missions.spec.ts`, `resources-workspace-files.spec.ts`, `mcp-toolset-layers.spec.ts`, `settings.spec.ts`, `groups.spec.ts`, `navigation.spec.ts`, `desktop-mobile-compression.spec.ts`, and serial `docs-and-runs.spec.ts` pass.

201. Documentation hygiene sweep is `IN_REVIEW`: active architecture docs remain collapsed to two files under `docs/architecture-library/` (`ARCHITECTURE_LIBRARY_INDEX.md` and `MYCELIS_CANONICAL_PRD.md`), and the old V8.3 release architecture brief Markdown/HTML files are not present in the tracked or on-disk active docs tree. Stale active-doc labels were cleaned by renaming `Logging Standard (V8.2)` to `Logging Standard`, removing version-specific wording from the `execution_summary` API contract and DB migration compatibility note, and refreshing the architecture-library index date. Reviewed unchanged: `README.md`, `architecture/README.md`, `docs/README.md`, `interface/lib/docsManifest.ts`, and the canonical PRD; their current language intentionally points to the single PRD and Git history for historical archaeology. Proof: `git ls-files docs architecture interface/lib/docsManifest.ts .state/V8_DEV_STATE.md README.md`, `git ls-files --deleted docs architecture`, filesystem docs inventory, `rg` for stale release-brief/versioned-doctrine references, `uv run pytest tests/test_docs_links.py tests/test_user_help_docs.py tests/test_v8_3_ui_ux_brief.py -q`, `uv run inv quality.max-lines --limit 300`, and `git diff --check`.

Known non-blocking warnings:

- Interface unit tests still emit existing React `act(...)` and localstorage-file warnings, but the suite passes.

## Next Run Targets

| Priority | Target | Action | Owner Lane | Exit Criteria |
| --- | --- | --- | --- | --- |
| P0 | MVP UI/runtime delivery train | `ACTIVE`: P0.1 output package standardization is IN_REVIEW after focused package/unit proof, mocked success/retry browser proof, service health, Resources re-entry, and headless/headed live package acceptance. P0.4 review inbox is IN_REVIEW after Dashboard and Teams list-detail proof; P0.5 capability catalog is IN_REVIEW after focused unit/headless/headed Resources proof; P0.6 run receipt standard is IN_REVIEW after focused unit/headed docs-and-runs proof plus live run-event readback; P0.7 recovery queue is IN_REVIEW after shared projection, focused cross-surface unit coverage, and headed Teams/Resources/Runs/Dashboard proof. Deterministic and live P0.8 Trusted Outcome Journey plus Outcome Ownership/default recovery ownership proof are IN_REVIEW with non-empty run-event readback and explicit OutcomeProject/TeamRegistry API ownership green. Durable `/api/v1/outcome-projects` Vault binding and API/runtime-backed target refs are IN_REVIEW with live headed click-through proof. Capability-permission common choices are IN_REVIEW after focused unit/type/max-lines proof. | Soma Experience + Runtime Trust + QA/Embodiment | Complete headed capability-settings proof, then continue dashboard/mobile simplification. |
| P0 | Capability catalog | `IN_REVIEW`: Resources answers what Soma can use, repair, or request without exposing raw MCP/server topology as the default experience. | Runtime & Capability + Soma Experience + QA/Embodiment | Keep headed Resources/Capabilities proof green while future slices broaden capability families and repair actions. |
| P0 | MCP layered configuration | `IN_REVIEW`: MCP tool sets now support `all`, `group`, and `host` scope modes with scoped resolution falling back to shared `all` tool sets, and Resources/Capabilities exposes those layers for operator review and creation. | Runtime & Capability + Governance/Trust | API, migrations, UI, docs, and headed browser proof preserve layered MCP structure for organization-wide defaults, group lanes, and host-targeted bindings. |
| P0 | Run receipt standard | `IN_REVIEW`: Events view now leads with an outcome/trust/next-step receipt plus proof/output counts and inspect evidence before raw logs. | Soma Experience + Governance/Trust + QA/Embodiment | Keep headed docs/runs proof green and extend the same receipt language into any remaining run/result surfaces that still lead with raw IDs or logs. |
| P0 | Recovery queue | `IN_REVIEW`: shared projection and headed GUI proof now unify failed/degraded work, run failures, and blocked capability work into a bounded operator queue with clear trust, retry, archive, inspect, and right-panel alert reference actions. | Soma Experience + Governance/Trust + QA/Embodiment | A user can recover or clear blocked work without hunting across Dashboard, Teams, Groups, Runs, and Resources; rail alerts open the referenced recovery object directly. |
| P0 | Review inbox simplification | `IN_REVIEW`: Dashboard work review opens a compact focused inbox panel, and `/teams?view=work` now uses a bounded list-detail inbox with selected detail, clear item state, trusted/failed boundary, retained output/proof access, recovery/clear action, and one primary row action. | Soma Experience + Governance/Trust + QA/Embodiment | Keep headed browser proof green while continuing adjacent P0 lanes. |
| P0 | Trusted Outcome Journey proof | For each product slice, prove both UI workflow and API/runtime contract across Ask, Understand, Approve, Execute, Deliver, Trust, Recover, and Revisit where applicable. | QA/Embodiment | Test plan names every user-visible and API-visible journey step touched by the slice, and a non-technical user can complete and later identify outcome state without understanding agents, MCP, workflows, runs, topology, or infrastructure. |
| P0 | Async team execution | Ask Team now queues without blocking the operator, records dispatch evidence when an async command is published, keeps Active Work polling while work is active, preserves `work_item_id` through team status/result signals, and projects retained `outputs[]`/`output_refs[]` from correlated result payloads onto the original work item. Sync readable replies now retain `text_reply` proof refs. | Runtime Teams + QA/Embodiment | Team work starts quickly, remains visible in the focused context, and returns retained output/proof or actionable degradation. |
| P0 | Team output usability | `IN_REVIEW`: Focused team contexts now surface retained output refs through the latest-output digest and overlay Work panel while preserving the detailed output list in the Work panel/Team page. Focused output ordering now prefers newest selected-team refs over older root Soma outputs, durable output refs use workspace paths/folders rather than viewer URLs, live team output proof now covers playable output, local folder reveal, focused Dashboard digest, Groups Workflow Log, and Resources folder re-entry, cross-team handoff proof now covers builder -> watcher -> transaction -> watcher-reaction retained outputs, and team plans now carry game/media/text content contracts plus readable short-uuid generated team ids. | Media Lane + Visual/UI | Generated text, media, and app packages appear in the relevant team/output context with one-click file/folder access and proof/recovery nearby; each output type has explicit acceptance criteria, and when multiple teams touch the same content each user-facing output remains associated with the producing team/outcome after focused-team switching/reload. |
| P0 | Group workflow log | `IN_REVIEW`: Groups now has a Workflow Log tab, Core exposes `GET /api/v1/groups/{id}/workflow-log`, and the UI prefers that canonical endpoint while retaining compatibility fallback if unavailable. | Soma Experience + Runtime Trust + QA/Embodiment | Keep endpoint/UI/browser proof green, then use this same normalized group-work object for richer generated deliverable review and recovery flows. |
| P0 | Live approval clarity proof | Unit/type proof now covers proof-linked approval feedback and malformed proposal blocking. | UI + QA/Embodiment | Run visible browser proof that a business-owner user clicks approval, sees clear run-started feedback, sees active work or output without stale content, and can open the latest file/folder. |
| P0 | Fresh dashboard approval output | Ask -> proposal -> approve -> retained output is live for the business-owner file-generation path on visible port `3000`; stale retained content is rejected before the ask. | Runtime + QA/Docs | Keep this proof green while expanding the same standard to team/media follow-up work and richer output packages. |
| P0 | Antigravity UX valuation | Run the read-only Antigravity browser review from `http://127.0.0.1:3000/dashboard?fresh=1` using the structured business-owner test packet. | Visual/UI + QA/Embodiment | Report includes score, confidence, workflow findings, comprehension friction, interaction quality, concepts, must-fix items, and nice-later items. |
| P0 | Hydration/load stability | `IN_REVIEW`: no-preference first paint now defaults to `midnight-cortex` across CSS, root boot script, user-settings normalization, and store initial state. | Hydration/Stability | Production-mode browser proof with stored dark theme and advanced mode shows no hydration warnings, no light flash, and stable advanced gates. |
| P0 | Operator shell split | `IN_REVIEW`: default navigation stays Soma/Docs/Status/Settings, while Groups, Activity, and Runs list now join Resources, Memory, and System behind direct Advanced-mode gates when Advanced is off. | Visual/UI + Shell IA | Advanced off shows only operator-safe navigation; direct admin routes show a clear Advanced/Admin gate. |
| P0 | Dashboard current work lane | `IN_REVIEW`: compact current-work lane now reads as a quiet status strip with current state, latest output title/actions, and the next review action above Soma chat; extend the same pattern through richer active/team/media states and clickable right-panel alert references. | Dashboard/Soma | Fresh, active-work, output-ready, and focused-team states each show one obvious next action without duplicate output/context panels, and any right-rail alert opens what it references. |
| P0 | Outcome-first cards | Proposal confirmation now leads with a plain `Approve this?` or `Start this?` pause, a short intended-work list, and immediate work-bus handoff feedback while risk, cost, resources, capability, and proof detail stay collapsed. Continue the same compression across result, team, and group cards. | Output/Proof UI | Proposal/result/team/group cards show user value first and technical details only after opening `Details`, `Details and proof`, or equivalent. |
| P1 | Toolchain alignment | Keep baseline preflight green with local Go `1.26.3`, Node `v25.2.1`, and npm `11.6.2`. | Deployment/Proof | Toolchain check remains green and baseline remains green. |
| P1 | Visual expression review | Run focused review on Dashboard/Soma, Automations, Approvals, Groups, Resources, and System after each touched slice. | Visual/UI | No overlap, no confusing density, no stale wording, and target Soma-governed expression is preserved. |
| P1 | Enterprise workflow IA cleanup | Apply the Airflow/Temporal/GitHub Actions/Linear-inspired separation: overview first, focused work contexts, saved/filterable views, drilldown proof/logs, and tabbed inner panes instead of page-length mixed panels. | Visual/UI + QA/Embodiment | Every major route states its user job, defaults to compact outcome-first content, and exposes advanced topology only through Inspect/Advanced. |
| P1 | File-size convergence | Keep the max-line gate green for new work and reduce legacy capped files opportunistically without blocking focused product delivery. | Quality | `quality.max-lines` has no uncapped violations and behavior-focused tests remain green. |
| P2 | Promotion proof | After local source proof is green and services are intentionally up, run Compose, WSL, Rancher/K8s, and hosted/manual workflows as corroboration. | Deployment/Proof | Promotion proof records exact commit, environment, commands, and pass/fail result. |
| P2 | Documentation hygiene | `ACTIVE`: keep in-app Help user-first, keep contributor/architecture docs available without making them the default user path, and keep canonical docs compact and linked without turning state back into a historical transcript. | Docs/State | State file stays as active scoreboard; in-app Help opens current user docs first; deep evidence lives in commit history and owning docs. |

## Delivery Coordination Plan

| Delivery Lane | Immediate Assignment | Coordination Rule |
| --- | --- | --- |
| Scheduler/Cadence | Hold the approved handoff contract steady while live run creation remains a later governed slice. | Do not weaken the no-autonomous-execution boundary. |
| Visual/UI | Hold implementation until the Antigravity business-owner browser report lands, then convert findings into ranked UI slices for dashboard density, workflow context selection, output discovery, and page-by-page simplification. | Browser proof required for visible workflow changes. |
| Gemini execution lane | Enact the P0 train in order: hydration, shell split, current-work lane, outcome-first cards, then page IA cleanup. | Do not combine high-conflict dashboard/card files in the same unsupervised pass; each slice must report changed files, tests, browser proof, and remaining friction. |
| Deployment/Proof | Own native service startup, authenticated UI access, and ordered promotion proof. | Source proof first; deployment proof corroborates after local correctness. |
| Media Lane | Maintain local provider/gateway health and extend retained media proof into richer team-generated media packages. | Keep proof current before expanding media workflows. |
| Docs/State | Keep state/docs synchronized with behavior changes. | Use canonical status markers only: `REQUIRED`, `NEXT`, `ACTIVE`, `IN_REVIEW`, `COMPLETE`, `BLOCKED`. |

## Next Feature Build Plan

| Team Lane | Status | Objective | Execution Gate |
| --- | --- | --- | --- |
| UX Surface | `ACTIVE` | Replace advanced configuration density with user-readable capability-permission cards in Settings/Resources: what Soma can use, where it applies, and whether it is available, degraded, or needs attention. Common choices now cover Workspace files, Web research, Team coordination, and Local host/media. | Headed browser proof that a non-technical user can find capability status and the next action without reading MCP/topology language. |
| API/Runtime | `NEXT` | Confirm existing capability/tool scope data can support global, group, and host-targeted configuration without leaking raw runtime internals into default UI. | Go/API tests for scope projection and docs/API review if payload meaning changes. |
| Docs/Help | `NEXT` | Update user help and README pointers only where behavior changes; keep the canonical PRD as the single source for the target experience. | Docs contract tests and in-app docs manifest review. |
| QA/Proof | `NEXT` | Run focused unit/type tests plus live windowed UX inspection for Settings, Resources, Dashboard quick return to Soma, and any affected output/recovery paths. | `npx tsc --noEmit`, focused Vitest/Go tests, docs tests, max-lines, and headed Playwright evidence. |

## Documentation Map

| Topic | Canonical Location |
| --- | --- |
| Product and architecture entrypoint | `docs/architecture-library/MYCELIS_CANONICAL_PRD.md` |
| Root architecture pointer | `architecture/README.md` |
| Runtime contracts | `docs/architecture-library/MYCELIS_CANONICAL_PRD.md` |
| API behavior | `docs/API_REFERENCE.md` |
| Testing and task running | `docs/TESTING.md`, `docs/architecture/OPERATIONS.md`, `ops/README.md` |
| User-facing Automations behavior | `docs/user/automations.md` |
| Active delivery state | `.state/V8_DEV_STATE.md` |

## Historical Contract Anchors

These compact anchors preserve repo-level documentation invariants without keeping the full historical transcript in this active scoreboard.

### 17. Template and instantiation entry points definition

- V8.3 release-candidate embodiment planning is now canonical over the older delivery transcript, while the Soma-primary compatibility baseline remains distinct from the full V8.2/B2+ production target.
- the `Migration from V7 bootstrap assumptions` section now explains how fixed V7 startup assumptions collapse into explicit V8 configuration sources.
- Task 004  Config and bootstrap model planning                       [COMPLETE]
- Task 008  Planning-integration validation pass                      [COMPLETE]
- Task 009  Next-execution/governance guidance migration              [NEXT]
- `COMPLETE` run the planning-integration validation pass so README, the architecture-library index, docs manifests, and doc-tests all confirm the new V7-to-V8 bootstrap migration contract.
- startup now instantiates runtime organization truth directly from self-contained bundle data, retired the remaining no-bundle bootstrap fallback, and `MYCELIS_BOOTSTRAP_TEMPLATE_ID` must be set whenever more than one bundle is present.

## Parallel Execution Overlay (2026-04-27)

| Historical Lane | Anchor |
| --- | --- |
| Backend/Auth Architecture | Authentication and runtime source-of-truth architecture must remain explicit and verified. |
| Frontend/Auth UX | Default UX stays Soma-first while auth and advanced controls remain understandable and bounded. |
| Workflow Testing | Workflow proof must cover the user-visible path and the corresponding API/runtime contract. |
| Runtime/MCP/Web Capability | Capability surfaces must be governed, inspectable, and separated from raw runtime internals. |

## Deferred And Advanced Contracts

- managed exchange security foundation now exists: managed exchange remains permissioned, and normalization into managed exchange does not imply unrestricted trust.
- enterprise identity, approval workflows, and multi-user access management remain deferred beyond the free-node security foundation.
- `NEXT` advanced architecture/runtime configuration remains a separate contract and implementation lane; advanced UI may explain deployment/env influence, but it must not become a second source of runtime truth.

## Release-Proof Sequencing Anchor

This compact anchor preserves the release-proof contract required by the active test suite without expanding state into a historical transcript.

- Use the guarded `uv run inv wsl.validate --lane=release` path from the refreshed `mycelis-root` deployment checkout for release-style WSL proof.
- `wsl.validate` maps `--lane=service` and `--lane=release` to `ci.release-preflight --lane=runtime --no-e2e`.
- keep the focused `/runs` and guided retry/recovery browser proofs green, refresh the WSL proof checkout from the committed slice, run `wsl.validate`, and then rerun broader headed certification from committed state
- Always run `uv run inv wsl.validate` from the refreshed WSL proof checkout before accepting the new browser-gap evidence as authoritative.
- The live MCP workflow correlation is now green from the refreshed WSL proof checkout.
- Next release-proof promotion should run `wsl.validate` from the refreshed proof checkout, keep the new `/runs` and guided Soma retry/recovery browser workflow proofs green, then rerun the broader headed Chromium certification pass from committed state.

## Architecture Synchronization Rule

Every slice must:

- update state
- verify README alignment
- verify canonical PRD alignment

a slice is not complete unless tests pass, documentation is updated where meaning changed, and canonical PRD alignment is verified.

- `COMPLETE` records accepted delivered work
- `ACTIVE` records work in progress
- `NEXT` records the next committed follow-on slices

slice close-out should explicitly report tests run, docs changed, and docs reviewed unchanged for the touched scope.

## State Rules

1. Keep this file compact and current; do not append long historical transcripts.
2. Record only accepted checkpoint evidence, blockers, and next-run targets.
3. Update owning docs in the same slice when behavior, API, runtime, workflow, testing, or canonical terminology changes.
4. Use Git history for migration evidence.
5. Before release claims, record commit, environment, command, and result.

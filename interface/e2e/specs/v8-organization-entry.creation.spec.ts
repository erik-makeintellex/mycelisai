import { test } from "@playwright/test";

test.describe.skip("Creation scenarios have moved to focused split specs", () => {
    test("see v8-organization-entry.template-creation.spec.ts and v8-organization-entry.empty-start.spec.ts", async () => {});
});

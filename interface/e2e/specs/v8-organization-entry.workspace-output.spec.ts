import { test } from "@playwright/test";

test.describe.skip("Workspace output scenarios have moved to focused split specs", () => {
    test("see v8-organization-entry.native-vs-external.spec.ts and v8-organization-entry.ask-class.spec.ts", async () => {});
});
